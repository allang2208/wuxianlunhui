# game-dev Agent 守则

## 协作与读取

- 开工读 `SKILL.md` 索引，只读任务相关小节；了解项目进展看最近5条 `git log` 与 `CHANGELOG.md` 相关条目。历史案例不是现行流程。
- 共享工作区只处理本任务，保留用户和其他会话已有改动，不排查、回退或提交无关修改。自己的文件出现并行修改时基于最新内容合并，不覆盖整份旧快照。
- 提交按 `skill/15-git-workflow.md` 精确选择文件/代码块；禁止 `git add -A`、`git add .`、`git commit -a` 混入未知改动。
- 任务分流见 `skill/00-workflows.md`；game-dev-lessons 只读索引与命中小节。

## 素材阶段与动画前置

- 候选选稿、正式素材制作、运行时接入按用户当前范围推进；母图获准不自动授权接入。用户已指定方向、参考或同意继续时完成必要核对，不重复索要相同许可。
- 动画新制、重抽、视频转表先读 `skill/16b-animation-alignment-and-timing.md` §1.1；提交视频再读 `tools/ai-gen/WORKFLOW.md` §3.6。记录同类已认可动作参考、身体/脚尖方向、相机、根点和关键帧；身份母图与武器朝向不能代替身体朝向确认。
- 关键帧不合格不提交视频，原视频方向/身份不合格不进入抠图和入库。局部Alpha/去绿修补走局部分支，不重做获准动作。
- 来源和最小可重建依赖保留；不以“未被游戏直接引用”为由删除母图、源视频、模型、提示词或派生脚本。

## 用户自有5080设备授权

- `http://192.168.3.142:8188` 和SSH别名 `r5080` 为用户自有RTX 5080设备；用户已授权为其要求的项目任务完成必要生成、处理、查询、上传下载、远程脚本及模型/依赖操作，无需对同设备必要步骤重复询问。
- 范围覆盖建筑、角色、动画、物品、UI、场景、音频等资产及必要参考图、Depth、蒙版、视频、音频、提示词、参数、模型输入和生成元数据。
- 不扩展到其他主机/公网服务、无关代码/存档/凭据、清空共享队列、删除无关文件、停止无关任务、无限重抽或发布游戏。模型/批次/选稿/精修/接入仍按当前任务授权；不绕过系统审查。用户可随时缩小或撤回授权。

## 主代理与委派

- 主代理先确定需求、现有消费者、修改范围和不能破坏的契约。模型采用实际任务设置，不在本文件指定型号。
- 仅边界明确、机械性强且值得委派的UI/参数/重复改动使用 `spark_worker`；同一时间最多一个可写子代理。任务须明确文件归属、约束和交付条件；子代理不得处理无关改动。
- 核心战斗、AI状态机、存档、占格、输入、Buff和跨模块生命周期由主代理处理。子代理遇歧义返回主代理，主代理查看真实diff并完成必要局部合同核对。
- 测试权限由用户当前授权决定，主代理不能自行扩大给子代理。
- reasoning建议：明确小改用low，普通功能/单系统修复用medium，复杂根因/跨系统核心逻辑用high，困难未解问题才考虑xhigh。这是选档建议，不会自动修改已有任务或用户全局配置；不默认拉满，也不为小改强制委派。

## 测试与交付

- 默认完成后立即交付，由用户测试。未经当前任务明确授权，不运行单元/集成/E2E测试、lint、类型检查、构建、开发服务器、浏览器/CDP、游戏、性能或截图比对等验证。
- 主代理仍查看本次真实diff，并做实现必需的局部调用链/契约核对；不扩展成正式审计或全局扫描。
- “代码审计/代码审查”只授权审查，不自动授权修改或测试；“你来测试/测试并修好”等明确要求才授权相称测试。“检查一下/看看是否正常”不自动授权运行测试。
- 素材制作与检查按本次明确范围执行；离线结果不代表实机通过。用户反馈后只修反馈范围，不自行补跑测试。
- 交付列修改内容、文件和已知风险/重点验收行为；未测试时明确“未运行测试或运行时验证，按约定由用户测试”。

## 浏览器探针（仅获授权后）

- 禁止按进程名无差别终止msedge，包括 `Get-Process -Name msedge | Stop-Process -Force` 和taskkill全杀。
- 使用 `powershell -ExecutionPolicy Bypass -File tools\cdp-run.ps1 <脚本名.mjs>`；不需清残留时可直接 `node tools/<脚本>.mjs`。
- 使用独立 `Temp\edge-cdp-*` profile；跑完退出并删除自己创建的临时目录，不长时间保活。残留按 `tools/cleanup-c-drive-caches.ps1` 的限定范围处理，不碰正常浏览器。

## 固定EXE发布

- Vite是实时开发端，固定EXE是用户手动验收端；仅用户明确要求“同步/更新EXE”才发布，不定时或随修改自动发布。
- 发布用 `npm run release:test` / `更新EXE测试版.cmd`，启动用 `npm run start:test` / `启动EXE测试版.cmd`，不用开发窗口冒充固定版。
- 普通开发不改 `.exe-releases/` 已发布目录、资源或存档，不关闭/刷新用户正在测试的EXE。发布写新目录，成功才更新指针，旧版运行保持不变。
- 发布授权包含必要构建，不包含测试或启动游戏；发布前协调并行会话，快照复制期间不编辑待发布内容。细则见 `docs/exe-test-release.md`。
