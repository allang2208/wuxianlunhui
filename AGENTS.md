# game-dev Agent 守则

## 浏览器探针 (tools/cdp-*.mjs) 使用规则

- **绝对禁止** `Get-Process -Name msedge | Stop-Process -Force` 或任何按进程名
  无差别杀 msedge 的命令——这会杀掉用户正在使用的正常浏览器, 用户已经因此受害。
- 运行探针一律使用安全入口:
  `powershell -ExecutionPolicy Bypass -File tools\cdp-run.ps1 <脚本名.mjs>`
  (该脚本只清理命令行含 `--headless` / `edge-cdp` / `remote-debugging` 的无头
  Edge, 不碰正常浏览器; 若无需清理残留, 直接 `node tools/<脚本>.mjs` 也可以)。
- 探针每次使用独立临时 profile (`Temp\edge-cdp-*`), 与用户正常浏览器互不影响,
  不要为了"保险"去杀所有 Edge 进程。
- 探针运行完必须退出并删除自己创建的 `Temp\edge-cdp-*` 临时目录; 有残留时用
  `tools\cleanup-c-drive-caches.ps1` 清理, 不要用 taskkill 全杀。
- 不要长时间挂起探针 (如无头 Edge 保活数小时); 跑完即退。

## 知识库查阅约定（2026-08-19 SKILL.md 拆分后）

- 开工先读 `SKILL.md`（仅索引，约 12KB），按任务定位到 `skill/` 对应分卷再深入，**不要全文加载任何分卷**，只读相关小节。
- 了解项目进展：看 `git log` 和 `CHANGELOG.md`。SKILL.md 是"怎么做"的知识库，不是进度记录。
- 技能 `game-dev-lessons` 触发时只注入索引；教训详情按需读 `.agents/skills/game-dev-lessons/lessons.md` 对应小节。

## Codex Sol + Spark 编排规则

- 主代理默认使用 GPT-5.6 Sol，先理解本次需求及相关现有系统，再确定不能破坏的项目契约、允许修改的文件范围和验收标准。
- 只有在实现方向已经确定且边界清楚时，才委派 `spark_worker`。适用范围仅限：UI 微调、明确的参数或配置调整、重复性或机械性改动，以及方案、文件归属、约束和验收标准均已明确的编码。
- 同一时间最多运行一个可写的 Spark 子代理。禁止多个代理并行修改代码，避免覆盖共享工作区中的改动。
- `spark_worker` 不负责需求判断、架构设计、跨系统方案、调用链探索或核心逻辑决策；遇到歧义必须返回主代理，不得自行扩大范围。
- Spark 完成后，Sol 必须检查真实 diff，并按风险补查相关调用链、项目契约和边界条件；由 Sol 决定接受、要求 Spark 返工，或亲自修正。
- 跨系统核心逻辑由 Sol 亲自实现，包括但不限于战斗/伤害链、AI 与状态机、存档兼容、建筑放置与占格、输入控制契约、全局状态/Buff、跨模块生命周期和公共数据协议。
- Spark 不得处理或排查其他会话的无关改动，不得回退用户或其他代理已有修改。
- 未经用户或主代理明确要求，Spark 不运行测试、浏览器探针、构建或额外验证；最终是否验证以及验证范围由 Sol 决定。

## 默认交付与测试约定

### 浏览器开发端与固定 EXE 测试端

- 浏览器/Vite 是实时开发端；固定 EXE 是用户手动验收端。完整功能或大版本开发完成后，仍须用户明确要求“同步/更新 EXE”才发布，禁止定时同步或每次修改后自动发布。
- 固定测试版统一通过 `npm run release:test`（或 `更新EXE测试版.cmd`）发布，通过 `npm run start:test`（或 `启动EXE测试版.cmd`）启动。不得用 `npm run dev` / `npm run electron` 的开发窗口冒充固定测试版。
- 普通开发不得修改 `.exe-releases/` 下已发布版本、替换其资源、移动其存档、关闭或刷新用户正在测试的 EXE。发布必须使用新目录，成功后才更新启动指针；旧版运行中保持不变。
- 用户明确要求生成/同步 EXE 时，授权执行该发布所必需的构建，不代表授权运行测试或启动游戏。发布前协调并行会话，复制快照期间不继续编辑待发布内容。详细用法、存档隔离与首次发布见 `docs/exe-test-release.md`。

### 默认验证边界

- 正常开发任务完成后立即交付并汇报结果，由用户自行测试。除非用户在当前任务中明确要求，否则 Sol 和 Spark 都不得主动运行任何测试或验证。
- 默认禁止的验证包括：单元测试、集成测试、端到端测试、lint、类型检查、构建、启动或重启开发服务器、浏览器/CDP 探针、手工运行游戏、性能测试、截图比对和其他运行时检查。
- 默认不进行正式代码审计、全局扫描或扩展排查。Sol 仍需查看本次真实 diff，并进行完成实现所必需的局部调用链和契约核对；这属于交付前范围确认，不视为用户要求的正式代码审计。
- 只有用户明确说“进行代码审计”“代码审查”或同等意思时，才执行代码审计；代码审计本身不自动授权运行测试或修改代码，除非用户同时提出。
- 只有用户明确说“完全交由大模型进行测试”“你来测试”“测试并修好”或同等意思时，才运行与任务相称的测试、构建或浏览器验证，并可根据结果继续修复。
- “检查一下”“看看是否正常”等模糊表达不自动授权运行测试；默认完成实现后直接汇报，必要时说明尚未测试。
- 每次默认交付汇报必须明确写明：未运行测试或运行时验证，按约定由用户测试；同时列出修改内容、涉及文件和已知风险或需要用户重点验证的行为。
- 用户测试后反馈问题时，只修复反馈范围；修复完成后仍按默认规则直接交付，不自行补跑测试，除非用户再次明确授权。
