# 剑类待机/步行抓握手（2026-08-31～09-01）

用户已认可奔跑握拳与肩摆、步行握柄贴合。本次补上待机抓握，并修正步行中段小臂突然回摆的问题。

- 步行身体来源：`assets/character/walk.png`，原512×516、8列、21帧、24FPS；保留肩/肘和腿部轨迹、原帧序及播放时钟。不是重新生成的动作视频。
- 抓握来源：已确认单剑奔跑素材中的 `solo-run/fist/generated-fist.png`，沿用原ImageGen产物及其alpha；实际原提示词、工具与来源保存在同目录 `prompt.txt`、`provenance.json`。本次没有再次调用生成模型，没有覆盖奔跑素材。
- `rig.json` 保留21个原腕点、手旋转角、旧掌轮廓、遮挡区域和握心作为来源标定。`loopRepair` 只处理源帧5～13：按邻帧加权后的方向绕原肘关节刚性旋转小臂，同步变换手掌和握点，不拉伸小臂，不单独平滑剑柄。导出元数据的 `joints` 记录最终腕点/手角度及修正量。
- 旧手遮住的少量股骨像素沿用同帧无遮挡骨段补齐。源帧7～9旋转小臂后露出的骨盆，取同一walk源帧10的无遮挡局部，仅填旧手/小臂遮罩范围，不复制整条腿或移动骨链。步行身体/手层仍以512×516源尺寸登记，紧裁仅减少透明空白。
- 剑柄使用四把实际贴图各自的 `textureGrips`，保留110°低持角和1.5缩放；21帧世界偏移由新掌心换算，双份weapon配置同步。手掌在剑柄前景，武器按身体当前源帧定位，收回原生纹理后再让动画推进。
- 待机来源：`assets/player/idle.png`，516×516。保留原腕点/手臂，替换张开的主手；导出完整身体、去除盾副臂的身体、抓握手三层。盾副臂遮罩沿用 `PLAYER_SHIELD_ARM` 的原轮廓，运行时先完成盾臂姿态再覆盖主手层，下一帧按相反顺序归还纹理。
- 待机剑柄直接绑定新掌心并读取四把剑各自的 `textureGrips`，保留原105°低持角和1.5缩放。旧idle holdOffset以标定值[-52,18]为微调基准，由运行时和T面板共用 `getSwordIdleGripPose`。左右朝向共用镜像变换。
- 运行时只对真实 `weaponType === 'sword'` 的待机/步行启用；法杖/弓枪原图不动。剑盾/单剑跑姿、攻击123、突击/收势、盾臂防御动作、移动及战斗数值不改。
- T面板剑待机/步行使用相同图集与握点配置，法杖继续独立。连续步行时换武器会归还原手层可见性。

执行 `build.py --publish-config` 可重建正式图集、握点配置与离线预览；不带该参数只导出图集/元数据/预览及待写入的 `walk-frames.json`。这是素材组装脚本，不运行游戏或测试。

正式图集 `assets/player/sword-walk-grip/walk.png` 为2037×1448，单页RGBA约11.25MiB，包含21步行身体+21步行掌层+3待机层，共45帧。待机保持516×516独立源尺寸，不套用步行的512宽。来源元数据为 `data/player-sword-walk-grip.json`。

循环诊断：原源帧9→10（显示第10→11帧）握点突跳约13.34显示像素，修正后约2.76；手角度变化24°→约-1.05°。末帧20→0仍约0.69像素，原repeat=-1未改，因此本次修的是中段回摆，不是补开循环。数值来自离线素材坐标，不代表实机验收。

`four-swords-both-directions.gif` 是离线合成，不是游戏截图或实机验收。四剑：生锈长剑、骑士长剑、符文长剑、夜与火之剑，均含左右朝向完整循环。游戏保持21/24=875ms；GIF按10ms分辨率累计取整为880ms，不能拿GIF的微小量化差异判断实机步频。

`idle-four-swords.png` 是四剑双朝向的待机离线合成图，展示主手贴合；不包含实机举盾与状态切换验证。

本轮仅查看局部调用链、真实改动和制作中的素材；未运行测试或运行时验证，按约定由用户测试。重新加载开发版后重点看四剑待机转身、举盾/放盾、步行中段回摆、待机↔步行/奔跑，以及连续步行换法杖时原手层是否恢复。未构建、更新EXE或提交。
