# 突击 V3：体量稳定与回待机跟手（已确认并接入）

针对“突击略微缩小形变、recover回待机武器不跟手”的反馈制作。已认可的普通冲刺和游戏中的原突击身体PNG保留；V2候选输出在V3确认后的发布收口中删除，V3重建仍保留所需算法。

- 预备/刺出/收势主体采用同一块原跑姿第6帧躯干切片，头、胸肋、骨盆只旋转和平移，比例固定为1；不再让这些部位随多个网格点独立挤压。蹬地、前压、前100ms伸臂及80ms回震保留。
- 收势主臂从掌点解算并真实重画，剑使用同一解算结果。副手肩随身体展开，盾臂在整个收势内渐降，而非一直举着到末段突变。
- 最后160ms分别支持回原 `assets/player/idle.png` 或继续低持奔跑。回待机分支确实以原idle双手和装备角度为终点，不用跑姿冒充待机。
- 8个进入步相×2个退出状态，仍为600ms攻击、500ms收势、零新增前摇。没有修改伤害、移动或动作取消规则。

双击 `preview.html`，选择“收势终点：待机/继续奔跑”；可慢放、拖动时间、显示骨链并左右并排。完整循环为 `thrust-idle-both-directions.gif` 和 `thrust-run-both-directions.gif`。带掌点的接触表为 `thrust-idle-binding-contact.png`。

`native-recover-palm-source.png` 是现有游戏收势原图的局部放大与人工掌点，黄色主手、青色副手。重新标定发现旧主手表第3～6帧明显落在空白或前臂上，已按同一条近侧臂重新取掌点。运行时修复位于 `data/player-thrust-recover-poses.json` 与 `PlayerSwordShieldMotion._syncNativeThrustRecovery`：按实际纹理和帧读取14组掌点，剑盾共享身体坐标变换；回idle后主剑保持该动作的掌点绑定，直至切动作/卸盾等退出。原idle副手/右键防御链不变。

**当前边界：用户确认“可用，继续”后，V3身体与其最终解算掌点已同步接入开发版。** 原生收势与V3不是同一套身体，不能混用两者的最终掌点。原生掌点表保留给原生回退及动作后的真实idle主剑；V3双臂经过进一步解算，装备读取V3最终显示的掌点。

## 接入记录（2026-08-31）

- `../../export_thrust_runtime.py` 直接提取这版已确认的 `transition-data.js`，不重绘、不缩放原像素，仅裁透明边、复用身体并装箱。源预览中的candidate状态字段保留作生成时记录，正式批准状态写入运行时元数据。
- 输出：`assets/player/sword-shield-thrust-v3/`共9张图集；`data/player-sword-shield-thrust-v3.json`含180个身体、183组握点姿态、8条按当前腿步相索引的预备轨道、8条攻击进入轨道及idle/run收势轨道。理论RGBA基础占用117.47MiB，未测实际显存、帧率或加载耗时；原低持/普通冲刺图集69.89MiB另计。
- `BootScene`加载独立图集；`PlayerSwordShieldMotion`在原动画/物理同步之后覆盖显示，下一帧或切动作前归还原纹理与缩放。原生动画仍负责回调和状态，伤害、3次命中、位移、600ms攻击/500ms收势基准、装备覆写与输入规则均不改；覆盖时长仍按游戏当前总时长缩放。
- 奔跑预备读取已有冲刺就绪计时（等级阈值最后120ms），保持实际0～7腿步相。只在真实持剑盾、真实冲刺且当前技能为突击时进入；RTS单独跑步不触发预备。点击不增加任何等待，攻击按最近显示步相接入。
- 收势使用原复位时钟，前340ms共用轨迹；到末160ms前读取继续奔跑意图并固定出口，避免末段反复换轨。松开输入回真实idle，继续跑从第3步相衔接。若末160ms内再次改变输入，仍由原移动仲裁接管，不增加操作锁，可能看见该次出口改变。
- 回跑后若游戏计时已再次就绪，只将上身预备在120ms内缓入，不清空冲刺计时、不限制立即再攻击。原生完成回调先idle后run的一帧由显示端承接；回idle后主剑继续绑定原掌点，右键副臂仍由既有防御链处理。
- 两套已确认图集均按512方形源空间和玩家名义尺寸显示，避免原生512×516/516×516切帧把源缩放带入图集。V3回原idle也保持同一名义尺寸；不改碰撞、脚点、原PNG、三连击、防御伸手参数或其他武器分支。

本次只导出已确认素材、查看本次代码diff及必要调用点。未运行测试或运行时验证，按约定由用户测试；未构建/同步EXE。需重新加载开发版载入新图集；重点确认双朝向、握柄遮挡、躯干/腿部接缝、回idle/回跑、连续突击、右键/卸盾退出与纹理驻留。二维切片没有产生新的三维透视。
