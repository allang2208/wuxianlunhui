# 霜缚矛卒 MiniMax H3 四动作

本任务从已修正的右向母图制作待机、持矛奔跑、单次直刺与单向死亡。用途档为 `crowd`，正式纹理目标 32MiB、硬上限 64MiB。

方向硬合同为头、胸、胯、膝、两只靴尖和根点运动轴全部朝屏幕右；长矛朝右不能替代身体朝向。结构硬合同为两臂、两手、两腿、两靴、单根完整长矛和单个矛头。左向只由运行时水平镜像提供。

四条 H3 源片及正式精灵图已经完成并静态接入。正式奔跑使用 `[f61,f92)` 的单个同足相位周期，共 32 帧、1292ms；f92 为重复端点并排除。正式攻击只取 f14–f74 的第一次完整直刺，共 41 帧；源片窗口为2500ms，运行时按普通攻击标准等比重定时到1500ms，不删帧、不重复帧；f80–f123 的第二次进枪全部排除。死亡只取 f28–f72 的一次倒地，共 45 帧、1833ms，f72 为首个稳定尸体终帧。

四动作均只进行一次 RIFE 半步插帧；待机、奔跑和死亡保留源片墙钟，攻击仅调整最终运行时时长，不重新截帧或再次插帧。正式生产真源为 `sprite-sheet-manifest.json`，预算输入为 `sprite-budget-manifest.json`，直接查看预览位于 `previews/sprites/formal-final/*/`。四张运行时 PNG 位于 `assets/enemies/frostbound_spearman/`，实际 RGBA 闭包为 29.1738MiB，低于 `crowd` 档 32MiB 目标。

2026-09-01 对齐复核：四动作运行时均使用 `170/256=0.6640625` 的等比像素尺度，死亡仅因288px宽格而扩大显示宽度，显示高度与其余动作一致，不存在状态切换缩放。攻击双脚支撑中点位于源格 x101.25–105.75，运行时使用固定 `anchorX=104`，不做逐帧居中；矛尖在首次满伸帧的实际前伸为80.3516世界像素，因此起手和命中距离统一标定为82px。定向单目标普通近战已经启用，f10为首次满伸接触帧，f10–f22为贴图保持完整前伸的有效窗口。

静态核对通过右向身体轴、两臂两手两腿两靴、单根完整长矛、透明边界、空帧、重复帧与单次攻击/死亡约束；未运行游戏、浏览器、构建或运行时验证。
