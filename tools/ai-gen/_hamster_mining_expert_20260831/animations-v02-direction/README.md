# 仓鼠矿业专家 · 移动朝向修订

## 后续接入记录（2026-09-01）

用户已以“好的，接入游戏”接受本轮方向。空载源24..60每2帧、负重20..50每2帧，经BiRefNet和2×RIFE分别得到38/32帧24fps正式循环。原片保留，负重采用本目录已确认的去尾尖处理；完整制作与预览见 `../runtime/asset-manifest.json`、`../runtime/accepted-actions.gif`。下文为生成阶段历史状态，运行时仍待用户测试。


用户确认以既有军事单位的移动方向为参考继续MiniMax重抽，并要求把朝向确认变为动画开工前必读规则。本轮只重做空载与背紫晶移动；原角色身份和挖矿候选保留，不改现有军事单位或普通矿工。

## 提交前已完成的方向核对

- 主要参照 `hamster_heavy_machine_gunner/running.png` 的0、7、14帧，动作关键帧使用第7帧；参考配置与精确裁框见manifest。火枪兵辅助判断方向，不复制其快跑节奏；特战步态偏正面，不作为本角色的方向标准。
- 内置image_gen保留获准矿工身份，先生成侧向迈步关键帧，再从这张动作帧添加原款棕色背包和纯紫晶。两张1254方图的原始生成像素完整保留在 `references/`；提示词在 `prompts/`，直接编辑祖先和复制来源见 `reference-preparation.json`。
- 已并看原母图、重机枪动作帧、新空载与新负重关键帧，核对鼻尖、胸胯、膝/脚尖及横向迈步轴。保留轻微俯视和部分正面可见，不虚构精确相机角度。对照：`previews/keyframe-direction-check.png`。
- 两张图共用1254→1536补白→576等比缩放→1024×576横向补白，不非等比拉伸，不分别fit身体。使用迈步相位作为首尾，原视频仍须确认方向保持及循环节奏。

## 视频制作

通过统一入口 `ai-asset.py video generate --provider h3` 各提交一条，1024×576、5.17秒请求、124帧/24fps、20步、首尾同图、character-asset/visual-only，seed分别901601和901602。完整命令日志在 `logs/`，源MP4、同名provenance与24点联系图由客户端保存到 `videos/`。

两条源视频已完成。24点原片联系图与12点放大姿态图显示胸胯和步轴保持新版侧向，未见旧版持续迎面迈步；最终选稿仍由用户确认。空载末段步幅收小，后续需从自然运动段选循环；负重仍有较明显抬腿，负重感也列为选稿观察项。

原速GIF由 `export-preview.py` 保留全部124源帧和自然轨迹，按累计时间分配40/50ms帧长，总5170ms。原始三列对照为 `previews/direction-motion-comparison.gif`；推荐查看 `previews/direction-motion-comparison-tail-clean.gif`，其第三列使用下述局部去尾版本。对照中的重机枪按自身22帧/24fps循环，仅比较方向，不代表同一节拍或游戏体型。

负重原片再次出现长尾。`prune-carry-tail.py` 依据本次源帧重新标定 `(345,360)-(440,435)` 后腰ROI，保护粗主体及深色装备，只清理暖色细尾尖和附近孤立残片，允许短根。填补使用同一源帧空背景列300:325的逐行中位RGB，避免纯白补丁；124帧合计清理66313像素，单帧最多1135。原始MP4与原始GIF不覆盖，衍生结果为 `previews/carry-walking-h3-v01-tail-clean.gif`；逐帧记录、原图/掩码/结果对照及全身联系图均保留。

本轮没有截取正式循环、抠透明或RIFE；候选不替换正式assets，也没有修改身体轨迹、逐帧缩放或重新居中。当前选稿状态及保留的旧挖矿候选见 `manifest.json`。

工作规则已写入AGENTS、SKILL索引、第16b卷第1.1节、第02卷交叉索引和WORKFLOW第3.6节：每次动画任务开工都先读、核对并留记录。已有用户方向授权时由助手执行核对，不重复询问相同许可。

未运行测试或运行时验证，按约定由用户测试。未修改正式assets、配置、AI、数值、生产或存档；未构建、同步EXE、提交或推送。
