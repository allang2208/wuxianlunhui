# 仓鼠矿业专家 · MiniMax H3三动作

用户明确指定“使用minimax管线”，本目录为当前活动版本。三条原地动作分别是电钻挖矿、空载移动、背紫晶矿石移动；角色身份沿用已确认母图，负重状态采用同角色派生参考。不修改普通仓鼠矿工，不接入游戏。

**归档状态更新：本目录只保留正式采矿源和旧移动失败元数据。** 用户反馈后，军事单位对照确认旧移动胸胯与步轴偏向镜头；正式空载/负重源已迁到`../../animations-v02-direction/`。旧移动MP4、联系图和GIF在定稿清理中删除，提示词、种子和provenance JSON继续保留用于说明失败原因。

| 动作 | 种子 | 状态 |
| --- | --- | --- |
| 挖矿 | 831601 | v01完成；`previews/mining-h3-v01.gif` |
| 普通移动 | 831602 | 方向不合格，二进制候选已清理，仅留元数据 |
| 背矿移动 | 831604 | 方向不合格，二进制候选已清理，仅留元数据 |

统一通过 `tools/ai-gen/ai-asset.py video generate --provider h3`，在项目局域网5080 ComfyUI `192.168.3.142:8188` 制作。使用first-frame模式，同一参考同时约束首尾，5.17秒/124帧、24fps、1024×576、20步、res_multistep/simple、character-asset视觉档、visual-only。挖矿和空载各生成一次，负重返工一次，共4条源视频；负重v01种子831603保留作来源对照。

两张参考先沿用上一阶段1536²统一留白源，再以同一0.375比例缩为576²，居中放进1024×576白底。没有将方图非等比拉伸；参考转换见 `reference-preparation.json`。本目录的英文提示词是为H3单独准备的不可变快照，保留原动作语义。

正式采矿源视频及全部生成provenance JSON保留在`videos/`；旧移动视频、联系图和GIF已按最终方向结论清理。历史GIF曾按累计源时钟分配40/50ms帧时长，124帧总计5170ms；这一记录只说明当时的审阅方法，不构成当前活动输出。来源和清理状态见`manifest.json`与上级`cleanup-manifest.json`。

负重v01出现参考图没有的尖尾巴，v02使用独立提示词约束后腰后仍未解决，因此停止重抽。旧局部去尾脚本及其派生GIF随方向不合格候选一并清理；参数和删除像素记录仍保留在manifest/JSON中，仅用于说明失败历史，不再作为重建入口。

已查看三动作24点源联系图、12点局部放大图及去尾掩码对照。挖矿施压幅度偏轻；两种移动末段有回到站姿的短收势，后续正式循环需选取步态周期。普通移动偶尔露出小圆尾根，负重版也允许保留短根；本轮没有声称完全无尾或已经达到运行时无缝循环。

同图首尾只是生成约束，不证明中段装备完整或循环缝已通过。原视频/GIF均为动作候选，不是正式透明精灵表；需要看全程的方向、步相、钻头刚性、背包和晶石连续性后再选择。游戏内体量、动作切换和实际移动速度尚未验证。

未运行测试或运行时验证，按约定由用户测试；未改正式assets、AI、配置、数值、招募或存档，未构建、同步EXE、提交或推送。
