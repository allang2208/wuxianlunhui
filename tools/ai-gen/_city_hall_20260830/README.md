# 市政大厅：三时代定稿归档

当前唯一有效选择见 `selection.json`。本地开发版已接入玩家基地、科技换形与冷钢详情；**本次Git提交只发布素材、制作来源和知识记录，不包含尚依赖未合入共享协议的运行时接线**。发布边界见 `docs/city-hall-closeout-2026-08-31.md`。

| 时代 | 定稿与授权 | 正式素材 |
| --- | --- | --- |
| LV1 古典议事厅 | 首批B/v02；助手按用户明确的默认LV1接入指令选择，并非用户逐图选择 | `assets/terrain/city_hall_lv1.png` |
| LV2 自治市政厅 | 阳台B02/03作为48步精修输入，采用R01/02；保留二楼中央门、阳台及三侧护栏 | `assets/terrain/city_hall.png` |
| LV3 现代市政中心 | 用户选中的首批02；保留其屋顶与植栽，不再按旧否决意见修改 | `assets/terrain/city_hall_lv3.png` |

## 保留与重建

- 四份原始图：LV1/02、LV3/02、LV2结构B02/03及精修R01/02；对应生成JSON、完整提示词与真实Depth保留。
- 三时代初始可编辑模型保留。LV2还保留窗旗对称模型→钟楼后收模型→阳台模型→最终钟盘控制模型及必要修订脚本，早期模型是后续编辑的依赖，不算废案。
- 正式主体、三档光照图、每档尺寸/地台标定元数据，以及 `runtime/city_hall_three_eras.png` 最终总览保留。4×4逻辑占地不变，每档贴图独立使用strict视觉标定。
- 默认重建入口为 `runtime/install-assets.py`，只消费现存定稿raw；不重新生图、不复跑旧局部补墙、不按旧候选清单补生成。安装器需要显式运行，素材重建不等于授权覆盖游戏配置。逐档命令和显示参数见 `runtime/README.md`。
- `render-models.ps1`仅重建初始三档白模；LV2最终形态须沿保留的修订链处理，不能拿初始白模覆盖已接受控制模型。

## 清理记录

`cleanup-20260831.json`逐项记录116个已清理文件、字节和原因，共83.15 MiB。删除未选raw、被结构修订替代的旧窗台图/蒙版、重复对照图、Blender备份与可重建body/日志；未改动正式PNG。历史批次JSON、旧制作说明及 `selection-history.json` 仅用于追溯，可能含已移除路径，不再作为活动来源清单。

未运行测试或运行时验证，按约定由用户测试。素材预览是既有离线制作证据，不代表本次做过游戏或视觉验收；本轮未构建或同步EXE。
