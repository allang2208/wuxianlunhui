# 仓鼠野战炮组：已接入游戏开发版

当前状态：**四个已确认v01动作均已导入，二级野战炮的招募、科技、战斗、升级、人口、存档实体工厂、按需纹理与图标已接齐。** 详见[RUNTIME-DELIVERY.md](RUNTIME-DELIVERY.md)。`spritesheet-manifest.json`为当前帧表/时长/脚点真源，`sprite-budget-manifest.json`为完整运行时资源族清单；源动作验收记录保留在`task-index.json`。未运行测试或运行时验证，按约定由用户测试，固定EXE未更新。

攻击烟焰已在画布内柔化消散，原四动作轨迹和时长不变。当前收尾重建流程见`../_engineering_line_completion_20260830/DELIVERY.md`；本目录以下为历史素材生产记录，早期候选文档中的“尚未导入”不代表当前状态。

## 来源与动作

- 来源：已确认的 `hamster_field_cannon_crew-mother-v07-infantry-camera-white.png`。
- `prepare.py` 仅按固定比例缩小并加白色留边，不重绘、不拉伸；1024×576 画布供全部动作共用。
- 待机：呼吸、眨眼、持杆姿态，炮车静止，首尾循环。
- 移动：两只仓鼠原地向右行走，炮手推车、装填手持杆，车轮随行走旋转。
- 攻击：预装填单次射击，右侧真实炮口闪光，古典炮车整体后坐后复位，不使用现代炮管滑轨动作。
- 死亡：两只仓鼠分别倒下，炮车保留，末尾静止、不起身。GIF 仅为便于预览而循环。

## 生成与交付边界

统一入口为 `tools/ai-gen/ai-asset.py humanoid video`，明确指定 H3；每动作一个候选、124 帧、24fps、20 steps。实际成片可能偏离动作设计，需先查看候选再决定是否采用。

`generate.py` 会把留白后的参考图及四份动作提示词上传到 `http://192.168.3.142:8188`，并将视频和来源记录下载至本目录。初次运行在上传连接建立前被沙箱拒绝；随后提权请求被安全审批器拒绝，理由为原授权仅覆盖投石机，不覆盖本次野战炮素材。已向用户说明实际素材和目标服务器，用户明确回复“同意”，本轮据此完成授权后提交。

生成入口为 `../ComfyUI/.venv/Scripts/python.exe -u tools/ai-gen/_hamster_field_cannon_animations_20260830/generate.py`，已有任务时不要再次提交。成功下载全部四个视频后，用同一 Python 执行本目录 `make_previews.py` 生成可重复播放的 GIF，保留原视频时长。

`logs/*-sandbox-blocked.log` 保留上轮失败证据，不是生成结果；本轮正式日志为 `logs/*-v01.log`。脚本会拒绝覆盖已有正式日志或视频，以防不明状态下重复提交。

本阶段只准备候选；不改战斗、招募、科技树或正式运行时资产。不运行游戏测试、构建或运行时验证。

## 透明图集制作

`prepare_sprites.py` 提取制作参考并登记固定比例/脚点；`make_sprites.py keys` 通过统一入口相同的 ComfyUI-RMBG 模块批量抠图并保存关键帧，`interpolate` 调用现有 RIFE 2×，`package` 从最终源帧时长生成图集清单、预算清单和可循环预览。不要把缓存中RIFE工具的固定帧率GIF当作最终播放时钟。

慢速段每4个源帧取关键帧，射击52–66和倒地36–58保留全部原生姿态。四动作同为0.35制作比例与(512,450)源画布脚点；不逐帧拉直或按包围盒缩放。炮口外侧漏抠烟火采用限定区域白底透明度反推，原片右边界裁切不补画。`cache/` 是可再生中间文件，不进入Git。

最新成品为62/62/85/97帧，均约5.167秒，四张RGBA图集共57.24MiB。源动画已获确认，透明及插帧成品仍待用户查看；未运行独立预算检查或游戏验证。
