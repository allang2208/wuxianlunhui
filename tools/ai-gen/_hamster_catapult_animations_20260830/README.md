# 仓鼠投石组四动作制作

已按用户要求使用 **H3** 完成四动作。2026-08-30用户确认“可用，导入游戏”，已接入源码与工程师营地招募；最终产物与状态以 `task-index.json` 为准，玩法与待测项见 `docs/hamster-catapult-crew.md`。

- 母图：工程兵母图任务中的 `hamster_catapult_crew-mother-v07-infantry-camera-white.png`。
- 构图：略俯视、朝右，两只等大仓鼠与一台投石机；只等比缩放并补白到 1024×576，没有重绘母图。
- 待机：抱石与握持、呼吸、眨眼，循环。
- 移动：搬石手抱石步行、操作手推车，轮子转动，循环。
- 攻击：装填一颗石头、释放、向右投出、绞盘复位，一次性动作。
- 死亡：双人倒地、器械停稳，末帧保持，不回绕。

## 授权与来源

2026-08-30：最初上传被沙箱与自动安全审查阻止。随后用户明确回复“同意”，批准将本任务母图和四份动作提示词发送至下述目标，已恢复生成。

实际脚本目标是 **http://192.168.3.142:8188**（局域网 RTX 5080 主机上的 ComfyUI；5080 是显卡型号，不是端口）。请求发送本任务的补白参考 PNG 和四份动作提示词，完成后下载视频与 provenance。未传输其他项目素材。

生成入口为 `tools/ai-gen/ai-asset.py humanoid video --provider h3`，单动作 5.17 秒、1024×576、20 steps、单候选，种子依次 830101–830104。四份不可变提示词位于 `prompts/`。待机/移动同时使用相同首尾帧；攻击只锁首帧并恢复器械；死亡只锁首帧且不回绕。

`make_assets.py` 提供离线 `preview`、`keys`、`interpolate`、`package` 四阶段。源视频不修改；BiRefNet 保留全部主体，避免删掉独立搬石手。四动作最终共用参考画布的 0.35 有效比例、共同原点 (512,460)，只按各动作整体透明并集对称紧裁，不逐帧缩放/居中。RIFE 分别处理 RGB 和 Alpha，保留自然倒地高度；最终 GIF 按源视频时间间隔播放，一次性动作不插首尾回绕。死亡动作仍为单次，审阅链接改用 `previews/die-preview-loop-v02.gif` 循环展示；只添加 GIF 重复播放标记，旧单次预览已可恢复归档，当前循环预览保持同样帧与时长，帧和时长不变。较早期 0.375 候选统一缩小约7%以控制四动作及石弹容量；没有删帧或加速。

制作比例0.35不变，导入统一运行时比例0.675（displaySize=345.6），按工程师主体高度而非器械Alpha外框选取；实际同屏尺度、正式预算检查和实机动作由用户验收。`import_runtime.py`只复制已确认的正式图并派生双份配置，不运行游戏或测试；重新制作候选后必须确认，再执行导入。

攻击 v01 因摆臂越出原视频顶部判废。v02 仅将同一参考画布等比缩到 70% 并置于 (154,153)，留出上方摆臂空间，种子 830203；不改变母图视角或人物比例。后处理按已知参考变换使用 0.35/0.7 的缩放和变换后的锚点，恢复与其他动作相同的有效主体像素比例，不使用逐帧 Alpha 自适应缩放。活动来源和裁帧参数以 `action-specs.json` 为准。

攻击原视频第42帧首次明确离勺，对应成品第32帧（均从0计数）。第42/43帧的独立石弹按明确区域拆出，`final/stone.png` 与单位表分开保存，已接入游戏发射时钟与抛射弹道。攻击摆臂段、死亡受击/倒地段增加源关键帧，逐帧时长仍忠实保留原片。

源码接入包含科技解锁、招募、战斗、动画配置及按需资源驻留。攻击后空手切待机恢复备弹姿态，未另制取弹过渡；后两级野战炮组和榴弹炮组均已接入源码，榴弹炮另有取弹衔接。

未运行测试、构建或游戏运行时验证。
