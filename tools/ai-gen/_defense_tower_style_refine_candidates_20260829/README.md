# 防御塔材质二轮候选（2026-08-29）

本目录只保存临时候选，不覆盖运行时资源。

固定合同：

- 模型、30 度正交相机、透明区域、324×498 底座画布与 261×164 炮臂帧画布不变。
- 正式炮臂表保持 48 帧、8×6 网格（2088×984），与运行时 Phaser 帧布局一致。
- 底座和炮臂使用正式资源 Alpha 逐像素锁定。
- 预览按当前运行时标定合成：底座约 279.31×429.31，炮臂装配缩放 1.643002，2×2 接地菱形 256×128。
- 候选阶段只渲染炮臂第 0 帧；用户选定后才统一重渲染 48 帧，避免逐帧 AI 重绘造成旋转闪烁。
- 正式资源、缩略图与 silhouette / projection / height / normal 均不在本轮候选阶段修改。

三版方向：

1. 桥接型：风化暖灰混凝土 + 分段枪灰钢 + 旧黄铜 + 克制青蓝识别标。
2. 中世纪工业型：矿物骨料混凝土 + 锻黑钢 + 氧化青铜，无发光。
3. 现代防御型：冷灰预制混凝土 + 蓝黑钢 + 暗黄维护标 + 微量青蓝。

材质源使用 FLUX.2 Dev 生成，Blender 仅替换材质并沿用既有几何和相机。

当前总览：`previews/defense_tower_style_refine_contact.png`。

用户已选择 3 号现代防御型。正式底座、48 帧炮臂和 128×64 缩略图已经替换；
silhouette / projection / height / normal 已按单建筑增量入口重建。模型、相机、
透明区域、碰撞与玩法配置保持不变。

定稿瘦身已完成：1/2 号候选图片和可再生成的 48 张 1024² 原始旋转帧已删除；
保留 3 号原始材质、prepared 材质、Blender 渲染、锁 Alpha 定稿、正式 48 帧表、
八方向预览、生成脚本与参数记录。被删内容可由保留的提示词、seed 和脚本重新生成。
