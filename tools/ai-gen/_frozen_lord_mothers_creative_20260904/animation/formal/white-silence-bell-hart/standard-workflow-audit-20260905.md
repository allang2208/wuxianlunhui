# 白寂鸣钟鹿六动作标准工作流复检（2026-09-05）

## 结论

用户已确认六动作总览 GIF 可用。按 `SKILL.md` 动画路由、`skill/16b-animation-alignment-and-timing.md`、`skill/16-character-sprite-production.md`、`tools/ai-gen/WORKFLOW.md` §3.6 和 `game-dev-lessons` 62–65 复检后：

- 原视频方向、相机、四足拓扑、一钟三坠与动作语义通过。
- 六张正式表的像素/Alpha/布局/插帧/时钟门通过。
- `stride` 回绕循环通过。
- 家族为 Boss 档，闭包 `140.188 MiB`；超过 128 MiB 目标但低于 256 MiB 准入线，已有用户要求补齐动作的目标线例外。
- 当前仍是素材层，未接入运行时。
- 原审计发现的 `double_toll_body` 族内尺度、逐帧锚与来源字段问题已完成修正：现为 208px、固定源锚 `x417`，并补齐 `sourceVideoProvider/sourceProvenance`。

复检状态：`offline_asset_passed; runtime_integration_not_started`。

## 逐动作结果

| 动作 | 正式帧/墙钟 | 首帧有效高度 | 脚线 | 原视频最小安全边 | 关键帧 | 空帧/触边/透明脏RGB | 结果 |
|---|---:|---:|---:|---:|---|---|---|
| `stride` | 54 / 2208ms | 208px | y=239 | 57px | 原生键精确保留 | 0 / 0 / 0 | 通过，回绕循环 |
| `double_toll_body` | 73 / 3000ms | 208px | y=239 | 60px | 原生键精确保留 | 0 / 0 / 0 | 通过，固定源锚 `x417` |
| `antler_body` | 101 / 4167ms | 208px | y=239 | 59px | 原生键精确保留 | 0 / 0 / 0 | 通过 |
| `hoof_sequence_body` | 57 / 2333ms | 208px | y=239..240 | 59px | 原生键精确保留 | 0 / 0 / 0 | 通过 |
| `long_tone_body` | 109 / 4500ms | 208px | y=239 | 57px | 原生键精确保留 | 0 / 0 / 0 | 通过 |
| `rhythm_shift_body` | 109 / 4500ms | 208px | y=239 | 59px | 原生键精确保留 | 0 / 0 / 0 | 通过 |

所有图集最长边不超过 4096px，宽高整除各自单格，末行空格比例为 0%–5%，低于 12.5% 门槛。GIF 帧数与正式表一致；GIF 的 10ms 时间量化误差为 0–3ms。

## 朝向、根点与动作轴

- 六条原视频均保持轻俯视、侧面占主导的右向低三分之四轴；喙首、胸腔、骨盆、膝与蹄尖没有转成正面或左向。
- `antler_body`、`long_tone_body`、`hoof_sequence_body`、`rhythm_shift_body` 使用固定摄影棚注册源；支撑蹄与基准线稳定，没有镜头跟随。
- `stride` 使用半开窗 `[f40,f93)`；`f93` 为与 `f40` 同相位的排除端点，正式尾到首由一次回绕 RIFE 补齐，不重复端点。
- `double_toll_body` 原视频后侧承重蹄在有效窗内约 `-0.09..+0.12px`，原片本身不需要逐帧重居中。正式表现已共用源 `f12` 的固定下半身锚 `x417`，没有逐帧几何重居中。

## 时钟与事件

| 动作 | 0-based 事件帧 | 1-based 消费者 | 运行时合同 |
|---|---|---|---|
| `double_toll_body` | `f24/f50` | `25/51` | 无伤提示环 / 伤害环；第三回声外置 |
| `antler_body` | `f60` | `61` | 前方叉角接触，只结算一次 |
| `hoof_sequence_body` | `f16/f24/f32/f40` | `17/25/33/41` | 四个外部圆区；整次施法每目标最多两次 |
| `long_tone_body` | `f56` | `57` | 前方长矩形共振，只结算一次 |
| `rhythm_shift_body` | `f52` | `53` | 非伤害阶段生效，只触发一次 |
| `stride` | 无 | 无 | 原地循环，世界位移由运行时负责 |

事件均位于有效帧范围内，0-based/1-based 映射一致。未来运行时必须让动画、动作锁、事件跨阈值、打断/死亡取消共用同一时钟；本次没有用静态表冒充运行时消费验证。

## 已完成修正与复审

`double_toll_body` 沿用原视频和源 `f12,14,...,84`，只做同族 208px 等比规范化、固定源锚 `x417` 与一次非回绕 RIFE。正式结果仍为 73 帧 / 3000ms，`f24/f50` 事件不变；36 个中间帧脚线位移均为 0，没有暗色、红色、蓝色或青色可见异常，也没有 hold 回退。六动作家族仍为 `140.1875 MiB`，低于 256 MiB 硬停线。

本检查不包含游戏、构建、浏览器、运行时状态切换或战斗判定验证。
