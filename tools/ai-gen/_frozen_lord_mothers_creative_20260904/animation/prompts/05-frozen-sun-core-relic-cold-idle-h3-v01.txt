The exact frozen-sun core relic from the first frame performs a minimal cold-shell idle loop without changing orientation. Locked slightly elevated orthographic-like game camera. The entire creature remains in the exact same fixed screen orientation for all 124 frames: the broken stone ring stays on the same screen-left side, the small right-side spike stays on screen-right, and the visible molten opening never rotates around the body. Exactly four lower supports remain planted and keep the same front-to-back overlap. Only a slow two-beat contraction passes through the translucent ice shell while the dim core brightens slightly and fades; the supports absorb a few pixels of vertical weight without stepping. End in the exact first pose for a seamless loop. Preserve the ring, shell, core, supports, scale and topology. Fixed center and contact line, pure white background, full silhouette. No axial rotation, turn, orbit, walking, support swapping, camera motion, hot-phase exposure, attack effect or cropping.
