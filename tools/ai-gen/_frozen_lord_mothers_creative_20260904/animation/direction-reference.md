# 雪原五领主动画方向说明与阶段闸门

## 共同方向契约

- 身份来源是本任务的五张获准母图；运动参照只提供相机、步轴、脚相和根点纪律，不把新领主改造成旧怪同类。
- 画面为轻俯视、侧面占主导的低三分之四视角。具备身体前后的怪物面向画面右侧；鼻端/口器、胸腔、胯部、膝与脚尖共同指向右侧。
- 所有移动样片均为原地循环，运行时由 Collider 驱动位移。画面根点和共同脚线固定，摄像机不跟随、不摇移、不变焦。
- 母图通过不等于动作通过。每只先生成一条 5.17 秒 MiniMax H3 移动样片，原视频按 24 个均匀抽样帧检查朝向、肢体数量、拓扑、根点和首尾；样片未过时，其攻击/阶段状态继续冻结。
- 首条样片通过后才制作动作专用首帧。攻击使用 `recover`，死亡/坍缩使用 `one-way`；只有自然循环使用 `loop`。
- 候选视频只有先通过本节的方向/拓扑门，才允许抠图、插帧和正式精灵表；正式表通过也不自动授权运行时配置。

## 已查看的项目内运动参照

| 参照 | 实际检查帧 | 只借鉴的内容 |
| --- | --- | --- |
| `assets/enemies/glacierback_war_ox/running.png` | f0 / f10 / f20 / f29 | 右向重型兽低三分之四相机、承重脚相、水平根点 |
| `assets/enemies/reed_shadow_sickle_mantis/walking.png` | f0 / f10 / f20 / f30 / f39 | 多足分层、前后肢不粘连、右向步轴 |
| `assets/enemies/core_drill_worm/crawling.png` | f0 / f15 / f30 / f45 / f59 | 低矮长体爬行、口器朝向与连续足波 |
| `assets/enemies/hollow_ovum/walk.png` | f0 / f20 / f40 / f61 | 抽象无头主体的固定拓扑、全方向语义和禁止镜像 |

## 分怪物判定

### 雪冢驮城兽

- 原母图问题：腿组过于正面，塔楼过高，直接进视频容易把步轴读成朝镜头移动。
- v02 虽修正朝向，但离线画面只明确显示四足，已在送视频前拦下。
- 已二次调整：`reference-source/01-snow-sepulcher-carrier-action-ready-v03.png`，低侧右向并明确显示六只独立足（近侧三足、错开的远侧三足），塔楼仍与背部融合；只有 v03 可作 H3 首帧。
- 本轮可进：`advance` 原地重步样片。
- 暂停：踏碾、犁雪、塔垛坠临、六足定城。原因是这些动作还没有专用预备/接触/收势首帧，且塔楼拓扑要先经移动样片验证。

### 极光织命母

- 原母图问题：细肢交叠，H3 容易交换足与织线臂、增减肢体。
- 已调整：`reference-source/02-aurora-fate-weaver-action-ready-v02.png`，固定六条承重步足与两条抬起织线臂，开放圆环保持可读。
- 本轮可进：`seek_band` 原地缓行样片。
- 暂停：剪命肢、三角命织、倒织旧步、极光回梭、断环重织。必须先确认八肢职责和圆环拓扑跨 124 帧不漂移。

### 白寂鸣钟鹿

- 母图的右向身体轴、四足和钟体悬挂关系已经清楚，不另画身份变体。
- 本轮可进：`stride` 原地缓行样片；钟摆只允许小幅被动摆动。
- 暂停：叉角校音、迟鸣双响、四蹄节拍、失声长调、第三回声。钟系技能需要按事件节拍分别设计动作首帧，不能从移动样片直接推演。

### 永冻地渊喉

- 原母图问题：环口接近正面圆，前后轴不成立。
- 曾调整为环口朝右张开的侧向椭圆、六足沿低矮身体排布；该参考随后未能越过拓扑门禁，实物已在归档清理中删除，失败结论保留。
- 本轮可进：`crawl` 原地爬行样片。
- 暂停：咬合、吞线、钻地、破土、三鳍排寒、脱环。钻地链必须在地表身份稳定后另做进入/地下轨迹 VFX/破土三段，不把一次 H3 视频当成完整状态机。

### 冻日核骸

- 该主体没有头胸胯和固定前方，按 `orientationInvariant/allDirection` 管理，运行时 `flipX:false`。
- 本轮可进：冷壳期 `cold_move` 原地缓慢挪动样片；四个支撑点、冰壳、石环和核心位置固定。
- 暂停：冷壳碾轨、零度收束、解冻、熔核待机、黑日灼环、熔核喷潮、重新结壳。冷热相变需要一对能闭合拓扑的冷/热首帧，当前母图只有冷壳身份。

## 进入下一阶段的最低条件

1. 24 帧接触表中没有朝向翻转、镜头旋转、主体出框和明显根点漂移。
2. 驮城兽/织命母/地渊喉的腿数与职责不变；鸣钟鹿保持四足；核骸保持四个支撑点且不产生头脸。
3. 身份关键件不重构：塔楼、开放织环与极光膜、腹钟、侧向环口与三背鳍、冰壳/熔核/石环。
4. 循环末端能回到首姿；若整条不自然，只选其中天然闭合段，不强行把坏首尾做插值。
5. 通过后仍只解锁动作首帧设计，不自动获得运行时接入许可。

## 2026-09-04 MiniMax H3 移动样片结果

| 怪物 / 样片 | 结果 | 离线判定 | 下一步 |
| --- | --- | --- | --- |
| 雪冢驮城兽 / `advance` | 通过 | 24 个抽样帧持续右向；六足可读；塔楼与背甲不重构；无明显横向根点漂移 | 只解锁踏碾、犁雪、坠塔、定城的动作首帧设计 |
| 极光织命母 / `seek_band` | 通过 | 开放圆环、六步足、两织线臂和极光膜职责稳定 | 只解锁五个技能的动作首帧设计 |
| 白寂鸣钟鹿 / `stride` | 通过 | 四足与右向稳定；腹钟保持悬挂且未提前生成技能波纹 | 只解锁钟系动作首帧与事件节拍设计 |
| 永冻地渊喉 / `crawl` | 拒绝 | 环口大体保持右向，但只见近侧三足，无法验证三对足；背鳍轮廓疑似漂移 | 停在首帧阶段。后续八足返修稿与五可见足返修稿也拒绝，不继续视频重抽 |
| 冻日核骸 / `cold_move` | 拒绝 | 主体绕竖轴转身，石环从左换到右；四支撑在转身段不稳定 | 停在移动样片阶段；重写“固定朝向而非旋转”的首帧/提示后再试，热相仍缺独立首帧 |

通过仅代表原始 H3 视频的方向与拓扑离线初检。截至本节的移动样片裁决时，五条视频都没有进入抠图、RIFE、精灵表、运行时配置或游戏测试；后续正式素材进度见文末 2026-09-05 记录。

## 2026-09-04 动作专用首帧与 MiniMax H3 签名动作

### 开拍前视角、朝向与身份纠错

| 怪物 / 动作 | 动作首帧门槛 | 处理结果 |
| --- | --- | --- |
| 雪冢驮城兽 / `plow` | 沿既有右向低三分之四步轴压低头胸；六足全部可分；背部塔楼与背甲融合；右侧留冲击空间 | `action-keyframes/01-snow-sepulcher-carrier-plow-prepare-v01.png` 通过，送 H3 `recover` |
| 极光织命母 / `triangle_weave` | 六条步足保持承重，只有两条织线臂展开；开放圆环和极光膜不闭合；外部三角由运行时生成 | 首帧当时通过并送 H3 `recover`；对应 v01 动画后因外部技能几何侵入判废，媒体已清理 |
| 白寂鸣钟鹿 / `double_toll` | 四足与右向轴固定；腹钟后摆预备；垂饰必须恰好三枚 | 初稿五枚、一次纠错稿两枚均拦截；`action-keyframes/03-white-silence-bell-hart-double-toll-prepare-v03.png` 恰好三枚后送 H3 `recover` |
| 永冻地渊喉 | 上游六足/三对足拓扑仍不可验证 | 本轮不生成动作首帧与视频，所有状态继续冻结 |
| 冻日核骸 / `cold_idle` | 复用固定石环侧别的冷态参考；四支撑不迈步；禁止轴向旋转和热相 | 仅送低风险 H3 `loop`，不代表 `cold_move` 或任何热态解锁 |

### 签名动作离线裁决

| 怪物 / 动作 | 结果 | 24 帧与循环/恢复判定 | 精确状态闸门 |
| --- | --- | --- | --- |
| 雪冢驮城兽 / `plow` | 拒绝 | 城塔和大体六足身份保住，但中段胸胯轴朝镜头偏航，末端未恢复目标运动朝向 | `plow` 继续冻结；基础 `advance` 样片的通过状态不受影响 |
| 极光织命母 / `triangle_weave` | 拒绝 | 本体和开放圆环稳定，但模型生成大型外部三角框与星芒，侵入运行时 VFX 职责 | `triangle_weave` 继续冻结；基础 `seek_band` 样片仍有效 |
| 白寂鸣钟鹿 / `double_toll_body` | 通过 | 四足、鹿角、腹钟和恰好三枚垂饰稳定；只有一次主要实体摆钟并回收，无外部回响特效 | 只解锁本体视频的抠图与帧事件规划；延迟伤害、第二次回响和运行时接入仍未授权/未制作 |
| 冻日核骸 / `cold_idle` | 拒绝 | 石环侧别、四支撑和朝向稳定，但中段核心转为强橙红并染红外壳，擅自跨入热相 | `cold_idle`、`cold_move` 和全部热态继续冻结 |

本轮生成 4 条、通过 1 条、拒绝 3 条；地渊喉因上游门槛未过而未生成。通过项继续保留源视频、参数、联系表和 GIF；三组拒绝媒体已在 2026-09-05 归档清理中删除，但本节的裁决理由继续作为门禁记录。截至本节裁决时尚未进行抠图、RIFE、正式精灵表、运行时状态机接入或游戏测试；鸣钟鹿后续已进入正式素材阶段。

## 2026-09-04 签名动作 v02 定向修正

### 开拍前修正

- 驮城兽：尝试重画更严格侧身首帧，但两次输出分别为七足可见和仅四足可见，均在上传前拦截且不写入项目；v02 回退到已通过的六足首帧。动作由完整冲撞缩为纯原地 `plow_prepare`，由运行时继续承担 Collider 推进。
- 织命母：保留原八肢动作首帧，提示词删除“三角、角点、描线”等构图动作，只保留双臂并行收束/释放；极光膜要求始终包含于圆环内部。
- 核骸：曾用内置 imagegen 将橙色熔核精确改为深蓝黑冻结矿物和微弱青色裂隙，石环左置、右侧尖突与四支撑全部锁定；该 v02 首帧随后未能阻止尖突变钩，相关拒绝媒体已清理。

### v02 离线裁决

| 怪物 / 动作 | 结果 | 修正效果与剩余问题 | 精确状态闸门 |
| --- | --- | --- | --- |
| 雪冢驮城兽 / `plow_prepare` | 通过 | 六足、塔楼、右向长轴和根点稳定；只完成一次轻微下沉蓄力并自然回收 | 解锁本体蓄力素材的抠图/帧事件规划；`plow_charge_and_impact`、Collider 推进和伤害仍未完成 |
| 极光织命母 / `triangle_weave_body` | 拒绝 | 外部三角与星芒已消失，六步足/双臂职责基本稳定；但极光膜中段完全消失后重建 | `triangle_weave` 继续冻结；下次需把膜作为静态身份层，不让其参与收缩 |
| 冻日核骸 / `cold_idle` | 拒绝 | 冷蓝暗核、石环侧别和四支撑稳定；但右侧小尖突扩大成大弯钩/颚状运动结构 | `cold_idle` 继续冻结；下次应锁死右侧构件，只允许核心亮度微变 |

v02 共生成 3 条、通过 1 条、拒绝 2 条。地渊喉保持上游冻结，鸣钟鹿沿用上一批通过的 `double_toll_body`。截至本节裁决时视频仍停在候选素材层；驮城兽与用户复判通过的织命母后续已进入正式素材阶段。

## 2026-09-05 织命母用户复判与正式精灵表

- 用户接受 `02-aurora-fate-weaver-body-cast-h3-v02.mp4`，把中段极光膜消失定义为主动“抽膜—释放—重织”，覆盖 2026-09-04 的自动拒绝结论。
- 源动作取 `f0..f80`，排除 `f81..f123` 的长收势停留；以 `f0,2,...,80` 保留 41 张透明原生键帧，固定右向低三分之四视角、六条承重步足、两条织线臂、开放圆环与锁定根点。
- 正式表仅做一次非回绕 RIFE 2×，得到 81 帧；原生键保留在偶数索引。墙钟 3333ms，0-based `releaseFrame=48`，未来 1-based 消费者对应第 49 帧。
- 相位：`gather 0..30 -> extract 31..40 -> released 41..54 -> reweave 55..64 -> recover 65..80`。外部三角织点、极光连线和伤害判定不烘进本体表。
- 资产包：`formal/aurora-fate-weaver/spritesheet-manifest.json`。当前仅为正式素材，运行时状态机/VFX/伤害接入仍冻结；`cut`、`oldstep`、`tether` 与半血 `reweave` 也没有因此自动解锁。

## 2026-09-05 驮城兽与鸣钟鹿正式本体表

### 雪冢驮城兽 `plow_prepare`

- 方向源沿用已通过的右向低三分之四动作片；源动作窗取 `f8..f100`，排除片头 `f0..f7` 和长收势尾 `f101..f123`。
- 隔帧保留 47 张 BiRefNet 透明原生键；六足、融合塔楼和右向长轴通过浅色主体探针。统一高度 224，使用下半身锚点锁定视觉根，不烘焙 Collider 位移。
- 一次性非回绕 RIFE 2× 得到 93 帧，单格 `480x256`，8 列 x 12 行，图集 `3840x3072`；原生键保留在偶数索引。
- 墙钟 3833ms，0-based 相位为 `settle_into_brace 0..20 -> brace 21..60 -> fully_braced_hold 61..67 -> recover 68..88 -> settled 89..92`，`fullyBracedFrame=60`。
- 这张表只收口压低蓄力和自然回收。`plow_move` 所需的 Collider 推进、冲锋本体/轨迹、撞墙收势与伤害仍未制作；正式包为 `formal/snow-sepulcher-carrier/spritesheet-manifest.json`。

### 白寂鸣钟鹿 `double_toll_body`

- 源动作窗取 `f12..f84`，排除片头 `f0..f11` 和静止尾 `f85..f123`；隔帧保留 37 张透明原生键。鹿角、四足、腹钟与恰好三枚垂饰通过探针。
- 一次性非回绕 RIFE 2× 得到 73 帧，单格 `192x256`，15 列 x 5 行，图集 `2880x1280`；根点锁定，原生键保留在偶数索引。
- 墙钟 3000ms，0-based 相位为 `backswing 0..12 -> warning_toll 13..24 -> return_swing 25..49 -> damage_toll 50..56 -> recover 57..72`。`warningRingFrame=24`，`damageRingFrame=50`，相隔约 1083ms。
- 本体表不含冰纹。无伤提示环、延迟伤害环、伤害判定、0.75 秒第三回声和运行时状态机仍未制作；正式包为 `formal/white-silence-bell-hart/spritesheet-manifest.json`。

两张正式表均为素材合同，不改变地渊喉、冻日核骸及其他未制动作的冻结状态，也未执行游戏内验证。

## 2026-09-05 豆包 `oldstep_body` 开工方向门

- 身份/相机源：`reference-source/02-aurora-fate-weaver-action-ready-v02.png`；继续沿用右向低三分之四视角、六条承重步足、两条织线臂、开放圆环与完整极光膜。
- 新动作首帧：`action-keyframes/02-aurora-fate-weaver-oldstep-prepare-v01.png`；只把两条织线臂收回到圆环前缘，身体、六足脚位、圆环和膜不改。生成器为内置 imagegen，提示记录在 `prompts/02-aurora-fate-weaver-oldstep-keyframe-imagegen-v01.txt`。
- 豆包参考：`action-references/02-aurora-fate-weaver-oldstep-prepare-v01-1024x576.png`；必须等比放入 16:9 白底，不横拉。目标动作是三拍本体回抽，根节点原地锁定，历史落点、抽打区域和伤害均不进入视频。
- 提交顺序：先 `--fill-only` 回读提示词，字符/哈希一致后只提交 1 个 5 秒候选。原视频必须先通过 24～32 点方向、八肢职责、圆环/膜和根点检查；未过则不抠图、不 RIFE、不进入正式表。
- 当前只解锁 `oldstep_body` 的豆包候选生成；`oldstep` 运行时事件、伤害与状态机仍冻结，地渊喉和核骸的上游冻结不变。

### 豆包原片与正式表裁决

- `--fill-only` 回读 664 个编辑器字符，SHA-256 为 `04244850ab60269fe73ef218e2695ca055a4c337e62e3c6586fe718ba434078b`；只提交 1 个 `Seedance 2.0 Mini` 候选，没有自动重抽。
- 原片 `videos/02-aurora-fate-weaver-oldstep-body-doubao-v01.mp4` 为 1280x720、24fps、121 帧、约 5.056 秒。32 点接触表及织臂近看表确认：镜头与视觉根固定，右向体轴不翻转；六条承重步足、两条织线臂、开放圆环和环内极光膜职责稳定。
- 三拍动作读点为源 `f24` 下臂、`f50` 上臂、`f66` 双臂；`f85..f120` 仅为长停留，排除。选 `f0,2,...,84` 共 43 张原生键，BiRefNet 探针没有吃掉浅色腿尖、织臂、背环或极光膜。
- 一次性非回绕 RIFE 2× 得到 85 帧，所有原生键保留在偶数索引，脚底统一到 y=239；无空帧、触边帧或透明区脏 RGB。动作墙钟 3500ms，正式事件仍为 0-based `f24/f50/f66`。
- 本体资产通过，正式包为 `formal/aurora-fate-weaver/oldstep-body/spritesheet-manifest.json`。历史位置快照、外部窄矩形攻击区、预警、伤害和状态机没有进入素材，继续阻断运行时转移。

## 2026-09-05 豆包 `tether_body` 开工方向门

- 身份与相机继续取 `reference-source/02-aurora-fate-weaver-action-ready-v02.png`；动作对照使用 `triangle_weave_body` 正式表 `f0/f48/f64` 与 `oldstep_body` 原片源 `f24/f50/f66`。三组素材都维持右向低三分之四体轴、固定根点、六条承重步足与两条较短织线臂的职责分离。
- 新动作不沿用前两招的向前点刺，而采用“上下张臂捕捉—身体前方交叉—向背环收卷—松开回位”的本体轮廓。命线、目标、1 秒延迟和敌人位移全部由未来运行时事件承担，不进入参考图或视频。
- imagegen 首稿的张臂轮廓可读，但只剩四个清楚落脚点，因此在上传前拒绝且不写入项目。修复稿 `action-keyframes/02-aurora-fate-weaver-tether-prepare-v02.png` 恢复身份源的六足职责和原脚位；远侧足保留原构图自然遮挡，五个落脚点清楚可追踪，两条织线臂保持上/下分离的宽 V 形。
- 豆包参考必须由脚本等比放入 1024x576 白底，禁止横拉；目标视频固定镜头、固定根点、五秒一次性 `recover`。提交前仍须执行 `--fill-only` 字符/哈希回读，只允许 1 个候选且不自动重抽。
- 原片必须先过 24～32 点方向、八肢职责、圆环/膜、捕捉拍与回拉拍检查；失败则停在候选层，不抠图、不 RIFE。即使本体动作通过，外部命线、目标筛选、LOS 复查、140 位移和运行时状态机仍冻结。

### 豆包原片与正式表裁决

- `--fill-only` 回读 736 个编辑器字符，SHA-256 为 `562ec7e64da27e6908936182d4c4f73494ffffc4c6c851db7f6172aa6a360f4c`；只提交 1 个 `Seedance 2.0 Mini` 候选，没有自动重抽。
- 原片 `videos/02-aurora-fate-weaver-tether-body-doubao-v01.mp4` 为 1280x720、24fps、121 帧、约 5.056 秒。32 点联系图与 `..._arms-detail.png` 确认镜头、根点和右向体轴稳定；f54～f78 两臂在头甲/背环前发生合理自遮挡，但两肩根、前臂和爪端仍可追踪，没有变成长腿或新增肢体。
- `f32` 是张臂内扣、建立外部命线的清楚事件点；`f58` 是双臂合扣后向背环完成回卷的拉拽点，相隔 26 个源帧即约 1083ms。源 `f105..f120` 仅为恢复后的停留，因此排除。
- 选 `f0,2,...,104` 共 53 张原生键，BiRefNet 探针保住浅色腿尖、双臂、开放背环和极光膜。一次性非回绕 RIFE 2× 得到 105 帧，原生键保留在偶数索引；无空帧、触边帧、透明区脏 RGB 或中间帧脚线位移，Alpha 底边仅在 y=239..240 之间波动。
- 本体动作通过，正式包为 `formal/aurora-fate-weaver/tether-body/spritesheet-manifest.json`。命线目标筛选、可见线、LOS 复查、140 单位拉拽和运行时状态机没有进入素材，继续阻断运行时转移。

## 2026-09-05 豆包 `cut_body` 开工方向门

- 身份与相机继续取 `reference-source/02-aurora-fate-weaver-action-ready-v02.png`。真实动作对照重新查看了 `tether_body` 正式表 0-based `f0/f32/f58/f104` 和 `oldstep_body` 正式表 `f24/f50/f66`：相机均为轻俯视、侧面占主导的低三分之四视角，头胸与开放背环长轴朝画面右侧；承重足共用同一脚线，动作根点固定。
- 本动作直接复用已通过方向门的 `action-keyframes/02-aurora-fate-weaver-tether-prepare-v02.png` 与其 16:9 等比参考 `action-references/02-aurora-fate-weaver-tether-prepare-v02-1024x576.png`。宽 V 上下双臂正好是剪刀张开的起势；不重画新母帧，避免重新引入腿数、织线臂职责或圆环拓扑风险。
- 解剖/动作轴：身份设定为六条长承重步足加两条较短织线臂；受远侧自然遮挡影响，参考帧有五个落脚点清楚可追踪，但六条步足的肩根与职责已沿用通过稿。`cut_body` 只允许两条短臂沿相反弧线向身体前方同一空点做一次剪合，六足、身体根点和背环中心不移动，胸甲只允许极小幅前压。
- 目标时间轴为 `0.0..0.35s` 宽 V 保持、`0.35..0.90s` 单次剪合、`0.90..1.15s` 接触停留、`1.15..1.85s` 反弹张开、`1.85..2.50s` 回到起姿、`2.50..5.0s` 克制待机；禁止第二次剪合。斩击弧、正面扇形、目标、伤害、击退和状态机全部属于未来运行时，不进入本体视频。
- 豆包提交仍执行 `--fill-only` 字符/哈希回读，只允许 1 个 `Seedance 2.0 Mini` 候选，不自动重抽。原片先过 24～32 点联系图与动态 GIF，逐段检查右向体轴、八肢职责、开放环/极光膜、根点和唯一接触拍；任一失败即停在候选层，不抠图、不 RIFE。
- 当前只解锁 `cut_body` 的离线候选制作。规划中的正面 180 度扇形、物理伤害、击退、接敌距离和状态机仍未授权接入；地渊喉与冻日核骸的冻结状态不变。

### 豆包原片与正式表裁决

- `--fill-only` 回读 671 个编辑器字符，SHA-256 为 `0a4171d409b5e84f66fc9028eb7558b07863bff5c2aac2fcbfe653e11384ed79`；随后只提交 1 个 `Seedance 2.0 Mini` 候选，没有自动重抽。
- 原片 `videos/02-aurora-fate-weaver-cut-body-doubao-v01.mp4` 为 1280x720、24fps、121 帧、约 5.056 秒。32 点联系图及闭合近看表确认镜头、根点和右向体轴稳定；两条织线臂在源 `f20` 对齐成上下两根独立剪刃，肩根、前臂和爪端仍可分别追踪，之后只是锁闭停留，没有第二次攻击。
- 取源 `f0,2,...,84` 共 43 张原生键，排除 `f85..f120` 的恢复后停留。BiRefNet 探针保住浅色腿尖、两条剪臂、开放背环和极光膜；一次性非回绕 RIFE 2× 得到 85 帧，原生键保留在偶数索引。
- 正式单格 `352x256`，8 列 x 11 行，图集 `2816x2816`，墙钟 3500ms；0-based `f20` 是唯一接触事件。无空帧、触边帧或透明区脏 RGB，Alpha 底边全程为 `y=239`。
- 本体动作通过，正式包为 `formal/aurora-fate-weaver/cut-body/spritesheet-manifest.json`。未来接敌距离与接触扇区必须分离；正面 180 度扇形、物理伤害、35 击退和运行时状态机仍冻结。

## 2026-09-05 豆包 `reweave_body` 开工方向门

- 身份/方向源继续使用 `reference-source/02-aurora-fate-weaver-action-ready-v02.png`，豆包首帧复用 `action-references/02-aurora-fate-weaver-body-cast-v02-1024x576.png`。该帧已通过右向低三分之四、六条承重步足、两条短织线臂、开放背环和环内极光膜门槛，因此不为阶段动作重新生成身份母图。
- 真实动作对照重新查看 `triangle_weave_body` 原片 0-based `f0/f24/f40/f48/f56/f64/f72/f80`：相机、右向体轴、脚线和根点稳定；`f40..f56` 展示膜由稀疏到恢复，证明环内织带可以作为本体层变化，但本轮不得复刻三角释放手势或让膜完全消失。
- 新动作轴为原地竖直压低与回升：六足只屈曲不换脚，两条短臂回收到圆环前缘并隔空调整环内织带；外环轮廓、头胸朝向和圆环中心锁定。目标 1.20..1.50 秒完成唯一一次重新绷紧拍，之后恢复参考姿态，不生成环外命线、三角、光束或伤害表现。
- 预算门：当前四张正式本体表为 124.9688 MiB。用户在已获知下一动作会超过 Boss 128 MiB 目标后明确要求继续并完成，因此本轮登记为用户接受的目标线例外；256 MiB 仍是硬停线，正式表构建后必须重新按实际 PNG 头计算完整家族预算。
- 仍先执行豆包 `--fill-only` 字符/哈希回读，只允许 1 个 `Seedance 2.0 Mini` 候选，不自动重抽。原片须先通过 32 点联系图、动态 GIF 和织带/双臂近看；任何方向、肢体职责、外环或冷热身份失败都停在候选层，不进入 BiRefNet/RIFE。
- 当前只制作 `reweave_body` 本体阶段动作。半血一次性触发、1.4 秒动作锁、技能节奏缩短、旧步第四历史点、外部 VFX 与运行时状态机仍未接入。

### 豆包原片裁决

- `--fill-only` 回读 824 个编辑器字符，SHA-256 为 `edd5559cd146f440cce2acb6fb54e6119d9a850fd79d480dd41773d5f7001d6f`；只提交 1 个 `Seedance 2.0 Mini` 候选，没有自动重抽。
- 原片 `videos/02-aurora-fate-weaver-reweave-body-doubao-v01.mp4` 为 1280x720、24fps、121 帧、约 5.056 秒。外环、视觉根和承重足大体稳定，但动作过程中身体/镜头逐渐漂向近正面，一条短织线臂穿入环心，两臂职责不再干净分离。
- 源 `f58` 出现高亮白紫爆点，`f62..f77` 进一步变成填满圆环的竖向紫色传送门盘，违反“只调整环内交叉极光膜、不生成爆发或环外技能表现”的本体合同，也破坏已认可的环内织膜身份。
- 候选在原片方向/拓扑/VFX 门禁拒绝并冻结；未执行 BiRefNet、RIFE 或正式精灵表构建，也没有运行时接入。织命母四张已通过正式本体表的家族预算保持 `124.9688 MiB`，没有因本候选增加。

### `reweave_body` 连续重抽授权与 v02 门（2026-09-05）

- 用户明确要求继续重抽直到合格，取代本动作此前“单候选后冻结”的重试边界；仍按每轮一个候选、每轮先过原片门禁推进。额度、账号权限或安全提示仍是硬停止条件，不能确认付费或用后处理伪装修复原片。
- v02 继续使用已核对的 `action-references/02-aurora-fate-weaver-body-cast-v02-1024x576.png`。真实动作对照仍为 `triangle_weave_body` 原片 `f0/f24/f40/f48/f56/f64/f72/f80`，相机和身体轴目标不变：轻俯视低三分之四、侧面占主导、头胸胯/膝脚朝右、脚线与环心原地锁定。
- 针对 v01 的失败，v02 不再要求织膜抽散或重新交叉。环内青紫交叉纹改写为固定在透明薄膜上的平面纹理，保持形状、密度和亮度；动作语义缩成六足原地小幅压低、两条短臂沿环缘相向校准、同时外拉一次、回位。
- v02 合格条件：没有近正面漂移；六条承重足与两条短臂职责可追踪；两臂不进入环心；开放骨环透视不变；环内交叉纹不变成亮点、圆盘或门户；动作只发生一次并自然恢复。

### v02 原片与正式表裁决

- `--fill-only` 回读 728 个编辑器字符，SHA-256 为 `c19fbe5c7498c070d008db7a32185a9671b38608fa5843959120ccb63ba48218`；用户授权连续重抽后只新增并提交这一条 v02，没有并发候选或未记录提交。
- 原片 `videos/02-aurora-fate-weaver-reweave-body-doubao-v02.mp4` 为 1280x720、24fps、121 帧、约 5.056 秒。32 点联系图、动态 GIF 和 11 帧环体近看确认：相机/根点/右向轴固定，六足与两条短臂职责可追踪，开放硬环与环内青紫交叉纹不变形，没有爆点、圆盘、门户或环外技能表现。
- BiRefNet 探针检查源 `f0/f24/f48/f60/f72/f84/f96/f108/f120`，保住浅色腿尖、短臂、骨环与半透明织膜。正式动作保留源 `f0,2,...,120` 共 61 张原生键，一次性非回绕 RIFE 2× 得到 121 帧；原生键保留在偶数索引。
- 正式单格 `288x256`，11 列 x 11 行，图集 `3168x2816`，墙钟 5000ms；0-based `f84` 是唯一重织完成提示拍。无空帧、触边帧、透明区脏 RGB 或中间帧脚线位移，Alpha 底边全程 `y=239`。
- 本体正式包为 `formal/aurora-fate-weaver/reweave-body/spritesheet-manifest.json`。半血一次性触发、三角节奏变化、旧步第四历史点、外部 VFX 与运行时状态机仍未接入。五张织命母正式本体表合计 `159.0000 MiB`，超过 128 MiB 目标但低于 256 MiB 硬停线，按已记录的用户目标线例外保留。

## 2026-09-05 豆包 `seek_band` 移动循环开工方向门

- 身份、相机与首帧统一使用 `animation/references/02-aurora-fate-weaver-locomotion-1024x576.png`。该图是当前移动专用参考：轻俯视、侧面占主导的低三分之四角度，头胸胯与膝脚朝画面右侧；开放圆环、环内青紫交叉膜和低矮身体轮廓均完整可读。
- 真实移动对照重新查看 `animation/videos/02-aurora-fate-weaver-seek-band-h3-v01_contact.png` 的源 `f0/f21/f43/f64/f86/f107/f123`。该候选的相机、右向身体轴和脚线稳定，六条长步足承担移动，两条短织线臂保持抬起；本轮只借用它作为实际步态方向与肢体职责参考，不把 H3 候选直接视为已完成的正式循环。
- 豆包目标是固定根点的原地潜行循环：六条承重足按前中后三足一组交替迈步，迈步轴沿画面左右；两条短织线臂只做小幅反向平衡摆动且绝不触地。头、胸、胯、膝、脚尖与背环中心持续朝右，不允许转向近正面。
- 开放硬环与环内青紫交叉膜属于静态身份层，禁止呼吸、旋转、扩张或变成光效。视频不含攻击、施法、命线、粒子、残影和地面表现；移动速度、路径、碰撞和 AI 状态切换全部留给未来运行时。
- 提交前执行豆包 `--fill-only` 回读，只允许 1 个 `Seedance 2.0 Mini` 候选。原片先检查 32 点联系图与动态 GIF，再寻找实际相位匹配的半开区间 `[S,E)`；若不存在方向稳定、腿组职责稳定且 `E -> S` 接缝干净的窗口，就停在候选层，不抠图、不 RIFE，也不通过丢帧或后处理伪造循环。
- 五张现有正式本体表合计 `159.0000 MiB`。用户在收到该数字后继续推进本动作，因此沿用已记录的 128 MiB 目标线例外；256 MiB 家族硬停线仍有效，正式构建前后都必须按实际图集 RGBA 预算复核。
- 当前只解锁 `seek_band` 的离线移动素材。`seek_band` 运行时状态、寻路、位移、速度、碰撞与其他攻击状态仍冻结；地渊喉和冻日核骸的上游冻结状态不变。

### 豆包原片与正式循环裁决

- `--fill-only` 回读 901 个编辑器字符，SHA-256 为 `ed798f70a5013d0ad279a621398fde0b9e41a4822bf488c2c920664c6c5ce65d`；随后只提交 1 个 `Seedance 2.0 Mini`、16:9、5 秒循环候选，没有自动重抽。
- 原片 `animation/videos/02-aurora-fate-weaver-seek-band-doubao-v01.mp4` 为 1280x720、24fps、121 帧、约 5.056 秒。32 点联系图、动态 GIF 和腿部近看表确认相机、视觉根和右向体轴稳定；六条长承重足交替迈步，两条短织线臂始终抬起，开放环与固定交叉膜保持身份，没有攻击或外部 VFX。
- 相位扫描后选择半开区间 `[f6,f114)`；密集首尾图确认 `f114` 与 `f6` 的前中后足落脚关系、双短臂、环体高度最接近，故 `f114` 只作为重复端点验证。保留 `f6,8,...,112` 共 54 张原生键，BiRefNet 探针保住浅色腿尖、短臂、开放环和半透明膜。
- 一次回绕 RIFE 2x 得到 108 帧，原生键保留在偶数索引，最后一张插值帧补齐 `f112 -> f6` 的半步。正式单格 `416x256`，9 列 x 12 行，图集 `3744x3072`，墙钟 4500ms、24fps、`repeat=-1`；无空帧、触边帧、透明区脏 RGB，Alpha 底边仅在 `y=239..240`。
- 正式包为 `animation/formal/aurora-fate-weaver/seek-band/spritesheet-manifest.json`。六张织命母正式表合计 `202.8750 MiB`，仍低于 256 MiB 硬停线；寻路、世界位移、速度、碰撞和运行时状态机没有进入素材，继续阻断运行时转移。

## 2026-09-05 豆包：雪冢驮城兽 `advance` 移动循环开工方向门

- 身份、相机与首帧使用 `animation/references/01-snow-sepulcher-carrier-locomotion-1024x576.png`。右侧长楔形构件是前端头甲，左侧短雪檐是后端；头甲、胸腹、膝脚和移动轴持续朝画面右侧，维持轻俯视、侧面占主导的低三分之四角度。
- 真实移动对照重新查看 H3 `animation/videos/01-snow-sepulcher-carrier-advance-h3-v01_contact.png` 的源 `f0/f27/f53/f80/f107/f123`；塔楼与背甲不重构，六条粗壮承重足保持三对职责，脚线和视觉根稳定。另查看正式 `plow_prepare` 源 `f8/f32/f60/f84/f100` 只确认塔楼、积雪山体与脚线身份，不把原地下压当作移动参考。
- 豆包目标是固定根点的原地重步循环：六足按前/中/后三对交替承重，迈步轴沿画面左右；塔楼、城垛、小石堆、积雪山体和背甲作为刚性融合结构，只随身体产生克制惯性，不掉落或重建。
- 提交前执行 `--fill-only` 回读，只允许 1 个 `Seedance 2.0 Mini`、5 秒循环候选。原片先过 32 点联系图、动态 GIF、六足/塔楼近看，再寻找实际同相位半开区间 `[S,E)`；不存在干净接缝就停在候选层，不用后处理伪造。
- 当前正式家族只有 `plow_prepare`，解码 RGBA 为 `45.0000 MiB`。新移动表必须与它合并核算，128 MiB 是目标、256 MiB 是硬停线。
- 本轮只解锁 `advance` 离线移动素材。寻路、世界位移、速度、碰撞和运行时状态机仍冻结；`plow_move` 继续等待 Collider 推进、冲锋本体、撞墙收势、轨迹与伤害合同，不因移动循环通过而自动解锁。

### 豆包原片与正式循环裁决

- `--fill-only` 回读 875 个编辑器字符，SHA-256 为 `d3594a7dfed88acac0ad81085c36134bc06232739153ea2b8c739eb0d21d1559`；随后只提交 1 个 `Seedance 2.0 Mini`、16:9、5 秒循环候选，没有自动重抽。
- 原片 `animation/videos/01-snow-sepulcher-carrier-advance-doubao-v01.mp4` 为 1280x720、24fps、121 帧、约 5.056 秒。32 点联系图、动态 GIF 与六足/塔楼近看确认：镜头、视觉根和右向体轴稳定；六条承重足维持三对职责，塔楼、城垛、石堆、积雪山体与背甲保持刚性融合，没有掉落、重构、冲锋或外部 VFX。
- 全帧相似度曾给出 `f20 -> f88`，但人工检查发现足相位不一致而拒绝；`f0 -> f120` 也因腿组相位不同被排除。下半身轮廓扫描与逐对核对后选择半开区间 `[f25,f113)`，`f113` 仅作为同相位重复端点证明。保留 `f25,f27,...,f111` 共 44 张原生键。
- BiRefNet 探针保住浅色足端、塔楼、雪檐和头甲；目标本体高度由 224 微调为 222，避免塔尖触及安全边界而不裁切身份结构。一次回绕 RIFE 2x 得到 88 帧，原生键保留在偶数索引，最后一张插值帧补齐 `f111 -> f25` 的半步。
- 正式单格 `448x256`，8 列 x 11 行，图集 `3584x2816`，墙钟 3667ms、约 24fps、`repeat=-1`；无空帧、触边帧或透明区脏 RGB，Alpha 底边仅在 `y=239..240`。正式包为 `animation/formal/snow-sepulcher-carrier/advance/spritesheet-manifest.json`。
- 驮城兽两张正式表合计解码 RGBA `83.5000 MiB`，低于 Boss `128 MiB` 目标。导航、世界位移、速度、碰撞和运行时状态机仍未接入；`plow_move` 的依赖缺口保持阻断。

## 2026-09-05 豆包：雪冢驮城兽 `trample_body` 开工方向门

- 身份源、相机源与同角色实际动作对照重新查看：`animation/references/01-snow-sepulcher-carrier-locomotion-1024x576.png`、正式 `advance` 源键 `f25/f41/f57/f73/f89/f105/f111`、正式 `plow_prepare` 源键 `f8/f32/f60/f84/f100`。三组都锁定轻俯视、侧面占主导的低三分之四相机；右侧长楔头甲为前端，头胸长轴与膝脚朝右，视觉根和共同脚线固定。
- 行走帧和整身压低的 `plow_prepare` 都不适合作为单足踏碾首帧，因此先用内置 imagegen 从已认可移动参考编辑出 `animation/action-keyframes/01-snow-sepulcher-carrier-trample-prepare-v01.png`。关键帧保留恰好六足：靠右头甲的近侧前足单独抬起，完整肩根、膝、小腿和石足可读；其余五足落地承重，融合塔楼/城垛/石堆/雪山拓扑保持刚性。
- imagegen 原图按项目 `video-safe-reference.py` 只做等比缩放和白底补边，得到 `animation/action-references/01-snow-sepulcher-carrier-trample-prepare-v01-1024x576.png`；主体框为 `(190,92)..(813,507)`，左右/上下均保留安全边，不做非等比拉伸。
- 豆包目标为一次性 `recover`：抬起的近侧前足在约 0.80 秒形成唯一落地接触，身体短暂压低后在 2.30 秒前恢复六足站稳；不再次抬足。踏地矩形、物理伤害、击退、震波、地裂和状态机全部留给未来运行时事件。
- 提交前先执行 `--fill-only` 回读，只允许 1 个 `Seedance 2.0 Mini` 候选，不自动重抽。原片必须先通过 32 点联系图、动态 GIF 和前足/六足/塔楼近看；方向、足数、唯一接触拍或恢复任一失败就停在候选层，不进入 BiRefNet、RIFE 或正式表。
- 当前家族预算 `83.5000 MiB`。按本动作正式图集实际尺寸重新核算；128 MiB 是目标、256 MiB 是硬停线。`plow_move` 仍因 Collider 推进、冲锋本体、撞墙收势、轨迹与伤害合同缺失而冻结。

### 豆包原片与正式表裁决

- `--fill-only` 回读 714 个编辑器字符，SHA-256 为 `8e398ac073b4ba07e36447eefa6b31e4946a7a8bc1056f649ce9defa704589b8`；随后只提交 1 个 `Seedance 2.0 Mini`、16:9、5 秒候选，没有自动重抽。
- 原片 `animation/videos/01-snow-sepulcher-carrier-trample-body-doubao-v01.mp4` 为 1280x720、24fps、121 帧、约 5.056 秒。32 点联系图、动态 GIF、六足/塔楼近看和 `f0..f32` 踏地密集表确认：相机、视觉根和右向头胸轴稳定；六足职责可追踪，只有近侧前足完成一次大幅动作，塔楼/城垛/石堆/雪山保持融合，没有第二次攻击或外部 VFX。
- 近侧前足在源 `f26` 首次接触共同脚线，`f28` 已稳定承重；取源 `f0,2,...,f84` 共 43 张原生键，排除 `f85..f120` 的收势后长停留。BiRefNet 探针保住抬起足、足端、塔尖、雪檐和头甲，并清除灰底与接触影。
- 一次性非回绕 RIFE 2x 得到 85 帧，原生键保留在偶数索引；正式单格 `480x256`，8 列 x 11 行，图集 `3840x2816`，墙钟 3500ms、约 24.286fps、`repeat=0`。无空帧、触边帧、透明区脏 RGB 或中间帧脚线位移，Alpha 底边全程 `y=239`。
- 正式包为 `animation/formal/snow-sepulcher-carrier/trample-body/spritesheet-manifest.json`。三张驮城兽正式表合计解码 RGBA `124.7500 MiB`，仍低于 128 MiB 目标；前方矩形、伤害、击退、震波、接敌距离和运行时状态机未接入，继续阻断运行时转移。

## 2026-09-05 豆包：雪冢驮城兽 `tower_drop_body` 开工方向门

- 身份与相机重新核对 `animation/reference-source/01-snow-sepulcher-carrier-action-ready-v03.png`，同角色实际动作对照为 `advance` 原片源 `f25/f41/f57/f73/f89/f105/f111`、`trample_body` 原片源 `f0/f26/f28/f48/f72/f84` 和 `plow_prepare` 源 `f8/f32/f60/f84/f100`。三组素材都维持轻俯视、侧面占主导的低三分之四相机；右侧长楔头甲是前端，头胸长轴、膝脚与动作轴朝右，视觉根和共同脚线稳定。
- 内置 imagegen 从通过身份门的 `action-ready-v03` 精确编辑出 `animation/action-keyframes/01-snow-sepulcher-carrier-tower-drop-prepare-v01.png`，生成提示记录为 `prompts/01-snow-sepulcher-carrier-tower-drop-keyframe-imagegen-v01.txt`，SHA-256 为 `838e0e9cfef54fa57941c86f028495ff00142b5205965d57270a630587b79e69`。动作母帧保持恰好六条可追踪承重足：前后足略外撑，中足接近竖直，身体小幅压低；塔楼、城垛、石堆、雪山与背甲仍为竖直刚性融合结构。
- 母帧经 `video-safe-reference.py` 只做等比缩放和补边，得到 `animation/action-references/01-snow-sepulcher-carrier-tower-drop-prepare-v01-1024x576.png`；输出主体/参考框为 `(261,92)..(743,507)`，不做非等比拉伸。
- 豆包目标为一次性 `recover` 本体动画：约 0.85/1.20/1.55 秒发生三次可数的整身小幅压低—回弹，间隔约 0.35 秒；六足全程落地且不换位，塔楼只随整体承重平移，垂直轴与拓扑不变。三处预测点、石冰块投落、每目标最多两次命中、物理伤害与 0.35 秒事件调度全部属于未来运行时事件，不进入本体视频。
- 提交前执行 `--fill-only` 字符/哈希回读，只允许 1 个 `Seedance 2.0 Mini` 候选，不自动并发重抽。原片必须先通过 32 点联系图、动态 GIF、六足/塔楼近看和三拍密集表；相机、体轴、足数、塔身刚性、三拍可数性或 VFX 纯净度任一失败，就停在候选层，不进入 BiRefNet、RIFE 或正式表。
- 当前三张正式本体表为 `124.7500 MiB`。用户在明确获知下一张会超过 128 MiB Boss 目标线后回复“继续”，本动作登记为用户接受的目标线例外；256 MiB 家族硬停线仍有效，正式构建前后必须按实际图集 RGBA 重新核算。
- 本轮只解锁 `tower_drop_body` 离线本体素材。`tower_aim -> tower_drop_events -> tower_recover` 运行时状态、预测点锁定、三次投落事件、伤害、命中次数上限和冷却尚未接入；`plow_move` 仍因 Collider 推进、冲锋本体、撞墙收势、轨迹与伤害合同缺失而冻结。

### `tower_drop_body` v01 原片拒绝与 v02 修正门

- v01 `--fill-only` 回读 768 个编辑器字符，SHA-256 为 `a522a7562a4f144792a124ad94b0d4f7fe99b3bfffb0ba70457274e401bee166`；只提交 1 个候选。原片 `videos/01-snow-sepulcher-carrier-tower-drop-body-doubao-v01.mp4` 为 1280x720、24fps、121 帧、约 5.056 秒。
- 32 点联系图、动态 GIF 和 16 帧塔/反冲近看表确认融合塔楼没有重建，也没有生成落石或外部 VFX；但角色/摄影机在三拍之间明显左右偏航，右向低三分之四透视来回改变，三次承重又被放大成左右摆身。该候选在方向/节拍门禁拒绝并冻结，不进入 BiRefNet、RIFE 或正式表。
- 用户此前要求继续抽到合格为止；v02 仍沿用同一已通过六足撑地身份门的动作参考，但删除“向后承重”，改为仅允许竖直微压缩。塔尖中心、右侧头甲尖端、身体中心与六个落脚点的横坐标必须逐帧锁定；禁止镜头运动、透视变化、转体与侧摆。
- v02 仍按单条顺序提交并先做 `--fill-only`。合格线不变：固定右向体轴、六足原位、竖直刚性塔、三次可数的纯竖直微压缩、无外部表现；任一失败继续停在候选层。

### `tower_drop_body` v02 原片拒绝与 v03 节拍门

- v02 `--fill-only` 回读 869 个编辑器字符，SHA-256 为 `ec8a35e5a79bfdfe12e33b07d394f2169add6508127d9f93cc1a098db07706d1`；只提交这一条新候选。原片 `videos/01-snow-sepulcher-carrier-tower-drop-body-doubao-v02.mp4` 为 1280x720、24fps、121 帧、约 5.056 秒。
- v02 已明显收住镜头和横向摆身，六足、右向头甲与竖直塔楼可追踪，画面无落石/VFX；但 `f18..f36` 是一次长压低后回正，`f50..f84` 又形成第二次长压低，未出现三次彼此分离且每次完整回弹的反冲拍。因此在节拍门禁拒绝并冻结，不进入抠图、插帧或正式表。
- v03 将动作改写为六个明确姿态：约 0.65 秒下、0.82 秒上、1.00 秒下、1.17 秒上、1.35 秒下、1.52 秒上；每次最低点停留不超过 0.10 秒。相机、右向轴、六个足锚、塔尖中心及刚性拓扑门槛不变，仍只按顺序提交一个候选。

### `tower_drop_body` v03 付费额度阻断

- v03 `--fill-only` 回读 723 个编辑器字符，SHA-256 为 `9676382b089dff4ce16cec67048462bfb234b9200c80d5bc93dab6c26b7b4bc8`，检查阶段没有上传或消耗额度。
- 随后的生成请求没有产出视频；等待到 1800 秒超时后，只读检查当前豆包会话确认页面停在“今日视频生成免费额度已用完，本次将消耗付费额度，是否继续生成？”并要求点击“确认生成”。付费确认不在当前授权内，因此没有点击、没有重复提交，也没有 v03 MP4。
- 在 v03 阶段，`tower_drop_body` 当时停在“v01 方向/摆身拒绝、v02 三拍节奏拒绝、v03 付费确认阻断”。三张当时的正式表和 `124.7500 MiB` 家族预算不变；后续改走本地 H3 与既有原生键派生路线，结果见本文末的 v06 正式表结论。

### `tower_drop_body` H3 v04 免费额度回退门

- 用户在豆包付费确认阻断后要求继续，但没有授权付费。按 `tools/ai-gen/WORKFLOW.md` 第 3.6 节“确认豆包免费额度不可用后考虑 H3”的回退规则，改用此前已授权的本地 MiniMax H3 / 用户自有 5080，只生成 1 个 v04 候选。
- H3 首帧改用已通过同角色方向/拓扑门的 `animation/action-references/01-snow-sepulcher-carrier-plow-windup-v02-1024x576.png`：它比绿色 imagegen 工作母帧具有更干净的纯白视频背景，同时保持右向低三分之四、原地六足撑地、竖直融合塔楼和安全框。绿色动作母帧仍保留为姿势设计来源，不把其背景矩形传给 H3。
- 提示文件为 `animation/prompts/01-snow-sepulcher-carrier-tower-drop-body-h3-v04.txt`，H3 使用 `free` 时间合同，避免 `recover` 包装器追加“单一峰值”而与三连发冲突。目标仍为 0.65/0.82、1.00/1.17、1.35/1.52 秒三组下—上关键姿态；外部预测点、投落、伤害和状态机保持分离。
- v04 原片仍须先过 24～32 点方向、六足/塔楼、三拍节奏和无 VFX 门禁。失败即冻结候选，不进入 BiRefNet、RIFE 或正式表；通过后才按真实源帧重建事件时钟和家族预算。

### `tower_drop_body` H3 v04 拒绝与 Ref2VA v05 门

- H3 v04 原片 `animation/videos/01-snow-sepulcher-carrier-tower-drop-body-h3-v04.mp4` 为 1024x576、24fps、124 帧、约 5.167 秒；相机/右向轴和竖直塔楼大体可读，但动作被扩展为贯穿整段的多次大幅蹲伏，末尾仍继续重复，既不是三次短促回弹，也没有按 2.30 秒后保持收势。原片在节拍与非重复门拒绝，不进入 BiRefNet/RIFE。
- v05 改用 H3 Reference 模式：身份图继续使用已过门的 `plow-windup-v02-1024x576.png`，动作参考由 `build-tower-drop-motion-reference.py` 从豆包 v02 的完整原帧精确复用构成，不裁切、不缩放、不插值、不重绘。参考序列先保持源 f14，然后把源 `[f18,f20,f22,f32,f34,f36,f38,f40]` 的一次下—上完整段连续复用三遍，最后保持 f40；三个低点在参考输出 f14/f22/f30，间隔均为 8 帧即约 333ms。
- Ref2VA 提示明确让 `<Picture 1>` 只定义身份、`<Video 1>` 只定义三拍时间与竖直轨迹。v05 仍只生成 1 个候选，须重新通过固定朝向、六足、塔楼、三次且仅三次回弹、尾段静止与无外部 VFX 门禁。

#### v05 动作参考提交前替换

- `tower-drop-three-pulse-motion-v01.mp4` 虽然按 8 帧间隔精确复用了三遍下—上段，但上传前动态检查发现源豆包 v02 的压低姿态仍带轻微透视摆动。该参考只作为审计记录保留，不上传 H3。
- 正式 v05 动作参考改为 `animation/action-references/01-snow-sepulcher-carrier-tower-drop-three-pulse-abstract-v02.mp4`：由 `build-tower-drop-abstract-motion-reference.py` 绘制白底抽象刚性塔/躯干和六个固定足锚，只有躯干与塔沿竖直轴按 `[0,6,12,18,12,6,0,0]` 像素位移形成一次下—上，并连续复用三次。三个低点为输出 `f15/f23/f31`，间隔均为 8 帧，`f36..f123` 完全静止。
- 抽象参考不含项目身份、材质或外部效果，只负责节拍与竖直轨迹；身份、六足拓扑、右向相机和雪山塔楼全部由 `<Picture 1>` 约束。

### H3 Ref2VA v05 拒绝与豆包 v02 子段派生门

- H3 Ref2VA v05 原片 `animation/videos/01-snow-sepulcher-carrier-tower-drop-body-h3-reference-v05.mp4` 为 1024x576、24fps、124 帧、约 5.167 秒。三拍参考影响了上下轨迹，但 H3 把驮城兽重画为悬空的细腿龙形生物：六足不再落地，脚端、山体比例、头甲和塔楼/城垛拓扑均脱离通过身份，故在身份/足锚/塔楼门禁拒绝，不进入抠图或正式表。
- 豆包 v02 整片此前只因“三拍被做成两次长压低”而拒绝；重新密查其源 `f14/f16/f18/f20/f22` 后，该短子段保持右向低三分之四、六足承重职责、竖直融合塔楼与无外部 VFX，允许作为一次压缩的原生键来源。
- `build-tower-drop-derived-source.py` 只以完整原帧序列 `[f14,f16,f18,f20,f22,f20,f18,f16]` 组成一次压缩—回弹，精确复用三遍；不裁切、不缩放、不平移、不插值、不重绘或局部变形。输出前保持 f14 共 12 帧，三次低点位于派生 `f16/f24/f32`，间隔 8 帧约 333ms，尾部再保持 f14 共 20 帧。
- 派生视频不会登记为新生成结果；其 provenance 必须保留完整输出到源帧映射与三个 `f16 -> f14` 接点。先审派生原片 GIF；只有方向、身份、接点和三拍全部通过，才允许对五张唯一原生帧做 BiRefNet，并在无 RIFE 的前提下按相同映射构建正式表。

### `tower_drop_body` 派生 v06 通过与正式表门禁

- 派生 v06 动态 GIF 与接点密集表通过：固定右向低三分之四体轴，六足职责、竖直融合塔楼和足底根点保持稳定；三次压缩—回弹清楚可数，三个 `source f16 -> source f14` 周期间接点没有横向跳变，`f36..f55` 保持收势。
- 正式构建不从派生 MP4 取帧，直接从豆包 v02 原片提取唯一源 `f14/f16/f18/f20/f22`。五张 BiRefNet 探针保住塔尖、雪坡尾、六个足端和共同脚线；透明键统一到 `416x256`、脚点 `(208,240)` 后，再按派生 provenance 的 56 项映射完整复用/反序复用。全程 `interpolation.passes=0`，不使用 RIFE、局部变形或重绘。
- 正式 GIF 为 `animation/formal/snow-sepulcher-carrier/tower-drop-body/previews/snow-sepulcher-carrier-tower_drop_body.gif`，56 帧 / 2333ms、`repeat=0`；三次低点与外部事件拍为 0-based `f16/f24/f32`，相隔 8 帧约 333ms。Alpha 底边全程 `y=239`，无空帧、触边帧、透明区脏 RGB，所有输出映射位置逐像素等于对应原生透明键。
- `tower_drop_body` 因此只在“离线正式本体素材”层解锁。豆包 v01/v02 整片、H3 v04/v05 的拒绝结论与豆包 v03 的未付费阻断历史均保留；三个预测落点、石冰块/预警、120 半径、`1.45x` 物理伤害、每目标最多两次、11 秒冷却及 `tower_aim -> tower_drop_events -> tower_recover` 运行时状态仍未完成。
- 驮城兽四张正式表合计解码 RGBA `147.5000 MiB`，超过 128 MiB 目标线但低于 256 MiB 硬停线；该超线已按用户在开工前知情后继续的指令登记为目标线例外。

### `tower_drop_body` v06 用户观感拒绝与 H3 v07 自然承重重生门

- 用户检查正式 GIF 后明确反馈“动作太僵硬了重新生成”。该结论覆盖 v06 先前的离线结构通过：v06 保留为结构安全派生审计件，但不再作为合格或可接入的正式动作，不得因空帧/脚线/拓扑指标通过而忽略动态观感否决。
- 本轮重新查看同角色已认可的 `advance` 六足近看、`trample_body` 踏地近看、`plow_prepare` 联系图以及豆包 v02 首次压低密集表。固定相机仍为轻俯视、侧面占主导的右向低三分之四；长楔头甲位于屏幕右侧，塔楼竖直，六足共享地面线。自然运动基准改为前/中/后足错峰承重、关节缓入缓出、头部小幅反作用和雪壳余振，而不是六足同步活塞式上下。
- 从豆包 v02 源 f14 准备的白底参考因绿幕边缘仍有绿色残留，在上传前拒绝；v07 改用已通过 `advance` 方向/拓扑门的 `animation/references/01-snow-sepulcher-carrier-locomotion-1024x576.png` 同时作为 H3 首帧和尾帧，锁定 1024x576 构图、右向轴、六足、塔楼与最终恢复姿态。
- v07 使用本地 MiniMax H3、5.17 秒/124 帧、1024x576、20 步、单候选、`first-frame + last-frame` 双锁和 `free` 时间合同。提示把三拍改为幅度递增且支撑链错峰的连续承重：第一拍 0.55..1.15 秒、第二拍 1.15..1.80 秒、第三拍 1.80..2.60 秒，2.60..3.35 秒余振衰减，3.35 秒后只保留轻微活体呼吸，不得产生第四拍。
- 原片必须先通过固定镜头/右向轴、恰好六足、足端不换位、塔楼刚体、三拍可数、关节错峰自然度、尾段不重复和无外部 VFX 门禁。通过前不运行 BiRefNet、RIFE 或替换正式表；正式事件帧必须按新原片重算，不能沿用 v06 的 `f16/f24/f32`。

#### H3 v07 原片拒绝与 v08 单峰自然架势改案

- v07 原片为 1024x576、24fps、124 帧、约 5.167 秒；首尾双锁保住右向相机、六足、塔楼和最终恢复，关节过渡也比 v06 柔和。但 H3 把 0.55..2.60 秒的三组描述融合为一次持续下压和一次回正，缺少三个可分离的攻击读数，因此在原片动作语义门拒绝，不进入 BiRefNet/RIFE。
- v08 不再让整具巨兽重复三次蹲伏：本体改成一次 `rear-load -> rise-anticipation -> deep-brace -> firing-hold -> recover` 的有机承重链。三枚塔垛坠落由运行时在 `firing-hold` 内依次触发，本体只提供稳定发射架势；这保留三段攻击合同，同时避免同步活塞动作。
- v08 继续用同一已通过的 `locomotion-1024x576.png` 双锁首尾，改用适配单峰攻击的 `recover` 时间合同。原片门禁为固定右向相机、六足独立关节传力、单次清楚的低位峰值、稳定但不机械冻结的发射保持、完整自然回收、塔楼刚体与无外部 VFX；通过后再按实际保持窗重新放置三次运行时事件帧。

#### H3 v08 通过与自然动作正式表

- v08 原片 `animation/videos/01-snow-sepulcher-carrier-tower-drop-body-h3-v08.mp4` 为 1024x576、24fps、124 帧、约 5.167 秒，首尾均锁定已通过的 `advance` 身份参考。动态 GIF、32 点联系图与 16 帧近看确认：后足先蓄力，前躯抬势后沉入一次低位架势，六足关节存在错峰承重，保持段有微小活体张力，随后完整回正；右向轴、六足职责和融合塔楼稳定，无落石或外部 VFX。该动作通过原片自然度/身份/方向门。
- 正式表取源 `f0,f2,...,f122` 共 62 张 BiRefNet 透明原生键，排除只用于首尾身份精确锁的源 `f123`；一次性非回绕 RIFE 2x 后得到 123 帧 / 5083ms，单格 `448x256`、9 列 x 14 行、图集 `4032x3584`、脚点 `(224,240)`，`repeat=0`。
- 0-based 相位为 `rear_load 0..18 -> rise_anticipation 19..40 -> deep_brace 41..63 -> firing_hold 64..80 -> recover 81..122`。三枚外部塔垛坠落事件移到稳定架势的 `f64/f72/f80`，1-based 消费者为第 `65/73/81` 帧，相邻间隔 8 帧、按正式时钟约 331ms；不再要求本体同步蹲伏三次。
- 正式 GIF 为 `animation/formal/snow-sepulcher-carrier/tower-drop-body/previews/snow-sepulcher-carrier-tower_drop_body.gif`。Alpha 底边全程 `y=239`，无空帧、触边帧或透明区脏 RGB，原生键逐像素保留在偶数索引；RIFE 只有一对修复 9 个可见暗异常像素，无保持回退、红色异常或脚线位移。
- v06 明确保留为“用户拒绝的僵硬废案”，v07 保留为“一次长压、三拍语义失败”的原片废案；当前合格正式来源只有 v08。驮城兽四张活动正式表合计解码 RGBA `179.8750 MiB`，超过 128 MiB 目标例外线但低于 256 MiB 硬停线。预测点、落石/预警、120 半径、`1.45x` 物理伤害、每目标最多两次、11 秒冷却与运行时状态仍未接入。

## 2026-09-05 H3 `antler_body` 开工方向门

- 身份与相机首尾都锁定已通过 `stride` 的 `references/03-white-silence-bell-hart-locomotion-1024x576.png`。真实动作对照重新查看了 `stride` 与 `double_toll_body` 的 24 点联系图：轻俯视、侧面占主导的低三分之四视角稳定，喙状头、胸腔、胯、膝和蹄尖共同朝画面右侧；四足、腹钟和恰好三枚铃坠均可追踪。
- 本动作不用钟摆预备首帧，也不重画母图。动作轴为“后足错峰承重 -> 近侧前蹄短探 -> 肩颈斜后方蓄扭 -> 单次向右前下方叉角刺扫 -> 四肢顺序卸力回位”；远侧前足保持枢轴，避免四足同步蹲起造成僵硬感。鹿角视作刚性拓扑，攻击过程中不得增枝、融化或软弯。
- 目标时间线为 `0.00..0.60s` 活体警觉、`0.60..1.35s` 重心后移与短探步、`1.35..2.05s` 蓄扭、`2.05..2.60s` 唯一刺扫、`2.60..3.25s` 惯性吸收、`3.25..5.17s` 回到精确首姿。腹钟只允许小幅被动惯性摆动，不得抢占双响技能的主要摆钟语义。
- 使用用户自有 5080 MiniMax H3，首尾同图双锁、1024x576、5.17 秒、20 步、`recover`、纯视觉。原片必须先通过动态 GIF、32 点联系图和刺扫近看表；重点检查右向体轴、四条承重腿、刚性鹿角、腹钟/三枚铃坠、唯一接触峰及根点。失败则留在候选层重抽，不进入 BiRefNet/RIFE。
- `antler_body` 只提供身体表。前方 230 窄扇区、物理 `1.25x`、1 秒跛行、命中结算和运行时状态机全部保持外置；原片通过后才从实际动作确定事件帧，不先按提示词时间硬写事件。

### H3 `antler_body` v01 原片通过与正式表

- 原片 `animation/videos/03-white-silence-bell-hart-antler-body-h3-v01.mp4` 为 1024x576、24fps、124 帧、约 5.167 秒，首尾同图双锁。动态 GIF、32 点联系图和两组近看表确认：近侧前蹄先短探，后足错峰承重，肩颈抬起蓄扭后只做一次快速前下刺扫，随后前躯吸收惯性并逐段回正；不是四足同步蹲起，也不是整体平移。
- 右向低三分之四体轴、四条承重腿、刚性分枝鹿角、空腔胸架、腹钟和恰好三枚铃坠均保持可追踪。原片 `f48/f50` 是强运动模糊帧，因此不作为正式原生键；保留轻微动势的 `f47`，然后跨到清晰接触姿态 `f52` 做一次非回绕 RIFE 过渡。
- 正式源键为 `f0,2,...,46,f47,f52,54,...,104` 共 52 张；一次性非回绕 RIFE 2x 得到 103 帧 / 4333ms，单格 `288x256`、8 列 x 13 行、图集 `2304x3328`、脚点 `(144,240)`。为保住最高蓄扭帧的完整鹿角，统一体高取 208；没有裁角或放宽安全边。
- 0-based 相位为 `alert 0..14 -> weight_shift_and_probe 15..32 -> neck_coil 33..46 -> antler_strike 47..50 -> contact_followthrough 51..66 -> recover 67..102`，唯一外部接触事件为 `f50`（源 `f52`，1-based 第 51 帧）。Alpha 底边为 `y=239..240`，无空帧、触边帧或透明区脏 RGB，原生键逐像素保留在偶数索引。
- 正式 GIF 为 `animation/formal/white-silence-bell-hart/antler-body/previews/white-silence-bell-hart-antler_body.gif`。鸣钟鹿 `double_toll_body + antler_body` 家族解码 RGBA 合计 `43.3125 MiB`，低于 128 MiB 目标；前方 230 窄扇区、`1.25x` 伤害、1 秒跛行、命中和运行时状态机仍未接入。

### `antler_body` v01 用户镜头否决与 H3 v02 固定画布重制门

- 用户检查正式 GIF 后确认动作本身没有问题，但指出镜头存在位移并要求重新调整。该观感结论覆盖 v01 先前的离线通过：v01 的动作节拍可作为文字参考，但原片和由它生成的正式表不再是合格活动来源，不得靠透明表逐帧居中、平移、缩放或裁切伪装成固定镜头。
- v02 继续首尾双锁 `references/03-white-silence-bell-hart-locomotion-1024x576.png`，保持 v01 已获认可的“前蹄短探 -> 后足错峰承重 -> 肩颈蓄扭 -> 单次前下刺扫 -> 惯性吸收 -> 自然回位”。相机合同提升为固定 1024x576 画布坐标：焦距、尺度和白边全程不变，后侧承重蹄与骨盆根保持在开场屏幕区域；头角刺向右下时必须在静止画布中发生可见位移，镜头不得跟随、重居中或缩放补偿。
- v02 使用本地 MiniMax H3、1024x576、5.17 秒、124 帧、20 步、单候选、`recover` 和同图首尾双锁。原片须先看动态 GIF、32 点联系图和固定画布近看；除四足/鹿角/钟体拓扑外，重点核对后侧承重蹄、骨盆/钟架屏幕轨迹与中立段主体尺度。任何整体平移、推拉、跟拍或重构图都直接重抽，不进入 BiRefNet/RIFE。
- v01 的正式 `f50` 事件和 4333ms 时钟随活动来源一并失效。v02 通过后必须按新原片重新选清晰源键、重算接触帧、动作墙钟和家族预算，不能机械沿用旧帧号。

#### H3 v02 固定画布失败与 v03 蹄点注册门

- v02 原片 `animation/videos/03-white-silence-bell-hart-antler-body-h3-v02.mp4` 保持 1024x576、24fps、124 帧和首尾同图，但固定画布审计仍未通过。非白主体原始帧轨迹显示：开场后侧蹄约为 `x315`，从 `f24` 到 `f84` 长时间落在 `x249..250`；骨盆/躯干锚在活动窗内相对开场约 `-55..+65px`。这不是只由最右前蹄短探产生的局部位移，仍会形成用户指出的镜头/构图漂移观感。
- v02 在原片镜头与根点门禁拒绝，不执行 BiRefNet、RIFE 或正式表替换。审计证据为 `animation/videos/03-white-silence-bell-hart-antler-body-camera-audit.json` 与同名 PNG；其中只量原始帧，没有对任一视频做平移、缩放或稳定化。
- v03 继续使用同一首尾身份参考与自然刺扫语义，但把画面注册写成显式屏幕合同：开场四个蹄点约为 `x315/x387/x460/x532`，前三个支撑蹄保持原位，最右前蹄只可小幅抬起并回到 `x532`、不得越过 `x565`；骨盆根保持在 `x429±12px`。所有大幅动作改由肩颈与鹿角完成，禁止用后蹄后滑扩大姿势。
- v03 仍是本地 H3 单候选、首尾同图、5.17 秒、20 步、`recover`。通过条件为原片主体尺度稳定、三支撑蹄轨迹和骨盆根符合上述范围、动作自然、四足/鹿角/钟体拓扑稳定；通过前不重建正式表。

#### H3 v03 原片拒绝与 v04 背景注册门

- v03 原片继续保持 1024x576、124 帧和首尾同图，但显式像素提示仍没有锁住中段构图。原始非白主体审计在活动窗得到骨盆根相对开场 `-50..+38px`、后侧承重蹄 `-48.75..+9.34px`、主体高度比例 `0.7064..1.1432`；联系图也能看到蓄扭到刺扫期间整身先左移再被重新居中。v03 因此在原片层拒绝，不进入 BiRefNet/RIFE。
- v04 不改用户已认可的单次刺扫语义，改用 `references/03-white-silence-bell-hart-locomotion-fixed-camera-1024x576.png` 首尾双锁。该图只在身份参考的空白背景添加外框、四个角标和下方基准线，角色像素完全不变；provenance 由同名 JSON 记录，参考图只用于生成与镜头审计，不是运行时素材。
- v04 提示改为更短的“原地防守式刺扫”：三只支撑蹄保持开场接触，近侧前蹄可小幅抬起并回原位，骨盆停在支撑三角上，攻击位移集中到肩、颈和头。背景注册标记必须全程静止，用于直接暴露推拉、平移或重构图，不能靠后期稳定化补救。
- 原片只有同时通过注册标记静止、主体尺度稳定、支撑蹄/骨盆不漂、动作自然、四足/刚性鹿角/一钟三坠拓扑稳定，才允许进入透明键与正式表。

#### H3 v04 固定镜头通过与正式表

- v04 原片 `animation/videos/03-white-silence-bell-hart-antler-body-h3-v04.mp4` 为 1024x576、24fps、124 帧、约 5.167 秒。外框、四角标和下方基准线全程静止；三个支撑蹄在活动窗相对开场约只漂 `-0.13..+1.08px`，主体左边界保持 `x301`，近侧前蹄单独抬落。联系图确认一次原地前下刺扫、完整回收、四足、刚性鹿角、一钟三坠和右向低三分之四身份均可追踪。
- BiRefNet 探针完整移除临时注册背景，没有把外框、角标或基准线带入透明键。正式构建固定使用原片源锚 `x429`，所有帧共享同一个源坐标映射；不逐帧估算重心、平移、缩放或裁切来补偿镜头。
- 正式源键为 `f0,2,...,100` 共 51 张，一次性非回绕 RIFE 2x 后为 101 帧 / 4167ms，单格 `352x256`、8 列 x 13 行、图集 `2816x3328`、脚点 `(176,240)`。0-based 相位为 `alert 0..16 -> weight_shift_and_probe 17..32 -> neck_coil 33..44 -> antler_strike 45..60 -> contact_followthrough 61..70 -> recover 71..100`，唯一外部接触事件为 `f60`（1-based 第 61 帧）。
- 正式 GIF 为 `animation/formal/white-silence-bell-hart/antler-body/previews/white-silence-bell-hart-antler_body.gif`。无空帧、触边帧、透明区脏 RGB 或插帧脚线位移，原生键逐像素保留在偶数索引；鸣钟鹿两张活动本体表合计 `49.8125 MiB`，低于 128 MiB 目标。前方 230 窄扇区、`1.25x` 物理伤害、1 秒跛行、命中和运行时状态机仍未接入。

## 2026-09-05 `long_tone_body` 开工方向门

- 同角色真实动作参考重新查看：`stride` 原片联系图 `f0/f32/f64` 用于右向步轴、四足和脚线；`double_toll_body` 正式表 `f0/f24/f50/f72` 用于钟摆轴与一钟三坠；`antler_body` v04 正式表 `f0/f60/f100` 用于固定摄影棚坐标和非逐帧重居中的根点策略。
- 相机继续为轻俯视、侧面占主导的右向低三分之四；喙尖、颈胸、骨盆、膝和蹄尖沿同一右向身体轴。身份/构图源为 `references/03-white-silence-bell-hart-locomotion-fixed-camera-1024x576.png`，外框、四角标与下方基准线用于暴露任何镜头移动。
- 动作关键帧目标：四蹄保持原屏幕接触，骨盆和肩部略降形成非同步承重，颈部微抬；唯一腹钟从原顶端铰点向屏幕左后方明显斜摆，恰好三枚垂饰分别随钟后摆。关键帧不含 620x120 共振、冰纹、光环或伤害表现。
- 关键帧通过后，视频时间线才允许为 `brace_and_draw 0..约1.3s -> forward_release -> resonance_hold -> recover`。本体只提供钟摆、身体吸收和回收；`long_tone_event` 的前方 620x120 长矩形、魔法 `1.60x`、0.7 秒眩晕与 12 秒冷却继续外置。原视频必须先通过固定注册背景、四足、一钟三坠、唯一释放峰和完整回收门禁，再做 BiRefNet/RIFE。

### `long_tone_body` imagegen v01 关键帧通过

- 编辑目标为固定相机身份参考 `references/03-white-silence-bell-hart-locomotion-fixed-camera-1024x576.png`；生成器为内置 imagegen，提示记录在 `prompts/03-white-silence-bell-hart-long-tone-keyframe-imagegen-v01.txt`，项目归档为 `action-keyframes/03-white-silence-bell-hart-long-tone-prepare-v01.png`。
- 关键帧保持右向侧面占主导的低三分之四轴、四条清楚承重腿、刚性鹿角、空腔胸架、唯一腹钟和恰好三枚垂饰；腹钟从原顶端铰点向屏幕左后方斜摆，外框/角标/基准线仍可用于镜头注册，未生成声束、冰纹或光环。
- H3 参考由 `prepare-white-silence-long-tone-reference.py` 只做等比缩放和白底补边，输出 `action-references/03-white-silence-bell-hart-long-tone-prepare-v01-1024x576.png`；禁止非等比拉伸、裁切或逐部件变形。
- 视频使用首尾同图双锁、5.17 秒、124 帧、1024x576、20 步、`recover`。唯一主动峰为约 1.30..1.70 秒的向右前摆；随后只允许更弱的被动衰减回摆，不能形成第二次伤害读数。

### H3 `long_tone_body` v01 固定镜头通过与正式表

- 原片 `animation/videos/03-white-silence-bell-hart-long-tone-body-h3-v01.mp4` 为 1024x576、24fps、124 帧、约 5.167 秒，首尾锁定同一注册关键帧。外框、四角标和下方基准线全程静止；四蹄保持开场接触，后侧承重蹄在活动窗相对开场约只漂 `-0.13..+1.51px`。主体轮廓根代理会被巨钟与上身前摆拉向右侧，不能把该轮廓变化误判为镜头平移。
- 动态 GIF、32 点联系图、释放密集表和尾段密集表确认：唯一腹钟先后摆蓄势，再随上身做一次强前摆，`f56` 首次达到最强释放姿并短暂保持；随后只有一次更弱的被动回摆，并在源 `f108` 回到开场姿态。四足、右向喙首、刚性鹿角、空腔胸架、一钟三坠和身份比例均稳定，没有外部共振/VFX。
- BiRefNet 探针完整移除注册背景。正式构建固定使用原片源锚 `x439`，保留源 `f0,2,...,108` 共 55 张透明原生键；一次性非回绕 RIFE 2x 得到 109 帧 / 4500ms，单格 `320x256`、10 列 x 11 行、图集 `3200x2816`、脚点 `(160,240)`。
- 0-based 相位为 `hold_prepare 0..8 -> backswing_charge 9..32 -> forward_release 33..56 -> resonance_hold 57..68 -> damped_return 69..92 -> recover 93..108`。唯一外部事件为 `longToneReleaseFrame=56`（1-based 第 57 帧）；保持与回摆段不得重复伤害。
- 正式 GIF 为 `animation/formal/white-silence-bell-hart/long-tone-body/previews/white-silence-bell-hart-long_tone_body.gif`。无空帧、触边帧、透明区脏 RGB 或插帧脚线位移，原生键逐像素保留在偶数索引。前方 `620x120` 长矩形、`1.60x` 魔法伤害、0.7 秒眩晕、12 秒冷却、VFX、命中和运行时状态机仍未接入。

## 2026-09-05 H3 `hoof_sequence_body` 开工方向门

- 同角色真实动作对照继续使用：`stride` 原片 `f0/f32/f64` 核对四足职责与右向轴，`double_toll_body` 正式 `f0/f24/f50/f72` 核对一钟三坠，`antler_body` v04 正式 `f0/f60/f100` 与 `long_tone_body` 正式 `f0/f56/f108` 核对注册背景、固定源锚和支撑蹄策略。
- 首尾锁定 `references/03-white-silence-bell-hart-locomotion-fixed-camera-1024x576.png`。相机为轻俯视、侧面占主导的右向低三分之四；喙首、胸腔、骨盆、膝和蹄尖共同朝右。开场四个可读蹄点约为屏幕 `x315/x387/x460/x532`，动作顺序固定为从屏幕左到右逐蹄抬落。
- 每拍只能有一条腿沿真实膝/踝链小幅竖直抬起并落回自身开场接触；另外三蹄保持注册，不允许把四拍做成走路、跳跃、四足同步活塞或整体横移。落蹄后的反作用按后躯到前躯错峰传递，腹钟只做次要被动摆动。
- 目标落蹄时刻约为 0.85/1.18/1.51/1.84 秒，对应未来正式表相邻约 8 帧的四个单次事件；事件帧最终必须从真实原片重新核定，不能只照提示词硬写。四个 105 半径圆区、物理 `1.00x`、每目标最多两次、8 秒冷却、落地 VFX、命中与状态机全部外置。
- 使用用户自有 5080 MiniMax H3，1024x576、5.17 秒、124 帧、20 步、单候选、首尾同图、`recover`、纯视觉。原片必须先通过注册背景静止、四足顺序、四个清楚落蹄读数、足点回原位、一钟三坠与完整回收门禁；通过前不做 BiRefNet/RIFE。

### H3 `hoof_sequence_body` v01 原片拒绝与 v02 低抬蹄门

- v01 原片保持注册外框、右向身份、四足、一钟三坠和首尾回位，但下半身密集表与固定蹄带审计只读出三段主动抬腿：屏幕最左蹄约 `f14..f42` 长时间悬空，中右蹄约 `f27..f42` 在第一蹄尚未落地时重叠抬起，最右蹄约 `f47..f60` 单独抬落；第二个蹄没有独立动作。它不是四个依次完成的节拍，因此在原片语义门拒绝，不进入 BiRefNet/RIFE。
- v02 保持同一固定注册参考与 5080 H3 参数，但压缩提示并把抬腿幅度限制为“蹄只离地少量、始终低于膝、不得收腿”。四个蹄点仍按 `x315 -> x387 -> x460 -> x532`，每只蹄必须完全回到自身接触后下一只才允许离地，任何双蹄同时悬空或缺拍都直接拒绝。
- 目标落地时刻约为 0.80/1.13/1.46/1.79 秒；这些仍只是生成引导。若 v02 通过，正式事件帧必须由原片落地接触重新核定，不能沿用提示词时间或 v01 的失败轨迹。

### H3 `hoof_sequence_body` v02 原片通过与正式节拍重定时

- v02 原片为 1024x576、24fps、124 帧、约 5.167 秒。四个固定蹄带分别读出互不重叠的低幅抬落：最左蹄 `f12..f21`、峰 `f16`、回地 `f22`；第二蹄 `f30..f44`、峰 `f37`、回地 `f45`；第三蹄 `f48..f59`、峰 `f53`、回地 `f60`；最右蹄 `f71..f79`、峰 `f74`、回地 `f80`。没有双蹄同时悬空或缺拍，四足职责、一钟三坠和右向身份稳定。
- 注册外框/角标/基准线固定，躯干根代理在活动窗相对开场约 `-16..+11px`，主体高度比例约 `0.9786..1.0167`；位移来自单腿抬落时的局部承重，不是相机跟随。源 `f108` 已回到稳定姿态，尾后锁帧不纳入正式表。
- 原片四拍间的中立停顿偏长。正式构建只选取 29 张完整原生帧 `[0,4,8,10,12,16,19,21,22,30,37,42,45,48,53,57,60,71,74,78,80,84,88,92,96,100,104,106,108]`，将中立间隙压缩后做一次非回绕 RIFE；不裁腿、不做逐帧平移、缩放、形变或局部重绘。
- 正式表为 57 帧 / 2333ms、单格 `256x256`、6 列 x 10 行、图集 `1536x2560`、脚点 `(128,240)`。四个外部落地事件为 0-based `f16/f24/f32/f40`，相隔 8 帧、按正式时钟约 333ms；对应原片回地 `f22/f45/f60/f80`。
- 四个 105 半径圆区、单次物理 `1.00x`、每目标最多两次、8 秒冷却、落地 VFX、命中与运行时状态机仍未接入。正式 GIF 为 `animation/formal/white-silence-bell-hart/hoof-sequence-body/previews/white-silence-bell-hart-hoof_sequence_body.gif`。

## 2026-09-05 H3 `rhythm_shift_body` 开工方向门

- 继续以 `stride`、`double_toll_body`、固定镜头 `antler_body`、`long_tone_body` 与 `hoof_sequence_body` 的真实关键帧为同角色对照。视角保持轻俯视、侧面占主导的右向低三分之四，四蹄保持开场屏幕接触；喙首、颈胸、骨盆、膝和蹄尖不能转成正面或左向。
- 变调不是另一发长调攻击：巨钟保持近竖直，只做次要被动余摆；四腿从后到前传递张力，胸架和颈首抬起形成一个非伤害的阶段锁定姿。恰好三枚既有垂饰做一次紧凑的左到右响应，禁止增生垂饰、脱落或生成彩色第三环。
- 目标时间线为 `0.00..0.45s` 准备、`0.45..1.25s` 后到前张力波、`1.25..2.10s` 三垂饰短响应、`2.10..2.60s` 唯一阶段锁定姿、`2.60..3.55s` 前到后卸力、`3.55..5.17s` 精确回位。最终事件帧从真实峰值核定。
- 使用用户自有 5080 MiniMax H3，首尾锁定 `references/03-white-silence-bell-hart-locomotion-fixed-camera-1024x576.png`，1024x576、124 帧、5.17 秒、20 步、单候选、`recover`、纯视觉。阶段解锁、每两次双响后的第三回声计数、0.75 秒延迟、窄环 `0.90x` 魔法伤害、颜色、VFX 与状态机全部外置。

### H3 `rhythm_shift_body` v01 固定镜头通过与正式表门

- 原片 `animation/videos/03-white-silence-bell-hart-rhythm-shift-body-h3-v01.mp4` 为 1024x576、24fps、124 帧、约 5.167 秒，首尾锁定同一注册中立姿。外框、四角标和基准线全程静止；后侧承重蹄相对开场约 `-0.22..+0.63px`，四蹄始终接地，主体高度比例约 `0.9976..1.0381`。
- 动态 GIF、32 点联系图和胸钟近看确认：后躯张力传到胸颈，身体只形成一次抬胸校音与持续锁定，源 `f52..f68` 为清楚平台；巨钟附着且只作次要摆动，没有复刻长调的大幅横向释放。右向喙首、刚性鹿角、空腔胸架、四足、一钟三坠均保持可追踪，无彩色环或外部 VFX。
- 唯一阶段生效事件取锁定平台前段的源 `f52`；保持区不得重复触发。正式源窗拟取 `f0,2,...,108`，使用固定源锚 `x429` 与一次非回绕 RIFE；BiRefNet 探针通过前仍不登记正式资产。
- 该本体动作只承载非伤害阶段转换。生命低于 45% 的一次性触发、每两次双响后的第三回声计数、第二响后 0.75 秒调度、窄彩环、魔法 `0.90x`、伤害、VFX 和运行时状态机继续外置。
- BiRefNet 探针与正式表硬门通过。正式表取源 `f0,2,...,108` 共 55 张原生键，一次非回绕 RIFE 后为 109 帧 / 4500ms、单格 `256x256`、图集 `2560x2816`、固定源锚 `x429`；0-based 唯一阶段事件为 `f52`。无空帧、触边、透明区脏 RGB，Alpha 底边全程 `y=239`，原生键逐像素保留在偶数索引。
- 正式 GIF 为 `animation/formal/white-silence-bell-hart/rhythm-shift-body/previews/white-silence-bell-hart-rhythm_shift_body.gif`；只解锁本体阶段动作，不解锁运行时阈值、计数、调度、VFX 或伤害。

## 2026-09-05 H3 `stride` 正式循环补全门

- 移动原片仍使用已通过方向/拓扑门的 `animation/videos/03-white-silence-bell-hart-stride-h3-v01.mp4`，不重新生成身份。重新查看 24 点联系图的源 `f0/f16/f32/f48/f64/f80/f96/f112/f123`：轻俯视、侧面占主导的右向低三分之四轴稳定，四条长足交替迈步，腹钟只作小幅惯性摆动，鹿角和空腔胸架没有技能变形。
- 正式循环不得直接把 124 帧首尾锁区整段烘入。先在原始 1024x576 帧上搜索完整同相位周期，再以半开窗 `[start, duplicateEndpoint)` 保留唯一原生键；重复端点只用于闭环，不写入源表。循环 RIFE 必须回绕补齐尾到首的半步，不能靠重复首帧停顿。
- 方向仍以真实步态为准：喙首、胸腔、骨盆、膝、蹄尖朝屏幕右；四足不得换位、缺失或穿过空腔，视觉根保持原地，世界位移、寻路、速度、碰撞与运行时状态机全部外置。
- 原始相位扫描的最佳闭环为源 `f40 -> f93`，两帧主体面积比约 `1.01`，首尾平均 RGB 差约 `18.08`；视觉检查确认四肢相位、身体高度和右向构图一致。正式半开窗定为 `[f40,f93)`，活动窗躯干根代理约 `-12..+11px`、高度比例约 `0.9831..1.0337`，属于交替承重而非镜头跟随。
- BiRefNet 探针确认四条腿在中段短暂遮挡时仍可分别追踪。保留 `f40,42,...,92` 共 27 张原生键，一次回绕 RIFE 2x 得到 54 帧 / 2208ms；单格 `256x256`、图集 `1536x2304`、固定源锚 `x433`，尾到首由回绕半步补齐。
- 正式表无空帧、触边、透明区脏 RGB 或插帧脚线位移，Alpha 底边全程 `y=239`，原生键逐像素保留在偶数索引。正式 GIF 为 `animation/formal/white-silence-bell-hart/stride/previews/white-silence-bell-hart-stride.gif`；世界位移、导航、碰撞和状态机仍未接入。

## 2026-09-05 `double_toll_body` 族内尺度与固定源锚归一化门

- 本次只重制正式精灵表，不重抽视频、不改变动作语义。重新对照同角色已通过的 `stride` 正式 `f0/f20/f40/f53`、`antler_body` 正式 `f0/f60/f100`，以及 `double_toll_body` 现有正式 `f0/f24/f50/f72`：三者均为轻俯视、侧面占主导的右向低三分之四；喙首、胸腔、骨盆、膝和蹄尖共同朝屏幕右侧，四足、一钟与恰好三枚垂饰保持可追踪。
- 原片 `animation/videos/03-white-silence-bell-hart-double-toll-h3-v01.mp4` 的有效窗仍为源 `f12..f84`。原始像素相机审计显示后侧支撑蹄相对开场仅约 `-0.09..+0.12px`，轮廓根代理 `0..+12px` 的变化来自钟摆和承重变化，不是镜头移动；因此不得再逐帧重居中。
- 用有效窗首帧 `f12` 的 BiRefNet 原始抠图计算固定下半身源锚 `x417`，全部 37 张原生键共用这一锚点。族内目标高度由旧值 `224px` 归一为与本族其余五个正式动作一致的 `208px`，脚线继续固定在 `y=240`。
- 仍保留源 `f12,14,...,84`、一次非回绕 RIFE、73 帧 / 3000ms，以及外部事件 `warningRingFrame=24`、`damageRingFrame=50`。警示环、延迟伤害环、未来第三回声、命中与运行时状态机继续外置；本次不得顺带接入。

## 2026-09-05 `plow_prepare` 族内尺度与固定源锚归一化门

- 本次只重制正式精灵表，不重抽视频、不改变蓄力语义。重新对照同角色已通过的 `advance` 源 `f25/f41/f57/f73/f89/f105/f111`、`trample_body` 源 `f0/f26/f28/f48/f72/f84` 与 `plow_prepare` 源 `f8/f32/f60/f84/f100`：轻俯视、侧面占主导的右向低三分之四相机一致，右侧长楔头甲为前端，塔楼竖直，六足共享地面线。
- `plow_prepare` 仍只做原地错峰下压与回收，Collider 冲锋继续外置。用有效窗首帧 `f8` 的 BiRefNet 原始抠图计算固定下半身源锚 `x317`，全部 47 张原生键共用这一锚点，禁止逐帧重居中。
- 族内目标高度由旧值 `224px` 归一为与 `advance`、`trample_body`、`tower_drop_body` 一致的 `222px`；脚线继续固定在 `y=240`。保留源 `f8,10,...,100`、一次非回绕 RIFE、93 帧 / 3833ms 与 `fullyBracedFrame=60`，碰撞体推进、雪铲拖尾、冲击与伤害继续外置。
