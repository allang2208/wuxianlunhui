# 工程建筑系列：局部修正交付（2026-08-30）

用户确认的修正来源为营地48步02（124832）、工坊48步01（124841）、工厂48步01（124851）。三档修正版均已完成并直接查看；本轮不重新抽整栋建筑，不修改模型、科技配置或游戏资源。

## 修正内容

| 建筑 | 本轮处理 | 保持不变与限制 |
| --- | --- | --- |
| 工程师营地 | 使用局部候选01恢复横梁和左立柱的木色；右立柱在显式多边形内调整RGB。恢复原48步齿轮齿形，并降低局部黄铜饱和度与亮度。 | 皮革围护、茅草屋顶、入口、工具台和地台保持。吊架脚座仍为原灰石色，茅草细纹未重新处理。 |
| 工程工坊 | 使用局部候选02将拱形侧窗改成直边矩形窗；再单独去掉主入口旁的额外窗，恢复连续石墙，保护前景工具架。恢复原齿轮几何并压低亮金感。 | 红瓦屋顶、烟囱、开放作业口、绞盘与地台保持。新窗为四格，白模为双格；直边外形已恢复，但不宣称窗格完全复刻白模。地台凸石未改。 |
| 载具工厂 | 局部候选01补回双扇侧窗；最终使用同图相邻混凝土墙面纹理去除控制箱，原齿轮恢复并局部调色。 | 三条采光顶、开放卷帘口、吊具、工作台、主体与地台轮廓保持。原屋面与混凝土细纹未改。 |

## 过程与来源

- 第一轮六张局部生成使用原完整Depth、48步、CFG 3.5、Euler/simple、Depth 0.75。营地局部denoise 0.55，工坊/工厂0.70，均显式`--allow-nonstandard`并保留元数据。不能将这批写成标准0.30精修。
- 初轮多处同时修正时，额外窗/箱未消失，且生成器改变了齿轮齿形。这些未经处理的原始候选不直接作为最终交付。
- 去窗/去箱各为单张、单目的小蒙版试验：48步、Depth 0.75、denoise 0.90，显式记录非标准参数；保留初轮局部图与标准48步图。工坊去窗蒙版补齐了原先漏掉的拱顶，止于前景工具架之前。
- 工厂原Depth在箱体位置含有工具架背板；降低Depth到0.15的额外单张局部试验仍保留了面板，因此两次工厂去箱生成结果均未进入交付。最终由`repair-factory-wall-pixels.py`在明确的小多边形内，从同图同一墙面右上方取样（相对目标偏移[24,-92]），按周边色差匹配并向内羽化3.5px修补。只改目标RGB，取样避开门楣，来源与RGB偏移完整记录在`local_repair_v4_wall_pixels/`。
- 所有生成结果只在对应局部蒙版内合成；VAE造成的蒙版外变化不进入交付图。原RGB在零蒙版区直接保留，没有全图色调或轮廓变换。
- `finish-local-materials.py`恢复源图齿轮区域后，在该区域的黄铜色像素内做饱和度0.65、亮度0.72调整；齿数、轮廓、内圈和接触阴影来自已选48步图。营地右柱使用独立小多边形与RGB倍率[1.12, 0.78, 0.49]，补足初轮蒙版漏掉的柱面，保留原几何和明暗。
- 所有贴图仍为完整绿底RGB，没有抠图、Alpha修复、lighting maps、占地标定或运行时替换。最终透明边缘、外部阴影和游戏内尺寸仍属于后续正式入库步骤。

来源索引：`local-repair-index.json`；选择记录：`local-review-selection.json`；每张`*_finished_metadata.json`逐级指向原48步图、局部生成图、去附件图和相应蒙版。

本轮共保留六张初轮局部生成、工坊去窗一张、工厂去箱两次试验；交付只展示各档最终选出的修正版，失败图不包装为合格候选。

执行文件：`local-repair.py`、`remove-extra-details.py`、`repair-factory-wall-pixels.py`、`finish-local-materials.py`、`publish-local-repairs.py`。仅写入本系列候选目录及变更记录，未修改公共生成器、游戏源码、科技/建筑配置或其他会话内容。

查看结果：`previews/engineer-branch-local-repair-lineup.png`为三级总览；`previews/engineer-branch-local-repair-comparison.png`为修正前后对照；每档另有独立对照大图。

未运行测试或运行时验证，按约定由用户测试。修正版已直接查看并发布候选预览；未运行像素差异测试，局部外原像素由合成操作保留。图片观察不等于游戏内占地、遮挡和透明边缘验收。
