# 2026-07-03 工作日志：AI系统重构 + 50+怪物性能优化

## 版本号
**v0.196 → v0.197**（已提交 git + 打标签 + 备份）

## 今日目标
解决场景2/4面对50+怪海时的严重卡顿问题，重构AI系统架构，将移动/战斗/感知逻辑统一交给外部系统驱动。

## 关键修改（7 个核心文件）

### 1. 性能诊断
- **瓶颈1**：Enemy.update 中 O(n²) 目标扫描（100只僵尸×100实体=10,000次/帧）
- **瓶颈2**：PerceptionSystem._findBestTarget 每 500ms 全量扫描（10,000次距离计算）
- **瓶颈3**：A*寻路风暴（双重触发：Enemy._updateMovement + MovementSystem）
  - 100只僵尸每 500ms 触发寻路，单次 3,600 格子 × 32,400 次空间哈希查询 = ~50,000 次操作
  - 总计：100只 × 50,000 = 5,000,000 次操作/500ms
- **瓶颈4**：CombatSystem._updateAttack 每帧 100 次 WallSystem.blocked 射线检测

### 2. 架构重构（game.js + enemy.js）
- **game.js**：在 entity.update 后统一调用 MovementSystem/CombatSystem/PerceptionSystem
- **enemy.js**：当外部系统存在时跳过旧逻辑（移动/攻击/状态效果），避免重复调用
- **fallback 机制**：当外部系统不存在时，enemy.js 自动启用旧逻辑

### 3. MovementSystem 优化
- **寻路冷却**：`_pathfindCooldown = 2000ms`，冷却期间不触发 A* 寻路
- **SpatialPartition 范围查询**：替代 `entities.forEach` 全量遍历，只扫描附近实体
- **Grid 分辨率**：20px → 40px，减少 75% 的格子数

### 4. CombatSystem 优化
- **LOS 缓存复用**：优先使用 PerceptionSystem 的 `lastLOSResult`，避免每帧 WallSystem.blocked 调用
- 缓存未命中时 fallback 到 WallSystem 直接检测

### 5. 备份管理
- **清理旧备份**：删除 50+ 个旧备份文件（legacy_*.js、game-style_v*.css、index_v*.html），释放 18MB
- **保留备份**：v2026-07-03_22-24-01（v0.196 版本）
- **新备份**：v2026-07-03_23-16-19（v0.197 版本）

## 预期性能提升

| 优化项 | 当前成本 | 优化后 | 提升 |
|--------|----------|--------|------|
| 目标扫描 | O(n²)/帧 | O(n)/帧（SpatialPartition） | 100x |
| 寻路触发 | 双重触发，无冷却 | 单一触发，2秒冷却 | 10x |
| A* Grid | 3,600 格子/寻路 | 900 格子/寻路 | 4x |
| 视线检测 | 100次/帧 | 复用缓存，200ms一次 | 100x |
| **总体** | 卡顿严重 | 50-100怪流畅 | **~100x** |

## 已知问题
1. 需实际测试场景2/4确认怪物行为正常
2. 如果 MovementSystem 有 bug，enemy.js 有 fallback 逻辑
3. 战术小队偶尔被复杂地形卡住（v0.196 遗留）
4. 无人机 debuff 在指挥官死亡后不会自动清除（v0.196 遗留）

## Git 提交
- 提交：`e7da369` feat(v0.197): AI系统重构 + 50+怪物性能优化
- 标签：`v0.197`
- 变更：398 files, +13,745 -239,308（主要是删除旧备份文件）

## 备份路径
`C:\Users\allan\Documents\kimi\workspace\game-dev\backup\v2026-07-03_23-16-19\`

## 今日目标
将玩家同款武器系统（枪械/弹药/散布/过热）复用给战术小队（6人 AI 战术小队），修复弹道不可见，新增指挥官无人机技能与状态栏集成。

## 关键修改（41 个文件，+6647 -567 行）

### 1. 新增 Combatant 基类（src/entities/combatant.js）
- **目的**：共享武器/弹药/散布/过热系统，Player 和 Enemy 都继承
- **设计**：默认弹药无限、体力无限、散布/过热不生效；Player 覆盖实际检查；Enemy 覆盖简化逻辑
- **关键方法**：`fireProjectile()`、`canFire()`、`renderWeapon()`、`triggerWeaponAnim()`、`_consumeAmmo()`

### 2. 新增 HumanoidMonster 基类 + 5 个子类（src/entities/humanoid-monster.js）
- **Commander**：qjb201 轻机枪，无人机技能
- **MachineGunner**：pkm 机枪，火力压制
- **Rifleman**：akm 步枪，战术规避
- **FlankRifleman**：qbz191 步枪，侧翼包抄
- **ShieldBearer**：pistol + 盾墙，前排防御
- **关键**：从外部配置 `data/humanoid-squad-config.json` 读取武器和角色数据

### 3. 修复弹道不可见（致命 bug）
- **根因**：`fireProjectile` 中 `new Projectile(...)` 参数顺序错误
- `Projectile` 构造函数签名：`(x, y, angle, speed, maxRange, size, damage, piercing, source, entities, image, isTracer, isGold, isDarkGold, damageType, noRender, isGreen)`
- 第15位是 `damageType`，第16位是 `noRender`
- 原代码传：`false, // isGreen`（第15位）+ `'physical' // damageType`（第16位）
- 结果：`noRender = 'physical'`（truthy）→ `Projectile.render()` 中 `if (this.noRender) return` 直接退出
- **修复**：修正参数顺序，敌人使用 `isEnemy`（`true`）作为 `isTracer` 曳光弹参数

### 4. 修复 CombatSystem 重复方法覆盖
- **根因**：`combat-system.js` 中有两个 `_updateAttack` 定义，第二个覆盖第一个
- 第二个版本没有 `_isHumanoid` 分支，战术小队走传统路径时 `attack` 为 `undefined`
- **修复**：删除第二个重复方法，保留包含 `_isHumanoid` 分支的完整版本

### 5. 修复 renderer.js 小地图实体检测
- **问题 A**：`if (Array.isArray(Game.entities))` 永远为 `false`（`Game.entities` 是 `Map`）
- **问题 B**：`typeof Enemy !== 'undefined' && e instanceof Enemy` 永远为 `false`（`Enemy` 未导入 renderer.js）
- **修复**：使用 `Game.entities.forEach`（Map 原生方法）+ `e._faction === 'enemy'` 属性检查 + `e.itemData` 属性检查

### 6. 修复 Enemy 继承 Combatant 后数据字段覆盖
- **根因**：`Enemy` 构造函数直接 `this.data = { ... }` 覆盖了 `Combatant` 已创建的 `this.data`（包含 `name`, `level`, `stamina` 等）
- **修复**：`Object.assign(this.data, {...})` 合并而非替换

### 7. 新增指挥官无人机技能
- `TacticalSquadAI._updateCommander`：当玩家在 800px 内时自动施加 `applyDroneVulnerability(1)`
- `TacticalSquadAI.renderDrone`：绘制红色虚线圆圈（800px 半径）
- `Player.applyDroneVulnerability`：设置 `_droneVulnerabilityStacks` + `StatusBar.addEffect('droneVulnerability', 5000)`
- **状态栏图标**：🛸 无人机易伤（灰蓝色，5秒倒计时 + 进度条）

### 8. 修复 ES 模块中全局变量访问问题
- **根因**：`player.js` 没有 `import { StatusBar } from '../ui/status-bar.js'`，但使用了 `typeof StatusBar !== 'undefined'`
- 在 ES 模块中，`typeof` 对未导入的变量永远返回 `'undefined'`，即使 `window.StatusBar` 存在
- **修复**：添加 `import` 后，将所有 `typeof StatusBar !== 'undefined'` 替换为直接使用 `StatusBar`

### 9. 修复场景五卡墙/怪物刷在墙里
- **根因**：`scene-manager.js` 中玩家和战术小队直接传送到固定位置，没有检查墙壁
- **修复**：`_loadScene5` 中使用 `WallSystem.canMoveTo` 检测，最多 50 次/20 次尝试重新选择安全位置

### 10. 新增 6 个 AI 子系统
- **CombatSystem**：攻击执行、冷却更新、武器动画、状态效果
- **PerceptionSystem**：目标选择、视线检测、共享视野（30秒过期）
- **DecisionSystem**：FSM 状态切换、阶段转换
- **MovementSystem**：寻路、移动、冲刺、Dash 眩晕
- **FormationSystem**：阵型编队（楔形/环形），阵型目标位置
- **SpatialPartitionSystem**：空间分区优化碰撞检测

## 学到的关键教训

### 教训 1：ES 模块 ≠ 全局变量
在 ES 模块中，`typeof` 对未导入的变量永远返回 `'undefined'`，即使 `window.StatusBar` 已被挂载。必须使用 `import` 引入变量，或在运行时通过 `window.StatusBar` 访问。

### 教训 2：构造函数参数顺序是致命的
`new Projectile(...)` 的 `noRender` 参数是第16位，如果误传 `'physical'`（字符串 truthy），会导致整个弹丸不渲染。使用具名参数或 ES 对象解构可以避免此类错误。

### 教训 3：类继承时数据字段合并
当子类需要覆盖父类的 `this.data` 时，应使用 `Object.assign(this.data, {...})` 而非 `this.data = { ... }`，否则父类已初始化的字段（如 `stamina`, `name`, `level`）会被完全清空。

### 教训 4：instanceof 在 ES 模块中依赖导入
`e instanceof Enemy` 在 renderer.js 中永远为 `false`，因为 `Enemy` 类没有被 `import` 进该模块。应使用 `e._faction === 'enemy'` 等属性检查。

### 教训 5：Map 不是 Array
`Array.isArray(Game.entities)` 对 `Map` 永远返回 `false`。`Map.prototype.forEach` 是原生支持的方法，不需要转换为数组。

## 已知问题
1. 战术小队偶尔被复杂地形卡住，需进一步优化寻路
2. 无人机 debuff 在指挥官死亡后不会自动清除
3. 指挥官红色范围圈在指挥官死亡后仍显示（应隐藏）

## 备份路径
`C:\Users\allan\Documents\kimi\workspace\game-dev\backup\v2026-07-03_22-24-01\`

## Git 提交
- 提交：`b94c155` feat(v0.196): 战术小队武器系统 + 弹道渲染 + 无人机状态栏
- 标签：`v0.196`
