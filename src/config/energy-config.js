/**
 * 世界-122 能源系统配置（纯数据，无 DOM/Phaser 依赖，可 node 单测）。
 */
export const ENERGY_CONFIG = {
    gatherRatio: 0.5,          // 每次攻击按造成伤害的 50% 产出能源
    respawnMs: 90000,          // 资源点耗尽后刷新时间
    nodeSize: 84,              // 显示尺寸基准（正方形；2026-08-16 由 56 放大 50%）
    nodeRadius: 45,            // 碰撞半径（受击判定；2026-08-16 由 30 放大 50%）
    storage: { min: 2000, max: 4000 }, // 单点储量（= hp）
    // 玩家基地核心周边禁矿带（2026-08-16）：半径内不生成任何能源矿
    baseExclusion: { x: 532, y: 2048, radius: 800 },
    // 大能源点（2026-08-16）：簇状集中生成，每簇 10~20 块，玩家可集中采集。
    // 簇心避开基地菱形房（x 388..1412, y 1792..2304）、玩家出生点与右端刷怪带（x>5600）；
    // 树木散布按「簇心 ±(spread+140)」整圈排除，防止树压到矿点。
    // 2026-08-16 调簇位：原 (2000,1300) 距基地/玩家仅 ~1250px，落在常见建屋区
    // （小屋门口出现既有矿点的观感来源），东移至 (3000,1500)（距基地中心 ~2170px、
    // 距最近簇 (3600,900) ~850px，仍在中东部采集区且不与右端刷怪带重叠）。
    clusters: [
        { x: 3000, y: 1500, count: 14, spread: 320 },
        { x: 3600, y: 900, count: 14, spread: 320 },
        { x: 3600, y: 3200, count: 14, spread: 320 },
        { x: 4900, y: 2048, count: 12, spread: 300 },
    ],
};
