# 地牢独立路线恢复与控制台候选（2026-08-30）

> 本文记录历史过渡。当前为exploration v10，用户反馈基本达到预期；未选第1/3版已清理，第二版的必要编辑祖先保留。详见dungeon-exploration-console-v9.md。

## 已执行的显示恢复

- 对照Git `79d1e0a`中的独立路线实现、`dungeon-horror-exploration-ui-upgrade-2026-08-29.md`的回退段落和旧母图生成记录，恢复贴图规划之前的40/60显示方式。
- 默认`mapLayout`改为`legacy`；恐怖与矿洞六档的`mapPresentation`改为`legacy`，退出地标投影和最近的组合版布局。
- 恐怖三级恢复`dungeon-map-bg-horror-cold-steel-v2.png`及原40/60全底图分支；矿洞三级恢复`dungeon-map-bg.png`及原上区contain分支。其它地牢恢复各自原独立路线分支。
- 不重写整个历史文件：保留最近并行的战斗加载、事件、输入、奖励、存档及其它功能修改。现有旧分支直接消费生成器节点坐标，不再执行背景道路投影、线束布线、稳定换序或36/64组合布局。
- 旧母图、通道模板和组合版源文件暂留为历史资源；它们不再作为默认路线显示。保留已有分区导航，不回滚地牢玩法或整仓库到旧提交。

## 三张候选的共同范围

仅生成下方控制台的设计候选，使用旧冷钢母图的上方环境作为共同参照，不替换任何正式图片或接入新控制台代码。三张图包含用于表达交互的示例节点/文案，不能当作运行时截图。

- 大地图优先：主要空间留给路线，轻量底部导航与悬停档案。
- 地图与档案并列：路线和所选节点的风险/收益信息同时可见。
- 当前选择优先：突出当前位置与四个可选目标，完整路线通过总览进入。

三张均沿用冷钢材质、克制银青高亮和既有节点徽记；保留HUD安全区，不接入识图模型，不新增玩法。用户选择之后才实施其中一版。

## 交付边界

本轮只对相关配置和说明做局部修改，没有整文件checkout、删除背景或回退其它会话的修改。未运行测试或运行时验证，按约定由用户测试。需重点确认下方恢复独立路线、背景不再约束节点、恐怖/矿洞进出与HUD恢复。

## v9 已选并接入（2026-08-30）

用户确认“OK，按这一版先出”后，已将v9作为当前探索台实现目标；早期“仅候选/尚未实施”保留为历史记录。默认mapLayout为exploration，真实路线分列、平行走廊、固定档案、五项收益及入侵/撤离在下方整合，进入按钮居中放大，旧HUD两侧避让保持。恐怖背景和门厅装饰已分别生成无UI独立资产，没有把整张概念图当作运行时界面。

文件、来源、玩法边界、局部回退及用户待测项见`docs/dungeon-exploration-console-v9.md`。未运行测试或运行时验证；参考图不代表实机截图。
