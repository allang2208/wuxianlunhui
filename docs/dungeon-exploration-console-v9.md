# 地牢探索台 v9 接入与 v10 布局收口（2026-08-30）

> 当前版本：v16；回归原冷钢配色与材质，路线优先、辅助分区减重。保留v15整体居中、对称撤离与原HUD；连续房图、固定row布线、拖动上沿及交互不变。历史章节按版本保留，当前外观以下方v16节为准。

## v16：回归主菜单原冷钢材质（2026-08-30）

- 沼泽初级/中级/高级新增同一套10张探索背景，独立配置到各自的`mapExplorationBackgroundVariants`，每局各1/10概率抽取，单局保持，允许新局重复；原`mapBackground`、其他地牢和40%/60%等比裁切布局不变。素材、逐图提示词、尺寸和来源见`tools/ai-gen/_swamp_exploration_banners_20260830/README.md`。03/06/07为9:4，其余为3:1，全部保留原图，较窄比例在宽幅槽中上下裁切更多；未运行测试或运行时验证，未同步EXE。
- 用户验收后推广到其他地牢：全部13档原本已通过默认配置共用探索台，本次为恐怖、冰封、瘴沼的各三档及恶魔洞窟共10档显式配置`mapLayout: exploration`并补齐独立探索横幅池，不复制UI、不修改玩法。统一沿用冷钢主题、40%/60%默认布局、等比居中裁切、路线边缘渐进裁切及拖动边界、左绿撤离/右侧放弃、右边缘原栏目、观察显隐和退出重试；原时钟、经济面板与顶部状态保持原样。

| 系列 | 覆盖档位 | 探索横幅来源与数量 |
| --- | --- | --- |
| 废弃矿洞 | 初级F / 中级E / 高级D | 既有10张独立横幅，本次不改 |
| 恐怖地牢 | 初级F / 中级E / 高级D | 既有纯古堡横幅＋`loading/zombie-dungeon-1～3.png`，共4张 |
| 冰封地牢 | 初级C / 中级B / 高级A | `loading/snowfield-1～2.png`，共2张 |
| 瘴沼地牢 | 初级E / 中级D / 高级C | `loading/swamp-forest-1～6.png`，共6张 |
| 恶魔洞窟 | C级 | 沿用既有纯古堡横幅1张，保持此前古堡题材，未新增专属美术 |

本次复用的加载图均为已有正式场景素材，原主题对应关系见`data/loading-screen-config.json`，不改加载配置、不重新生成图片。探索横幅不再引用带内置黑色留白的旧地图底图；旧`mapBackground`、`mapBackgroundVariants`仍保留给兼容模式。多图池按原机制每局等概率抽一次、局内保持，可以重复；单图池固定显示。此次实现仅修改data/public两份地牢配置，未改节点生成、迷雾、怪物、奖励、解锁或存档。

- 审查后退出修复：安全撤离与确认放弃共用`_returnToMainWithRetry`，沿用原结算/清理顺序，但保留冷钢返回面板直到切场成功；`switchScene`抛错、返回false或未到主城均显示重试。重试仅重新切场，不重复登记本局结果或清空背包；点击期间禁用重复提交，返回面板圈定键盘焦点。安全撤离不增加二次确认，放弃仍保留原确认；加载中、观察中、非地牢地图时不启动退出，玩家缺失不先结算。
- 废弃矿洞三档的正式横幅已由3张扩为10张：`assets/scenes/dungeon-exploration-mine/`。新增锁链牢房、地下墓室、淹水泵房、幽光菌窟、遗忘熔炉、封印圣所、地下湖码头，7次内置image_gen独立生成，均2172×724、原样复制。`mapExplorationBackgroundVariants`与旧地标母图池分开，路线生成后等概率抽取一张（每张1/10，可重复），单局保持不变，退出清理。该批扩图时其他地牢未改；本次全系列配置见上表，旧路线模式和原HUD仍不变。首批来源见`tools/ai-gen/_abandoned_mine_banners_20260830/README.md`，新增来源/提示词见`tools/ai-gen/_dungeon_theme_banners_20260830/README.md`；尚未运行游戏验收或同步EXE。
- 用户确认按风格分析升级，参照现有主菜单与背包主题，不生成另一套素材。探索台直接消费`--bp-ui-shell/control/surface-hover`，外壳圆角复用10px主题值，控件使用5～6px；去掉局部黑白渐变和多层硬边框。危险提示改用原`--bp-notice-danger`，成功绿沿用原主题。
- 背景衔接修正：针对中间大块黑带反馈，将原图与探索台放入同一个`.dxc-stage`上下两行Grid。首行图片`object-fit: cover; object-position: center`等比铺满全宽和全部行高，第二行紧接探索台；不再由底层Canvas读取缓存的`bannerBottom`另算图片高度。按最新要求默认上方背景40vh、下方控制台区域60vh（仍保留16px底部边距），保持既定上下布局；拖动/键盘调整及双击还原均消费同一默认比例；保留原冷钢底色、94%图片不透明度、左右对称边距及16px底部边距。原图保留宽高比，超出横幅的边缘居中裁切，不再压扁建筑，也不新增素材；旧路线模式继续由Canvas绘制。观察其他位面时通过探索台setVisible(false)同步隐藏背景与台面并释放拖动/栏目悬停，返回地牢地图恢复，离场同步销毁；原HUD仍在整个探索层上方，组件与设置不变。
- 路线边缘与拖动修复：节点不再因靠近边框就整块设为visibility:hidden；按钮、编号和名称始终交由同一视窗逐步裁切，允许部分露出。Canvas与节点坐标读取内层实际矩形，视窗使用overflow:clip避免原生焦点滚动造成错位；图标与窗口相交时才进入Tab顺序，完全离开的可走图标仍计入视野外提示。探索态拖动围绕真实房间范围钳制，保留水平60px/垂直56px随节点缩放的屏幕余量（上限各轴视窗1/4）；绕线包围盒与80世界单位余量只留给旧模式/完整路线拟合。触边后按实际钳制位置续拖，反向立即响应，不累计越界距离。原节点拓扑、迷雾、可达与进入规则不变。
- 队伍和档案维持等宽，宽屏侧栏上限从320px收至288px，为路线增加空间；两侧标题使用辅助正文档，队员卡和空槽弱化底色，档案原图标缩至64px。收益只保留一道弱分隔线，明细仍可展开，未隐藏或删除实际信息。
- 路线工具/下拉框最低38px，文字恢复14px正文；节点仍48px，原坐标/缩放/点击区域不变，编号与节点减少重复边框。当前/可走/选择继续保留文字、实虚线与明暗区别，可走路线亮于已走路线。
- 底部主进入继续浅银，安全撤离保留绿色且居左，强制放弃居右并改为深灰底/危险色标题；两侧等宽等高，上限260px，主按钮更突出。按钮最小56px，操作带84px（矮窗72px），窄窗换行规则不变；撤离条件和二次确认不变。
- 栏目交互按最新要求改为右边缘悬停：删除右上“菜单”按钮及两列浮层，鼠标进入最右16px后自动显示正常模式原单列栏目；位置、图标尺寸、按钮间距、onclick与快捷键不变。移入栏目和文字周围保持展开，移开180ms收起；拖动路线/分界线时不触发。悬停不抢焦点，只有焦点进入栏目时才接管Tab导航，单纯悬停继续使用原Tab背包快捷键；原栏目`.hidden`仍有效，点击栏目/外部、Escape、窗口失焦或离开窗口均收起，隐藏/销毁清理延时与监听。
- 经济面板、时钟、顶部血魔状态、原图标/头像、数据刷新与玩法均未改动。代码范围仅`ui/panel-theme-backpack.css`既有探索台块、`src/ui/dungeon-exploration-console.js`背景取色/上下布局生命周期/栏目显隐、`src/world/dungeon-map-system.js`探索背景分支与探索态拖动钳制，以及data/public双份地牢背景池。

交付前仅查看本次局部diff和必要坐标/取色接线；未运行测试、构建、浏览器、游戏或截图验证，按约定由用户测试，未同步EXE。重点验收1080p下冷钢风格、四槽可见性、工具换行、可走/已走辨识，原HUD保持情况，以及路线边缘局部可见/触边反拖/缩放、10图新局抽取、右边缘唤出/收起和原栏目操作；本次不声称已完成实机视觉验收。

## v15：已选居中预览落地，原经济面板/时钟不变（2026-08-30）

- 按用户确认的整体居中结构应用到`mapLayout: exploration`，不把后续被否定的HUD改绘或示意数值接入游戏。经济面板与时钟继续显示正常模式同一DOM，代码、尺寸、位置、字体、图标、真实数据、展开逻辑均未重建。
- 探索台左右共用12～24px边距，上沿默认24vh；队伍与档案共用等宽侧栏，中间路线不为常驻右菜单腾位。收益改为全宽；正式背景继续整图展示，允许自然留白，不使用AI重绘背景。
- 原`safeEvacButton`和`abandonButton`直接挂入同一底栏；安全撤离绿色居左，主进入居中，强制放弃居右，两侧等宽等高。主按钮内显示所选房间类型及真实风险提示，编号/目标/进入判定仍同源。安全撤离仅起点可用、离开起点禁用，强制放弃保留原二次确认。
- 原`.side-menu`默认折叠；106px上沿的菜单入口与原HUD分开，展开时在152px下方浮显为两列，图标、onclick及栏目挂载保持原样。点击栏目、点击外部、Escape均收起；补齐临时键盘焦点导航，离开探索态时隐藏入口、收起菜单并恢复原菜单属性，销毁时移除入口和监听。
- 只把探索台层级放到原`#uiLayer`下方，确保原经济展开与侧栏可覆盖台面；不修改原经济/时钟层级或其它设置。代码变更限`src/ui/dungeon-exploration-console.js`、`src/world/dungeon-map-system.js`探索背景尺寸及`ui/panel-theme-backpack.css`既有v9/v10/v11块。
- 1399px以下收紧两侧宽度；1100px以下沿用横向队员条；960px以下进入按钮单独一行，撤离/放弃仍左右排列。极小窗口和极端下拉保留滚动，不承诺所有信息始终无需滚动。

已查看本次局部diff、原按钮接线、菜单生命周期及HUD层级；未运行测试、构建、浏览器、游戏或截图验证，按约定由用户测试。未同步固定EXE。

用户验收重点：1080p四槽和可走分支是否完整、主地图/进入按钮是否居中；原经济与时钟是否保持原样，经济展开是否可读；菜单点击/快捷键/Tab/Escape及战斗/事件返回后的侧栏恢复；安全撤离禁用状态和强制放弃二次确认（不要在重要存档确认放弃）。

## v14：连续房图与通关返回修复（2026-08-30）

- 不再按窗口宽度拆分区段，去掉上一段/下一段和区段卡片；全部真实节点与边始终保留在同图。“完整路线”适配整张路线，“当前位置”恢复可读视角，调整窗口和台面高度保持观察中心。
- 缩放范围10%～160%，低倍率缩小节点并隐藏细字，档案与相邻节点选择框继续提供完整信息。只查看远端节点不改变玩家位置；最终进入入口也检查当前真实邻接关系。
- 补齐通关/退出决策弹窗基础样式，修复隐藏通关弹窗锁住路线交互；奖励结算后返回主神空间，加载失败保留同一弹窗供重试。
- 迷雾、收益、回头路、陷阱跨越、房内战斗阶段与分岔选择不变，未新增全旁支清空门槛。连续房图和退出决策的当前约束见本节及v16；本交付不包含其他任务的怪物修复。

未运行测试或运行时验证，按约定由用户测试；未同步固定EXE。

## 选定来源与范围

用户确认`tools/ai-gen/_dungeon_console_concepts_20260830/02-dark-exploration-hybrid-v9.png`后，将该版接入既有地牢地图系统，不是独立网页或静态截图。借鉴暗黑探索的上方场景、房间走廊、固定节点档案表达；冷钢材质、字体、安全区和玩法仍归本项目。

## 涉及文件

- 新增`src/ui/dungeon-exploration-console.js`：规划台DOM、真实节点按钮、连接Canvas、选中档案、分区总览、平移/缩放/相邻目标选择。Canvas只在视图或状态变化时重画。
- 修改`src/ui/dungeon-expedition-layout.js`：可选`corridors`展示投影，完整图稳定换序、独立平行轨道、按列间负载扩宽列距；同列侧绕、远跨列外绕，背景不参与排布。
- 修改`src/world/dungeon-map-system.js`：挂载台、提供迷雾允许的节点数据、场景绘制与生命周期接线；`isNodeClickable`、`_enterNode`、原撤离和确认流程继续持有玩法。同文件并行战斗/竞技场改动保留，不属于本轮。
- 修改`src/config/dungeon-config.js`默认`mapLayout`为`exploration`；`data/dungeon-config.json`及`public/data/dungeon-config.json`恐怖/矿洞六档同步表现标记，恐怖三档引用新场景与档案图。其它主题换共享布局，保留各自环境图片；旧母图变体、模板、分支和资源不删除。
- 修改`ui/panel-theme-backpack.css`：新增探索台作用域样式，不缩放/重新布局原血魔条、组队与右侧入口；炭黑渐变、轻毛玻璃、雾蓝与青色高光。中央大进入按钮、右侧撤离，窄窗内部滚动。
- 同步`SKILL.md`、`skill/10-ui-party.md`、冷钢规范、候选状态和`CHANGELOG.md`。

## 独立美术资产

- `assets/scenes/dungeon-exploration-horror-v9.png`，2172×724：纯古堡横幅，左骷髅烛光、中央门阶、右渡鸦。
- `assets/ui/dungeon-map/exploration-doorway-v9.png`，2048×768：无类型暗示的门厅装饰，不代表真实房间情报。
- 两图由builtin image_gen以已选v9为参照分别生成，原始源图、完整prompt及尺寸记录在候选目录的`exploration-horror-v9-asset.json`与`exploration-doorway-v9-asset.json`。已查看素材，没有运行时截图比对。
- 节点、金币与收益小图复用已有正式资源；未引入识图服务、字体库或图标依赖。

## 不变的玩法与新交互

- 点击房间只选中，中央“进入节点”执行前重查真实邻接；不可进入的房间可以查看已知档案，但主按钮禁用。相邻目标选择框只定位，不自动进入。
- 已揭示类型才显示图标与说明；未知事件风险为“未确认”，不虚构收益、检定或房内敌人。预计经验使用当前连战倍率，奖励条是规则预览而非已获得。
- 安全撤离仅起点可用，保留全部背包；其它位置按钮禁用并提示返回起点。强制放弃沿用原二次确认并丢失背包全部物品。
- 入侵标签、追击标记及区段位置只读原AgentInvasionSystem；判定、追击、消费与击败后移除不变。
- 节点/边/迷雾/事件/战斗/奖励/存档未因参考图固定化。不新增火把、压力或补给。
- 观察其它位面或进入战斗/事件时隐藏规划台，返回重置选择到当前位置，关闭清理DOM、ResizeObserver和指针捕获。原生按钮支持键盘焦点，Tab/Enter/空格/方向键不穿透游戏；鼠标松开仍交给全局清理。

## 回退边界

阶段收口后已移除临时before整文件副本，回退依据本任务Git提交逐段处理。公共文件仍有并行改动，禁止整文件覆盖恢复。可显式切回legacy兼容分支；移除引用后再撤销本任务新增UI模块和两张正式图，不删除旧正式资源或他人功能。

公共主题曾发生并行重写。正式CSS中的BEGIN/END dungeon-exploration-v9及v10为本任务作用域；提交合并保留远端其它样式。重复片段副本已清理，后续按Git差异定向合并。

## 交付与待测

仅查看本轮真实差异及必要生命周期、输入、迷雾、奖励和撤离契约。未运行测试、lint、类型/语法检查、构建、开发服务器、浏览器、游戏或运行时截图验证，按约定由用户测试。没有可用于宣称像素一致或性能通过的游戏截图。

重点查看恐怖/矿洞进出、四条以上路线与同列岔路、拖动不误触、跨区/回头路、入侵标记、起点撤离/放弃取消、事件/战斗返回及观察其它位面后恢复。小窗口可能需要台内滚动；长边交叉采用留隙表示跨越，不代表图论上完全无交叉，实机可读性与毛玻璃性能尚未验证。

## v10：针对实机反馈重新布局（2026-08-30，布线继续沿用）

用户反馈线路仍叠绕、上方背景显示不全、底部操作栏挤占路线空间。本节覆盖前文v9中逐列换序、扩列距侧轨及大按钮尺寸描述。

- 根因：ZombieDungeonMapGenerator原本生成横向同row通道和纵向连接，v9却逐列居中/重排，并让所有竖线绕行，破坏原平面网格。现在按全图row对齐196px列距/112px行距，缺行留空，四条以上主通道各守一行；正常横竖边直接连接，只有被其它节点阻挡时走避让轨道。仍对双向边去重，不修改生成器或真实可达关系。
- 背景按规划台实际顶部计算可用高度，等比contain完整显示；取消底部淡出，比例不符时保留黑边而非裁掉城堡和阶梯。没有重新生成或修改PNG。
- 进入按钮最小高度76→46px，底栏最小高度138→78px，右侧安全撤离/强制放弃并排；台面底边20→10px，标题/收益栏收紧。矮窗进入按钮最小42px；窄窗继续在台内滚动，不侵占两侧HUD。
- 修改四个实现文件：src/ui/dungeon-expedition-layout.js、src/ui/dungeon-exploration-console.js、src/world/dungeon-map-system.js、ui/panel-theme-backpack.css。前三者分别负责固定网格、线路层级/背景空间测量、contain绘制；CSS只新增BEGIN/END dungeon-exploration-v10局部覆盖。
- 临时before文件和独立CSS片段已在收口时清理；回退依据本任务Git提交，只撤销相关代码块，不覆盖并行文件。

已查看本轮实际差异及必要生成器契约；未运行测试、构建、lint或运行时验证，按约定由用户测试。重点查看多行主线、缺行、宝箱岔路、回头通行、背景完整性、窄窗底栏与进出恢复。特殊跨列连接不承诺绝对无交叉；完整背景可能留下黑边，远处岔路仍需拖动查看。

## 阶段结束与仓库整理

用户确认“基本达到预期”。第1/3版未选PNG与两个实施目录的临时文件已列入清理清单；获准第二版到v9的实际编辑链、完整提示词、最终概念、两张运行时PNG保留。来源目录README与cleanup-manifest.json区分已清理文件和必要祖先。

提交仅收录路线表现功能，远端已经存在的旧地图/模板保留；同文件的并行加载、竞技场、事件及通知更改没有混入。未运行测试或运行时验证，按约定由用户测试。

## v11：队伍融入台面与可调整背景（2026-08-30）

- 台面左右均留116px，左列纳入玩家与最多3名队友状态卡，路线/档案/收益位于右侧，底部操作栏贯穿整台；宽屏进入按钮与屏幕中心对齐。既有头像占位框从30px放大至64px，显示等级、身份、血魔完整数值与条形、受伤/危急/倒下提示；未制作新人物肖像。
- 卡片复用PartySystem选择与招募规则，只读实体data。成员变更即时同步，探索帧循环每250ms补刷数值，总览也更新，不增加定时器、不改变血魔或队伍容量。旧partyBar只在探索界面隐藏，进入战斗/事件或切换观察视图由既有显隐出口恢复；关闭取消成员订阅和拖动捕获。
- 根布局默认上沿34vh，支持鼠标上下拖动、上下键12px微调、Home/End和双击还原；高度上限同时限制为60vh及保留360px台面空间。背景依台面实测顶部完整绘制，纵向压缩最多15%/拉伸最多20%，宽度至多视口80%且避开左右116px，底部贴台面、左右留白。不改PNG，不遮阶梯；留白无独立交互层。调整仅保留于当前台面实例，不写入存档。
- 小于1100px把队员列转成横向滚动卡列；较矮窗口队员列和台面分别滚动。右侧入口和原顶部血魔条尺寸位置不变；路线拓扑、48px节点、迷雾、通行、收益、入侵和撤离确认均未调整。
- 实现仅修改`src/ui/dungeon-exploration-console.js`、`src/world/dungeon-map-system.js`及主题CSS，另同步索引、第10卷、设计规范和变更日志。修改前的三个实现文件保存在系统临时目录`codex-dungeon-console-20260830-v11/`，仅供逐段比对，不入Git；回退时移除CSS的v11块、控制台新增队员/上沿代码及地图背景/显隐两处接线，不整文件覆盖共享工作区。

已查看本轮真实diff与所需局部生命周期。未运行测试或运行时验证，按约定由用户测试；重点看满员/空位、受伤后返回、Shift选择、招募入口、背景上沿拖动与双击、总览拖边、窄窗滚动和进出战斗后原组队栏恢复。有限纵向变形及窄窗空间取舍尚待实际观感验收。

## v12：默认横幅、1080P四人状态与背包同系外观（2026-08-30）

- 保留用户认可的拖动上沿，不生成或替换图片。默认上沿从34vh改为`min(42vh, (视口宽−232)/4+8px)`并沿用高度边界；1920×1080默认430px，既有3:1古堡图片按绘制公式约1583×422px显示，较上一版约1268×359px扩大。整图保留，左右留白，纵向最多压缩/拉伸20%，双击回到新的默认高度。
- 四个位置仍为主角＋三名侍从，不扩充队伍。卡片最小88px、间距6px，头像64px，姓名/等级、当前状态与两条血魔统一放在右侧；身份说明保留悬停/无障碍文本，底部操作提示缩成一行。1080P默认尺寸预算容纳四张卡，不通过隐藏滚动条或裁切成员实现；大幅下拉压缩台面与较矮窗口仍保留滚动降级。
- 外壳改为半透明黑底、灰白反光渐变与毛玻璃；主操作复用背包浅银渐变/深色文字，其余按钮和队员卡用中性深灰。路线/选中/焦点改银灰白，去除蓝青光晕；生命/魔法与危险提示颜色保留。颜色变量仍在主题CSS，Canvas读取同一主题值，不对整台做灰度滤镜。
- 修改前三个实现文件的局部内容，并同步索引、规范和日志；修改前文件保存在系统临时目录`codex-dungeon-console-20260830-v12/`仅供逐段回退，不入Git、不整文件恢复并行工作。没有新增CSS版本覆盖块，直接维护既有探索台作用域。

未运行测试或运行时验证，按约定由用户测试。上述像素尺寸来自布局公式，并非实机测量；重点看1080P四卡状态、背景比例、默认/悬停/禁用按钮对比度及拖动后可读性。

## v13：审计后的交互与信息层级收口（2026-08-30）

- 拉伸台面或展开收益时，保留缩放和观察中心，仅按地图边界钳制；取消ResizeObserver中回到角色节点的强制聚焦。横向缩放窗口导致重新分段时，以原区段最接近观察中心的房间更新归属，不强制回到主角。
- 区段按钮复用DOM，更新探索/当前位置/入侵者标记，不因刷新丢焦点；从总览选区段后，焦点转到对应导航按钮。
- 招募窗口标记为对话框，打开后聚焦关闭按钮，Tab/Shift+Tab在窗口内循环，Escape及原关闭方式退出后恢复入口焦点。探索台暂停接收操作；保留释放事件清除已有输入状态。该焦点行为同时适用于原队员面板打开的招募窗口，不改招募规则或容量。
- 血魔改为数字下方的长条；路线工具分组并显示缩放百分比和滚轮提示。收益默认折叠，摘要读取原奖励面板的首领/通关金币，原五项详情可展开，不把预览当已获奖励。
- 本轮修改探索台、招募UI、地图系统的奖励展示接线和既有探索台CSS块；背景素材、默认横幅、配色和玩法保持。同步SKILL索引、第10卷、设计规范与CHANGELOG。实现文件修改前备份位于系统临时目录`codex-dungeon-console-20260830-v13/`，仅供逐段比对，不整文件回退共享修改。

已查看本轮真实diff及必要局部调用链；未运行测试或运行时验证，按约定由用户测试。重点为1080P满员状态、远端路线拉伸/窗口宽度变化、总览键盘切换、招募窗口Tab/Escape与焦点返回、收益展开收起。极端压缩台面仍允许滚动；地图内容小于视口时仍按原规则居中。

## v14：1080p截图驱动的展示结构优化（2026-08-30）

用户提供1080p屏幕下的1912×948截图，确认上方留白、第四队伍槽与下方可走分支裁切，以及档案装饰重复。原图、逐步发现、修改文件、回退记录和验收限制见[本轮审计报告](audits/dungeon-route-1080p-2026-08-30/audit.md)。

- 默认上沿为`min(30vh,(视口宽-232px)/5.6+8px)`，保留手动拖动；按公式1920×1080约309px、1912×948约284px。队员卡最小96px，以增加台面高度容纳四槽，不裁切成员或改血魔。
- 默认与“当前位置”聚焦当前节点及真实可走邻居，手动恢复80%～100%可读视角，战斗返回保留缩放；仍用连续全图，前述历史分段/区段说明不适用于当前探索模式。视野外可走数量显式提示，原列表可定位房间。
- 档案去掉重复背景缩略图；地图、列表、档案、主按钮共用编号，选中后显示具体进入目标，未知房间不额外揭示情报。配色、字号与拖动手型消费既有冷钢主题，未生成或删除素材。
- 仅修改探索台、地图展示与原CSS块，保留所有生成、可达、奖励、撤离、存档以及顶部/右侧HUD契约。已看本轮真实diff；未运行测试或运行时验证，按约定由用户测试，未同步固定EXE。尺寸为设计预算，不是优化后实机通过证明。
