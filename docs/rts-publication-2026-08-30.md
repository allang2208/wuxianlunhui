# RTS本任务归档与发布范围（2026-08-30）

本轮从远端`13159caf`建立独立分支`codex/rts-safe-delivery-20260830`，不在旧历史、含并行改动的共享main上提交或推送。

## 分支发布与主线边界

本轮只推送独立任务分支，不更新远端main。推送前主线新增了三个提交；隔离分支rebase被自动审批以潜在误删风险拦截，未执行，也不绕过。按共同基点比较，本任务三个提交仅涉及18个新增/修改文件，无删除项；主线后新增的EXE/沼泽资产不属于本任务修改。主线整合另行处理，当前隔离工作区保留供审阅。

## 本次可独立发布

- `f13b7f50`：三兵种普攻遮挡。盾卫、民兵、战士各三处普攻判定共用承载面和墙体检查；不包含兵种AOE、跟随/离梯或驻守接线。
- `79dbd1eb`：方阵投影、紧凑间距和精确到位。地面槽位8px额外间隔、4px到达容差；保留远端原中继寻路，只补编队末段的合法短线段接近，不引入完整导航新协议。
- 第5/7/8卷记录普攻遮挡、方阵投影/到位及导航设计约束；导航计划作为明确标注的开发记录归档。

## 仍留在共享开发区的实现

完整成本场与多层借道、真实可达成本选梯、搜索状态/预算/生命周期、超时重新选梯、换层追击等，没有包含在上述运行时提交内。

发布阻断证据：基线`13159caf`没有`src/ai/elevated-garrison.js`；本地`rts-command-utils.js`、`movement-system.js`和玩家控制器直接导入它。本地路径/移动接线还使用远端缺失的`WallSystem.getEntityMoveOffset/resolveEntityMove`。`defense-system.js`同时混有其他会话的墙塔、视觉和公共接线变更。依照`skill/15-git-workflow.md`第15.3/15.7节，不整文件提交、不创建空壳依赖、不回退或打包他人代码。后续整合入口已放入TODO。

两份导航计划中的“实现”均描述共享开发区，不能据此认定远端已发布；完整开发代码原样保留。此次没有重写本地旧main历史、强推、移动存档或更新EXE。

## 清理与验收边界

本次未生成美术候选或废案；本任务的`.rts-repair.tmp/.bak`无残留。保留两份导航计划作为有用设计/人工验收记录，不删除其他会话文件。隔离工作区属于待合入的正式交付分支，保留；确认合入且无未提交修改后再移除，不视为废案。

已查看本任务实际diff、暂存内容与必要接口依赖；未运行测试或运行时验证，按约定由用户测试。优先观察三兵种隔墙/墙顶普攻、玩家与不同体型混编到位、穿门/下梯后成阵及巡逻恢复。
