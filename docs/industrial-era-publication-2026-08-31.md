# 近代时代归档与发布边界（2026-08-31）

本次用户要求：各系列近代兵种作为下一步待办；整理本任务废案，沉淀对应SKILL，并允许推送至 `https://github.com/allang2208/wuxianlunhui.git`。

## 本次可独立发布的内容

- 七栋已采用建筑的可编辑模型、Depth、模型预览、定稿透明PNG、选中12/48步原图、直接编辑祖先、提示词和制作记录；活动清单为 `tools/ai-gen/_industrial_transition_collection_20260831/archive.json`。归档源209个文件，约57.28 MiB，未改任何素材像素。
- `TODO.md` 将9种近代兵种按侦察营、军营、靶场、骑兵学院、工程营地分为5批，只列计划，未生成或注册新单位。
- `skill/02-ai-asset-pipeline.md` 沉淀直接编辑祖先、局部Alpha/RGB、减影与素材发布边界；第07卷沉淀时代ID、视觉与完整编制分离、成本百分比及旧档合同；第10卷沉淀真实透明通道、六边框安全区和时代标记核对。主SKILL索引同步。
- `cleanup.json` 记录48个可恢复移出文件及原因，合计约57.10 MiB：33张未选兄弟raw、13张旧批次对照图、1项可再生备份/字节码、1份已过时的一次性科技修改脚本。恢复目录为本地忽略路径 `tools/.trash-industrial-era-20260831/`，不提交Git。

## 未随本次发布的运行时接线

准备时远端main为 `5e25bfe486581cabe3679b83d6869865e31597f6`，科技数据v44；共享工作区有大量并行修改，其本地v63接线依赖尚未合入的市政厅和工程营地基础功能。按第15卷的独立工作树、精确归属和未合入依赖停止条件，本次不复制整份共享配置、不提交这些未闭合功能。

本地 `data/technology-tree.json`、`data/producer-buildings.json`、`src/world/technology-system.js`、运行时近代贴图/光照和相关资源索引保持原样；“已在本地接入”不等于“已推送功能”。`docs/industrial-era-technology-2026-08-31.md` 记录该本地方案与数值，但不作为本次远端已上线的声明。

归档七栋建筑源码是独立美术交付，不向远端 `assets/terrain/` 塞入暂不消费的半套接线，也不引入其他会话的共享协议。后续相关基础合入后，再单独迁移v63；不得直接重跑旧的一次性修改器。

## 图标与来源边界

七项科技徽章原图已生成，但实际为1254×1254 RGB，黑底/棋盘格并非透明通道，透明化和 `iconPath` 接入尚未执行。七张当前原图及靶场直接编辑祖先继续保存在本地 `tools/ai-gen/_industrial_technology_icons_20260831/`，不作为已验收正式PNG提交，也不当废案删除。生成图标不等于开发近代兵种。

旧现代图标仍用原三晶标记，后续应随IV级显示统一处理；这一项与透明化一起列入TODO。没有覆盖旧图标、改科技数值或启用占位兵种。

## 重建与验收边界

`archive.json` 是本批有效来源的唯一列表。可直接打开保留的Blender模型继续编辑，或从保留的选中raw和记录参数继续制作；需要生成器时另按当前项目管线办理，不使用历史生成manifest中的整批候选列表作为现存文件清单。依赖其他未合入源系列的旧模型脚本只留在本地，不伪装成独立可运行的发布入口。

当前本地安装脚本仍只消费已保留的选定PNG。阶段JSON中的未接入状态与已清理候选路径是历史事实，不能覆盖归档/发布状态；它们保留的是提示词、参数和生成沿革。

本轮只做范围、来源、真实提交差异及远端引用核对。未运行测试、lint、类型检查、构建、浏览器、游戏或运行时验证，按约定由用户测试；未同步EXE。共享工作区、已有暂存内容及其他会话改动均不纳入本次提交。
