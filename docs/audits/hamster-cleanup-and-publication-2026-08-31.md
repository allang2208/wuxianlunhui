# 仓鼠审计资料整理与Git发布范围

日期：2026-08-31。只处理本任务产生的审计、攻击距离和节奏资料，不清理其他会话素材或共享代码。

## 清理结果

- 删除19个本任务冗余文件，共3,186,637字节（约3.04MiB）：10张修改前浏览/联系图、6张修改后逐帧浏览图、2份已并入报告的表格，以及1份完整的修改前配置副本。
- 完整配置副本提炼为16,358字节的`source-frame-inputs.json`，仅保留15种近战单位前后标定所需的缩放、脚点、接触帧、距离与图片布局。逐字段参数变化仍保留在`parameter-delta.json`，没有丢失本轮调参依据。
- 保留报告引用的4张最终源帧图、前后透明轮廓参考数据、两份审计报告和离线抽帧工具。抽帧工具消费冻结输入，防止之后的配置变化改写历史证据；可再生浏览图加入本目录`.gitignore`。
- 前一轮已删除3份临时编辑/报告脚本和10张过时根目录预览，本次不重复计算。未合入代码所需的Temp原始差异备份暂保留，用于后续安全拆分；它们不进入Git。
- 本次没有删除正式动画、音效、兵种配置或其他任务目录。精确清单见[cleanup.json](hamster-attack-tempo-2026-08-31/cleanup.json)。

## 推送边界与依据

已查阅`skill/15-git-workflow.md`的并行隔离、归属、资产、提交和停止条件，以及第02卷最小来源归档规则。共享main仍有大量并行修改，因此从远端main建立`codex/hamster-audit-archive-20260831`隔离worktree，不在共享工作区暂存、切分支或回退文件。

检查远端main基线`28e24a72`时，确认以下前置内容尚未合入：

- `data/hamster-catapult-crew-config.json`、`data/hamster-field-cannon-crew-config.json`、`data/hamster-howitzer-crew-config.json`及其炮组基础实现；本次审计是在这些工作区单位已存在的基础上修复。
- `src/combat/melee-surface.js`的远端版本只有`canMeleeShareSurface`，尚无工作区使用的`canMeleeReachTarget`；相关共享近战/承载面和墙体接线不能随意混入仓鼠归档提交。
- `Companion`、`GameScene`、生产系统和后台模拟还包含其他会话的共享修改，不能整文件复制或用`git add -A`夹带提交。

因此遵守第15卷15.7：**本次只提交清理记录、审计报告、必要证据、离线资料工具和对应SKILL条目，不提交仓鼠运行时代码、31份兵种配置或其他会话的前置实现。**这不是运行时功能发布；本地仓鼠修改保持完整，未撤销。独立EXE发布任务曾复制的本地快照不等同于Git主线已经合入。

后续集成顺序：先由对应任务合入炮组基础单位/素材登记及公共战斗协议，再从新main精确迁移本次仓鼠实体/AI、渲染、后台估算和双份配置；同时核对派生单位与动作时钟，不能只推配置或只推新helper。若仍有未知重叠，继续保留工作区而非猜测归属。

## 知识沉淀

- 第05卷：起手/命中几何分离、锁定目标方向、同层/墙体复查、源帧姿态标定，以及高速弹按接触先后处理。
- 第09卷：真实继承链、统一动作时钟、首帧零起点、旧回调隔离、升级攻速与整段动画同步、控制和在途弹分离、派生身份、死亡配置和前后台节奏。
- 主`SKILL.md`只补导航，不堆叠过程记录；历史首航的固定帧率不作为当前参数真源。

## 检查状态

执行了Git历史/依赖/本次提交差异核对和精确清理；没有运行游戏测试、lint、类型检查、构建或运行时验证，按约定由用户测试。报告中的时间来自配置推导，源帧图来自离线PNG读取，不是实机通过证明。
