# 熊德鲁伊、红狼、僵尸工头反馈处理（2026-08-31）

本轮只处理这三只怪物。检查方式为相关代码、配置、Git 历史及用户所问的源视频/透明帧/精灵表人工查看；未启动游戏、运行测试、构建、浏览器探针或其他运行时验证。共享工作区其他会话的改动保留。

## 5. 熊德鲁伊

内部 ID 仍为 `blackBear`。代码中已有初始德鲁伊、半血变熊两阶段，不是需要新增的空白怪物。

| 接入环节 | 本轮静态结果与处理 |
| --- | --- |
| 配置、实体、工厂 | `enemy-config.json → BlackBearEnemy → createBlackBear → ZOMBIE_FACTORY_MAP.blackBear` 已接线；T 面板使用同一工厂。未改名称、ID 或另造替代实体。 |
| 副本来源 | 沼泽怪物池已有 `blackBear`；保留原池和领主身份，不把它强行加入其他副本。此项不等于已经观察到实际刷出。 |
| 资源加载 | 熊形四表、德鲁伊五表、变身两表共 11 张 PNG 均有源文件和 Boot 注册。动态资源管理器按配置递归收集两个目录的资源族，未发现单纯漏注册。 |
| 大图兼容 | 熊形攻击表原为 6912×2048，超过项目默认 4096 单边约束。将 21 个 1152×512 原帧由 6 列重排为 3 列，成品 3456×3584；不缩放、不重绘、不丢帧、不改变攻击轨迹。同步运行时配置、生产脚本和生成成品/清单。 |
| 施法动画 | 原来施法伤害使用 `_castTimer`，贴图另走 Phaser 动画。现让攻击选帧读取同一逻辑时钟，保留冰锥→闪电→火球、原释放比例、伤害和冷却。 |
| 变身动画 | `toBear` 改用变身逻辑倒计时选帧，避免动画重播或暂停后与形态切换不同步；保留半血阈值、2 秒变身、减伤及一次性强化/回血规则。修正 Boot 中颠倒的过渡用途注释。 |
| 控制中断 | 接入公共控制判断与中断钩子，作废尚未释放的自管法术；石化保留已显示姿势，不再遗漏石化限制。 |
| 脚点/冻结姿势 | 德鲁伊脚点改用自身 167.3 尺寸，不再继承熊形 180 尺寸计算；状态切换前同步脚点。石化锚点按实际冻结的德鲁伊/熊/变身纹理取布局，不再沿用僵尸犬键前缀。 |
| 死亡链 | 两形态分别选择对应死亡表，仍走原保尸/清理链，未改死亡时长或掉落。 |
| T 面板报错 | 原来资源加载、实体创建、预览初始化都报告“资源未就绪”。现分阶段显示具体失败原因，避免把构造异常误报成缺图。 |

**限制：尚未取得用户当前失败时的完整报错，也未获运行测试授权。以上修复不能表述为已经实测熊德鲁伊成功入场；6912px 图集是明确的兼容风险，但尚不能确认就是当前设备失败的唯一原因。**

用户重点确认：T 面板可显示/生成初始德鲁伊；三系法术可释放；半血后完整变熊并近战；两形态死亡及施法中石化/解除表现。若仍失败，保留新提示中“资源加载/实体创建/预览初始化”阶段及完整错误文本。

## 6. 红狼咬合

只修改狼形撕咬，不修改黑狼、狼人形态或扑击。现有 21 帧顺序与像素完全保留。

| 0-based 帧段 | 新时长 | 用途 |
| --- | --- | --- |
| 0–4 | 每帧 60ms，共 300ms | 蓄力，接近原速度 |
| 5–17 | 每帧 30ms，共 390ms | 张嘴、前扑、咬合加速，减少张嘴停留 |
| 18–20 | 每帧 70ms，共 210ms | 闭嘴收招 |

整段由 1200ms 改为 900ms。命中窗口对齐第 17–18 帧（660–760ms），选帧和命中共用变时长表，仍对原锁定目标至多结算一次；伤害、距离、攻击冷却不变。狼人继续使用原均匀帧时钟及 42%–75% 命中窗口。

帧段判断依据：[现有攻击帧总览](../tools/ai-gen/_monster_feedback_20260831/red-wolf-bite-frames.png)。未以离线总览冒充实机冲击感验收。

## 7. 僵尸工头

### 移动回退

恢复 `1e3ca4d1^:assets/enemies/foreman_zombie/walking.png` 的旧 15 帧动画（后续 H3 20 帧版本的直接前版），不是另抽一条视频或回退整个怪物类。正式表为 4096×2048、每格 512×512、有效帧 0–14，以 1500ms 循环（10fps）。Boot 从配置取帧数/时长；攻击、待机、号召、死亡、碰撞和矿洞逻辑保留。

[旧 15 帧与被替换 20 帧对照](../tools/ai-gen/_monster_feedback_20260831/foreman-walk-versions.png)。被替换表另存 `tools/ai-gen/_monster_feedback_20260831/foreman-walking-h3-20-before-rollback.png`，保留追溯来源。

### 鞭子诊断

| 环节 | 证据与结论 |
| --- | --- |
| 原视频 | 所选 `tools/ai-gen/_foreman_whip_doubao_20260831/videos/whip-v04.mp4` 中有鞭子，并非源视频没有画。见 [源视频接触表](../tools/ai-gen/_foreman_whip_doubao_20260831/previews/whip-v04-source-contact.png)。 |
| 透明抠图 | `source-inputs/whip-v04-optimized/0047.png` 仍有完整鞭身；不是这一阶段把鞭子全部抠掉。 |
| 制作脚本 | `build-hybrid.py` 的 `actor_only()` 以人体最大连通域和形态学掩模剔除细长鞭身，另有脚旁绳段删除。`selection.json` 明确记载“仅选身体，原鞭因过长和拖影被弃用”。 |
| 正式攻击表 | `attacking_doubao_body.png` 是人体层，原视频鞭子没有进入该表。 |
| 当前游戏表现 | 另由 `ForemanWhipVisuals` 按程序曲线绘制；这条线不是原视频的离散鞭子轨迹。静态调用可追溯到 GameScene，但是否还叠加实时隐藏/遮挡问题，未运行游戏验证。 |

因此“没有还原原鞭”的已知原因在后续人体/武器拆层制作，不在原视频，也不在最初透明抠图。本轮按请求完成原因检查，**没有擅自重做攻击、把曲线鞭说成原视频还原，或声称已修复所有方向的鞭子可见性**。

## 本轮修改文件

- `src/entities/enemy-types.js`：红狼分段咬合；熊德鲁伊动作时钟、控制中断与脚点。
- `src/phaser/scenes/BootScene.js`：工头旧移动帧数/时长；熊德鲁伊过渡注释。
- `src/ui/collision-editor.js`：区分资源加载、实体创建、预览初始化错误。
- `data/animation-config.json`、`public/data/animation-config.json`：红狼帧时长及咬合窗口。
- `data/enemy-config.json`、`public/data/enemy-config.json`：熊攻击网格与工头旧移动布局。
- `assets/enemies/black_bear/attacking.png`：仅重排原帧；`assets/enemies/foreman_zombie/walking.png`：回退旧表。
- `tools/ai-gen/_black_bear_20260824/build-sheets.py`、`sheet-manifest.json`、`generated/final/attacking.png`：熊攻击生产来源同步。
- 本报告、`CHANGELOG.md` 及本轮来源对照/备份目录。

交付时只查看本次真实 diff 与必要局部契约，未运行测试或运行时验证，按约定由用户测试；未构建、启动、刷新或同步固定 EXE。现有页面已缓存的动画/纹理需用户重新进入开发页面后观察。
