# 人口、幸福度与经济面板整理交付

## 本次Git范围

- 独立修正：经济面板按视口可用高度向下展开，矮窗口详情可滚动；只含两处CSS、对应UI规范与日志。提交 `70c63cff`。
- 独立素材：七张已选幸福度图标，128px正式副本、1254px源图、提示词、生成记录与导出脚本。提交 `32803369`。远端不包含市政厅/房屋幸福度组件接线。
- 知识档案：第07卷的人口/幸福/口粮与收支接口、第10卷的显示/进度/图标/反馈规则、主索引，以及三份本地实现说明与本文。知识记录不代表运行时代码已发布。

## 核心代码暂不提交的依据

2026-08-31发布前已读取第15卷Git工作流、核心范围规则与资产归档边界，并获取指定仓库 `https://github.com/allang2208/wuxianlunhui.git` 的远端主线。核对时主线为 `cee4ec53bcde8055790bb4104952a34ba2b4bf1c`，共享本地main为 `afc8961b8cb5ee9b9fb500638154874954153b19`。

必要调用链存在尚未合入、且不属于本任务的依赖：

- `src/ui/city-hall-detail.js` 及其 `city-hall-policy-plan.js` 尚不在远端树中；本地幸福度接在这一市政厅模块上，不能整文件当成本任务新文件提交。
- 幸福度娱乐采样调用 `TavernEconomySystem.getWorkerOutputFactor()`；远端酒馆尚无该接口，相关岗位产出协议仍在本地并行改动中。
- 净收支周期来源包含 `getGeothermalPowerSnapshot()`，对应地热升级/profile尚未合入；后台生产、建筑与主场景文件也混有其他任务的服务与生命周期调整。

依第15卷§15.3、§15.7，不猜测混合代码归属、不夹带共享协议、不删除这些调用来掩盖依赖。因此人口增长、六项幸福度、3食物口粮、人数红绿反馈及净收支统计核心代码继续完整保留在共享本地工作区，未提交也未推送。本任务不改变这些本地功能。

保留范围包括 `data/population-economy.json`、`data/producer-buildings.json`、新增 `population-growth.js / population-happiness.js / economy-flow-system.js / population-growth-view.js`，以及人口经济、生产建筑、建造、前后台快照/账本、资源管理器、金币路由、HUD、市政厅、游戏时钟及主题中的对应代码块。其他任务的未提交内容和原暂存区保持原样。

## 清理结果

本系列七张幸福度图全部获准并接入本地界面，没有未选候选。源图是128px导出输入，正式副本是交付资产，两者都保留；重新导出只依赖仓库源图，不依赖作者机器的历史生成路径。

此前口粮、面板高度、净收支实现的三个临时对照目录均已删除，本次未发现新增废案。未全仓扫描或删除其他任务的临时文件；不执行 `git clean`、整目录回退或历史改写。

## 后续整合边界

共享市政厅、酒馆岗位/物流、地热经营及相关生命周期协议由其原任务先收口合入后，再按本任务原始代码块建立完整人口经济提交。不得把这里的素材/文档提交解释成核心功能已进入远端，也不得以发布为由回退共享工作区。

本次采用专用 `codex/population-economy-publication-20260831` 分支与稀疏worktree，三个可独立回滚的提交按快进方式推送远端main，不移动脏的本地main、不强推。

未运行测试或运行时验证，按约定由用户测试；仅核对本任务差异、必要依赖及暂存/推送范围，未构建、启动游戏或同步EXE。用户重点验收展开详情底部及矮窗口滚动；人口、幸福度、扣粮和收支功能须在保留实现的本地开发端验收，不能拿当前远端或旧EXE代替。
