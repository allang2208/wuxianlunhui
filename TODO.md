# 项目待办

本文件只记录已经提出、但尚未进入本轮实现范围的未来功能、延后优化和技术债。

- 新待办应写明来源、目标、前置条件和验收口径，避免只留下无法执行的一句话。
- 完成后从本文件移除，并把实际结果写入 `CHANGELOG.md`。
- 已验证的通用规则才沉淀到 `SKILL.md` 对应分卷；SKILL 不作为待办列表。

## 仓库后续独特升级与正式贴图

### LV2—LV5 仓库专属机制

- 来源：2026-08-26 仓库等级科技、容量曲线与独立升级数值调整。
- 当前状态：LV2—LV5 的科技门禁、等级扩建、基础容量和独立贴图键已经接入；立体货架、能源/粮食压缩与跨位面协议仍是每栋仓库独立项目。以下机制只完成设计，尚未写入运行时，不能以占位科技或隐藏按钮假装已开发。
- LV2 `warehouse_firebreak_cells`（防灾隔舱）：只保护仓库结构和灾害损失，不增加容量、不降低资源占用；待世界灾害对建筑/库存有统一损失协议后接入。
- LV3 `warehouse_loading_automation`（自动装卸）：只缩短以本仓为真实起点或终点的道路物流交互时间，不提高生产建筑基础产量，也不允许隔路传送。
- LV4 `warehouse_smart_allocation`（智能配仓）：新入库资源优先选择兼容且剩余物理容量更合适的本位面仓库；不得搬出旧库存制造资源，也不得改写压缩系数。
- LV5 `warehouse_phase_reserve`（相位保全）：仓库被毁时把明确比例的库存尝试转移到本位面其他活动仓库；剩余容量不足的部分按将来统一的建筑毁坏损失协议处理，禁止跨位面复制或与出售退款叠加。
- 前置条件：先分别完成灾害库存损失、道路物流交互时长、入库选仓策略、建筑毁坏库存结算的统一公共协议，再为四项机制确定科技位置、费用、数值和业务门禁。
- 验收口径：四项机制各自只有一个数值真源；前台、后台快照和跨位面边界一致；不改变 LV1—LV5 基础容量，不重置现有 `warehouseModules`，也不产生、复制或静默吞掉库存。

### LV2—LV5 正式仓库贴图替换

- 当前状态：稳定纹理键已预留为 `warehouse_lv2`、`warehouse_lv3`、`warehouse_lv4`、`warehouse_lv5`。LV2“扩建仓库”已采用12步结构 `v03`、48步精修 `v01`和用户处理的RGBA主体正式入库；外围绿幕残留经确定性清理后紧裁为独立`warehouse_lv2.png`，显示尺度、脚点、视觉占地及四张环境光照派生图均已同步。LV3“机械仓库”已完成组件化白模与标准12步三张结构候选，当前推荐`v02`、`v01`可选，等待用户选稿后才进入48步；LV3运行时仍复用LV1占位贴图，LV4—LV5仍未建模。
- 前置条件：每级正式图先完成建模、候选选择和脚点/主体范围确认；不得把未选候选直接放进运行资源。
- 验收口径：替换各级 `assetPath` 后，分别校准 `displayW/displayH/footOffsetY/visualFootprint`；升级只改变视觉与基础容量，不改变标准2×2逻辑占格、碰撞、道路预约或寻路 footprint；前台升级、读档和位面恢复都显示对应等级纹理。

## 怪物与图鉴

### 综合战斗等级 V2：纳入真实输出与机制威胁

- 来源：2026-08-24 怪物图鉴与战斗等级调整。
- 当前状态：V1 已以六维为主体，并加入生命、移速和阶级补偿；`level` 与 `combatLevel` 已彻底分离。
- 未来目标：在怪物攻击配置完成统一口径后，评估加入有效 DPS、实际攻击距离、控制、召唤、阶段转换和范围伤害等贡献；仍只作为横向战力指标，不参与地牢成长、经验结算或存档等级。
- 前置条件：先统一普通攻击、技能冷却、伤害倍率和机制标签的数据结构，避免为不同怪物硬编码评分。
- 验收口径：同配置必得同结果；图鉴与静态平衡报告共用唯一公式；任何怪物数值调整后无需改 UI 代码即可更新战力；不会改变 `enemy-config.json.level` 的既有语义。

### 图鉴战力排序与筛选

- 来源：2026-08-24 综合战斗等级落地后的可用性延伸。
- 未来目标：允许按综合战斗等级升降序查看怪物，并保留当前家族分类。
- 前置条件：先由用户确认排序控件的交互位置和默认顺序。
- 验收口径：排序只影响显示，不修改怪物配置、生成权重或运行时等级。

## 玩家技能与动画

### 推击技能重做：基于双手握点的枪托前顶

- 来源：2026-08-25 推击动画多轮调整后暂时搁置。
- 当前状态：正式战斗逻辑与运行时精灵图保留，技能在双份 `skills.json` 中以 `hidden + disabled` 全链禁用；实验候选、GIF、联系表、RIFE 中间帧与一次性脚本已清理。
- 未来目标：以现有持枪 `handLayer` 的前后手为真源，重建“枪口朝前→调转枪托→枪托前顶→收回→枪口朝前”的关键姿态和显式前/后景深度切层，完成后再恢复技能。
- 前置条件：先选一把参考长枪，逐帧标注两手锦标、贴图 `grip`、枪口/枪托方向和穿越躯干的切层帧；在参考枪首尾循环通过前不扩展到其他枪型，不再用盲目补帧替代关键姿态。
- 验收口径：参考枪的首帧/尾帧与真实待机无跳变，调转、命中和收回全程不脱手，左右镜像与前/后景切层正确；随后逐把枪独立校准，所有双手枪均通过后才移除 `hidden/disabled/shelvedReason`。

## 魔法体系

### 魔法书副手正式接入

- 来源：2026-08-24 用户确定法杖搭配盾牌/魔法书的装备方向。
- 当前状态：副手规则已预留 `spellbook/magic_book` 类型，但尚无正式道具、掉落、图标、属性或专属词条。
- 未来目标：新增独立副手魔法书道具，用支援属性/法术机制强化与盾牌的生存路线形成选择，不复制法杖 `matkFormula`，也不允许魔法书反装主手。
- 前置条件：用户先确定首本魔法书的来源、稀有度、基础属性、是否可强化/改造及视觉素材。
- 验收口径：只进入副手/备用副手；与单手法杖共存、与双手武器互斥；旧档、掉落、商店/奖励、强化和提示走现有装备数据协议；不让两本魔法书或未启用武器组重复叠加专属施法效果。

## 友军单位潜在项目

### 仓鼠特战

- 来源：2026-08-26 仓鼠动力甲母图迭代；潜在项目先命名为“仓鼠机甲”，随后按用户最新要求改名为“仓鼠特战”，并转为现代霰弹枪步兵方向。
- 当前状态：保留早期重装探索图 `assets/concepts/hamster-special-forces/hamster-special-forces-heavy-armor-concept-v01.png`；现代矮宽版母图和豆包四动作源视频已由用户确认。`tools/ai-gen/_hamster_special_forces_20260826/` 已完成 BiRefNet 抠图、主体连通域清理、体高/脚线归一和 RIFE v4.6 分流插帧，正式美术资产为待机 24 帧、奔跑 28 帧、攻击 41 帧、死亡 31 帧；未采用视频和可重建缓存已移出仓库，尚无单位配置、数值、生产来源、科技门禁或运行时注册，不得表述为已接入。
- 未来目标：开发一名现代特战风格的独立仓鼠友军单位；体型必须复用现有仓鼠反载/狙击手的大头、宽躯干、短粗四肢和低重心比例，禁止继续使用细腰长腿的人类体型。
- 前置条件：进入运行时实现前仍需确定战斗定位、生产/解锁来源和霰弹枪数值方案。
- 验收口径：现代装备和仓鼠身份在所有动作中稳定，墨镜、战术背心与霰弹枪不无故形变；正式资产、双份配置、生产入口和运行时动画完整接线后，才能从本待办移除并写入 `CHANGELOG.md`。

### 仓鼠募兵母图批次：未来动画与运行时接入

- 来源：2026-08-26—27 补齐未开发仓鼠募兵单位母图；用户已确认当前八张活动母图全部可用并要求保存为未来计划。
- 当前状态：`tools/ai-gen/_hamster_missing_mothers_20260826/task-index.json` 是活动母图唯一索引。仓鼠弩手、仓鼠长弓、仓鼠突击、仓鼠重机枪、仓鼠骑兵与仓鼠翼骑兵已完成母图、豆包四动作、BiRefNet、RIFE 正式表与运行时全链接入；两名骑兵分别沿用轻骑兵和骑士的既有移动/冲锋契约，并已登记生产工厂、升级、部队线、头像、分类、科技归属、存档识别和后台模拟。靶场三档六兵种已完整打通，不再属于本待办。
- 未来目标：继续完成仓鼠侦察游骑兵和仓鼠动力爆矛重骑兵；逐单位生成待机、移动、攻击和死亡四类动作，并通过正式精灵表门禁后接入对应生产槽位。
- 前置条件：逐单位先确定攻击方式、武器接触/开火帧、移动姿态和死亡残留策略；骑兵动作必须同时锁定仓鼠、猫、武器、鞍具及助推器等刚性装备。侦察步枪与爆矛/助推器的战斗机制、数值和特效必须在运行时开发前单独确认，不以母图倒推未授权机制。
- 验收口径：四类动作保持角色身份、视角、体高、脚线和装备结构稳定，并完成项目要求的正式插帧与黑帧门禁；双份配置、实体/AI、预载与工厂、UI 图标、建筑生产、科技所有权、单位升级、存档和后台模拟全部接通后，才能将对应单位从本待办移除并写入 `CHANGELOG.md`。
